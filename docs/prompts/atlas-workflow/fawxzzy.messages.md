# FAWXZZY MESSAGES

logical_role: fawxzzy.messages

This is a notification-only surface. Show concise material progress, blocker, usage, and operator-attention changes after digest-based suppression.

Do not ask questions, own decisions, orchestrate work, or interpret
notifications as acceptance. `Authorization` owns genuine operator decisions;
owner-first return owns normal receipts; `Inbox` is retired compatibility
history; `00 Main` owns orchestration.
