# Knowledge Capture And Transfer Recipe And Promotion-Surface Routing Family Shaping Pass 5 - 2026-05-29

- Date: `2026-05-29`
- Lane: `Knowledge Capture & Transfer recipe and promotion-surface routing family shaping pass 5`
- Mode: `docs-only root-bounded family shaping`
- Source surfaces:
  - `docs/ops/KNOWLEDGE-CAPTURE-AND-TRANSFER-DECISIVE-RECEIPT-AND-BLOCKED-WORK-LADDER-SHAPING-PASS-1-2026-05-29.md`
  - `docs/ops/KNOWLEDGE-CAPTURE-AND-TRANSFER-CROSS-LANE-CLOSEOUT-REASONING-CARRY-FORWARD-FAMILY-SHAPING-PASS-3-2026-05-29.md`
  - `docs/ops/KNOWLEDGE-CAPTURE-AND-TRANSFER-CAPTURE-SOURCE-AND-DOCTRINE-COMPRESSION-FAMILY-SHAPING-PASS-4-2026-05-29.md`
  - `docs/ops/UNIFIED-PLAYBOOK-CORE-PATTERN-HANDOFF-2026-05-24.md`
  - `docs/ops/CORE-PATTERN-DOCTRINE-ROUTING-PASS-1-2026-05-24.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/08-workflow-recipes.md`
  - `docs/atlas-book/11-system-map-graph.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/atlas-book/13-vision-and-endgames.md`
  - `docs/memory/README.md`
- Control-plane checkpoint: `main@9bb230c`

## Objective

Shape the current `recipe and promotion-surface routing family` into one exact operator-usable map that says which root-visible recipe surfaces route work into `Knowledge Capture & Transfer`, and which promotion surfaces route bounded evidence from ATLAS consequence into later Playbook-owned reusable governance.

This pass does not:

- execute doctrine promotion
- admit a continuity manifest for this lane
- reopen source/doctrine shaping
- widen into broad promotion strategy
- move code, repos, runtime, schema, env, or deploy state
- move the marker

## Root State

- branch: `main`
- HEAD: `9bb230c`
- status: shared root spine already dirty from adjacent durable work; this pass stays within bounded restart and continuity surfaces
- validation: green before shaping at `critical=0 error=0 warning=478`

## Family Ambiguity Before This Pass

Before this pass, the lane had already frozen its carry-forward and source/doctrine boundaries, but it still lacked one exact answer to:

- which recipes route repeated or promoted workflow evidence into KCT
- which surfaces own the handoff from ATLAS consequence into later Playbook-facing promotion
- where routing stops so KCT does not expand into generic promotion prose or premature manifest work

That ambiguity kept the manifest-admission family wider than necessary.

## Exact Recipe And Promotion-Surface Routing Map Frozen In This Pass

### Recipe routing classes

1. `generalized-workflow recipe trigger class`
   - surfaces:
     - `docs/atlas-book/08-workflow-recipes.md`
   - role:
     - Recipe 2 explicitly routes KCT when proof workflow lessons become generalized beyond one repo or local proof lane

2. `promoted-workflow recipe trigger class`
   - surfaces:
     - `docs/atlas-book/08-workflow-recipes.md`
   - role:
     - Recipe 4 explicitly routes KCT when Discord feedback workflow lessons become promoted rather than staying bounded to one operational thread

3. `pattern-and-doctrine extraction recipe class`
   - surfaces:
     - `docs/atlas-book/08-workflow-recipes.md`
   - role:
     - Recipe 10 defines the bounded owner, proof, receipt, and promotion path for receipt-backed repeated rules, patterns, and failure modes that should become reusable doctrine

### Promotion-surface routing classes

1. `ATLAS consequence routing class`
   - surfaces:
     - `docs/ops/UNIFIED-PLAYBOOK-CORE-PATTERN-HANDOFF-2026-05-24.md`
   - role:
     - freezes that ATLAS records stack-facing consequence, convergence state, and receipt linkage before anything becomes Playbook-owned governance

2. `Playbook-facing promotion routing class`
   - surfaces:
     - `docs/ops/CORE-PATTERN-DOCTRINE-ROUTING-PASS-1-2026-05-24.md`
   - role:
     - freezes the admission threshold for when evidenced rules, patterns, and failure modes are stable enough to route into later Playbook-owned reusable doctrine

3. `governed summary and promotion taxonomy class`
   - surfaces:
     - `docs/memory/README.md`
   - role:
     - freezes the difference between canonical retrieval surfaces and governed summary/promotion surfaces so routing does not widen into transcript residue or a parallel mutable truth store

## Exact Ambiguity Resolution

The ambiguity is now resolved as:

- KCT routing begins when a bounded workflow lesson becomes either:
  - `generalized` enough to apply beyond one local workflow
  - `promoted` enough to require durable doctrine handling
  - `receipt-backed` enough to pass through the extraction recipe
- KCT routing passes first through ATLAS consequence surfaces
- only after that may a later family ask whether the lane is compressed enough for manifest-backed continuity
- routing surfaces do not themselves admit the continuity manifest, and manifest pressure does not reopen recipe or promotion routing once these classes are frozen

This family therefore does not ask whether KCT should eventually promote doctrine.

It freezes when routing begins, which surfaces own it, and where that routing stops.

## Exact Downstream Family Order Frozen In This Pass

The downstream family order is now:

1. `manifest-admission compression family`
   - next family
   - reason:
     - the lane now has an exact carry-forward map, an exact source/doctrine map, and an exact routing map, so the next honest question is whether those surfaces are compressed enough for manifest-backed restart

## Exact Shaping Decision

`one decisive recipe/promotion-routing-family shaping move completed`

Completed result:

- one exact recipe-routing map now exists
- one exact promotion-surface routing map now exists
- one exact downstream family order now exists

## Marker Decision

Hold:

- `Knowledge Capture & Transfer: 81% -> 81%`

Why:

- the family is clearer
- no manifest-backed continuity widening occurred
- no new capture or promotion class executed
- this pass shaped routing boundaries rather than increasing lane strength

## What This Pass Proves

This pass proves:

- KCT no longer has to infer when a workflow lesson becomes a KCT routing event
- the ATLAS-to-Playbook promotion path is now exact enough to isolate manifest-admission as the only remaining family in the current ladder
- the lane can now ask one final continuity question without reopening adjacent family ambiguity

This pass does not prove:

- that the lane is already compact enough for a continuity manifest
- that the marker should move
- that Playbook-facing promotion should execute now

## Exact Recommended Next Move

`Knowledge Capture & Transfer manifest-admission compression family shaping pass 6`

## Rule

Freeze the recipe triggers and promotion-routing ownership before deciding whether the lane is compact enough for manifest-backed continuity.

## Pattern

freeze carry-forward boundary -> freeze source/doctrine boundary -> freeze recipe/promotion routing -> evaluate manifest admission last

## Failure Mode

KCT keeps treating “generalized” and “promoted” as informal operator instincts instead of governed routing states, so the lane never gets compact enough to know whether a continuity manifest would be honest.
