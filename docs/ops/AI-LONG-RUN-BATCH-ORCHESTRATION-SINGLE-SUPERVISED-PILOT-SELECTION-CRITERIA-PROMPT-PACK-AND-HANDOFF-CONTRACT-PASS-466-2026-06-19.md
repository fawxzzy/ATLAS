# AI Long-Run Batch Orchestration Single Supervised Pilot Selection Criteria Prompt-Pack And Handoff Contract Pass 466 - 2026-06-19

- Date: `2026-06-19`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded handoff-contract design`
- Scope: `freeze the exact worker objective, proof obligations, allowed-touch boundary, and stop conditions for the already-admitted fail-closed pilot-selection-criteria slice`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-CONTRACT-FREEZE-PASS-462-2026-06-18.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-OWNER-SURFACE-ADMISSION-PASS-463-2026-06-19.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-SUPPORTING-LANE-ADMISSION-PASS-464-2026-06-19.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-FIRST-IMPLEMENTATION-ADMISSION-PASS-465-2026-06-19.md`
- Control-plane checkpoint: `main`

## Objective

Freeze one compact authoritative prompt-pack and handoff contract for the already-admitted first implementation slice of the root-owned `single supervised pilot selection criteria` family.

This pass does not:

- implement code
- choose, compare, rank, or discover real repo candidates
- infer `_stack` execution-home ownership
- reopen owner-repo implementation, deploy/publication, `.env`, or secret work
- widen into awareness integration, selector redesign, or broader orchestration doctrine

## Root Health Baseline

- pass 462 already froze the exact pilot-selection criteria contract
- pass 463 already admitted ATLAS root as the owner-facing home
- pass 464 already proved separate support still honestly holds at `none yet`
- pass 465 already froze the exact first implementation slice around one fail-closed pilot-admission card, one fail-closed classifier, one bounded rejection-reason set, and one preserved no-comparison boundary
- root validation is currently clean at `critical=0 error=0 warning=7 info=0`

## Inherited Contract Spine

The future worker must inherit exactly:

- pass 462 exact pilot-selection criteria contract
- pass 463 root control-plane owner admission
- pass 464 supporting-lane hold at `none yet`
- pass 465 exact first implementation slice and exact proof matrix

No worker may reinterpret those seams as suggestions.
They are contract inputs.

## Exact Worker Objective

The next worker is allowed to pursue one exact implementation objective only:

- implement one root-local fail-closed pilot-criteria validator in `ops/atlas/pilot_selection_criteria.py` plus one direct proof file in `tests/test_atlas_pilot_selection_criteria.py` so the helper preserves only the admitted card fields, classifies only `admissible` versus `not_admissible`, fails closed on missing or widened control fields, keeps the no-comparison boundary intact, and proves behavior against the frozen pass-465 matrix

The worker is not allowed to pursue:

- real repo discovery
- candidate ranking or adjacency scoring
- branch or worktree enumeration from live repos
- `_stack` execution-home routing
- owner-repo edits
- broader awareness, selector, manifest, or restart-surface redesign

## Exact Preserved Criteria Surface

The worker must preserve exactly these surfaces:

- `owner_repo_count`
- `target_kind`
- `target_ref`
- `objective_summary`
- `allowed_write_scope`
- `checkpoint_surface`
- `verification_gate`
- `closeout_artifact`
- `park_or_escalation_rule`
- `protected_surface_exclusions`
- `status`
- `rejection_reasons`

The worker may render these surfaces only.
The worker may not widen them into candidate identity, repo inventory, worktree inventory, branch inventory, execution-home inference, or deploy/publication semantics.

## Exact Mandatory Proof Cases

The worker must satisfy exactly these proof cases:

1. complete bounded single-owner card
   - preserve `status` as `admissible`
   - preserve `rejection_reasons` as `[]`

2. owner count not equal to one
   - preserve `status` as `not_admissible`
   - preserve `owner_repo_count_not_one`

3. target not explicit
   - preserve `status` as `not_admissible`
   - preserve `target_not_explicit`

4. missing bounded-control fields
   - preserve `status` as `not_admissible`
   - preserve the exact missing-field reason only

5. missing or violated protected-surface boundary
   - preserve `status` as `not_admissible`
   - preserve either `protected_surface_exclusions_missing` or `protected_surface_violation`

6. preserved no-comparison boundary
   - the helper may validate one explicit card shape only
   - it may not discover, rank, compare, or choose real repo candidates

## Exact No-Mutation / No-Discovery Boundary

The worker must carry this wording forward verbatim:

`No-mutation and no-discovery guard: this packet may implement one explicit fail-closed pilot-admission-card validator plus direct proof for the already-admitted criteria fields and rejection reasons, but it may not discover or compare real repos, enumerate worktrees or branches, infer execution-home ownership, mutate queue, registry, runtime, session, merge, manifest, archive, repair, blocker, or owner-repo state, or widen into deploy, publication, .env, secret, or protected-surface work.`

## Exact No Hidden Transcript-State Inference Boundary

The worker must carry this rule forward:

- use only the cited durable surfaces in this receipt chain plus the admitted helper and test files
- do not infer candidate truth, owner readiness, execution-home meaning, or protected-surface exceptions from uncited transcript memory or broad root residue

## Exact Allowed Future Implementation Surfaces

The worker may touch only:

- `ops/atlas/pilot_selection_criteria.py`
- `tests/test_atlas_pilot_selection_criteria.py`

## Exact Forbidden Future Implementation Surfaces

The worker must not touch:

- `ops/atlas/marker_knockout_selector.py`
- `ops/atlas/continuity.py`
- `docs/atlas-book/*`
- `docs/memory/initiatives/*`
- owner repos under `repos/*`
- `_stack` helper-runtime or command-design surfaces
- `archive/`, `.vercel`, `.env*`, `secrets/`, screenshots, captures, `.playwright-mcp/`, or broad untracked root backlog

## Exact Stop Conditions

The worker must stop and return immediately if the slice requires:

- real repo discovery or candidate comparison
- worktree or branch enumeration from live repos
- execution-home inference or `_stack` ownership
- owner-repo edits
- deploy/publication, `.env`, secret, archive/delete, or protected-surface touch
- new status values, new rejection reasons, or widened criteria fields beyond the admitted set

If one of those triggers appears, the work is no longer valid first-slice implementation work.

## Exact Handoff Prompt-Pack Spine

Any future worker prompt-pack must include:

1. the exact implementation objective
2. the inherited passes 462 through 465 as frozen inputs
3. the exact preserved criteria surface
4. the exact proof matrix
5. the exact no-mutation and no-discovery guard verbatim
6. the exact no-hidden-transcript-state boundary
7. the exact allowed-touch and forbidden-touch surfaces
8. the exact stop-and-return conditions

Mirror surfaces may restate this spine.
They may not redefine it.

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot selection criteria implementation-readiness closeout and worker-routing pass 467`

## Marker Decision

- `none`

Why:

- this pass narrows the worker handoff contract only
- no new executed state, broader proof-backed adoption, or blocker clearance landed

## Rule

Freeze the worker handoff contract before authorizing any first-slice implementation work for the root-owned pilot-selection criteria family.

## Failure Mode

`Pilot-Criteria Handoff Drift`

This family becomes dishonest when the worker handoff contract stays implicit and the first criteria slice expands through prompt wording into repo discovery, candidate comparison, execution-home inference, or protected-surface exceptions that the durable chain has not admitted.
