# AI Long-Run Batch Orchestration Single Supervised Pilot Selected-Pilot Owner-Repo Mutation Prompt-Pack And Handoff Contract Pass 505 - 2026-06-21

- Date: `2026-06-21`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded prompt-pack and handoff contract`
- Scope: `freeze the exact worker objective, proof obligations, allowed-touch boundary, and stop conditions for the already-admitted fail-closed selected-pilot owner-repo mutation slice`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-OWNER-REPO-MUTATION-CONTRACT-FREEZE-PASS-501-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-OWNER-REPO-MUTATION-OWNER-SURFACE-ADMISSION-PASS-502-2026-06-21.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-OWNER-REPO-MUTATION-SUPPORTING-LANE-ADMISSION-PASS-503-2026-06-21.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-OWNER-REPO-MUTATION-FIRST-IMPLEMENTATION-ADMISSION-PASS-504-2026-06-21.md`
- Control-plane checkpoint: `main`

## Objective

Freeze one compact authoritative prompt-pack and handoff contract for the already-admitted first implementation slice of the root-owned `single supervised pilot selected-pilot owner-repo mutation` family.

This pass does not:

- implement code
- choose one concrete owner-side mutation packet, repo edit, branch, worktree, or execution home by adjacency
- reopen implementation-routing redesign, owner-repo implementation redesign, selector redesign, or queue-or-registry doctrine
- infer `_stack` execution-home ownership, actual owner-repo mutation authority, or Playbook doctrine export posture
- reopen deploy/publication, archive/delete, `.env`, or secret work
- widen into manifest, restart-surface, runtime-state, or broader orchestration redesign

## Root Health Baseline

- pass 501 already froze the exact selected-pilot owner-repo mutation contract
- pass 502 already admitted ATLAS root as the owner-facing home for that mutation seam
- pass 503 already proved separate support still honestly holds at `none yet`
- pass 504 already froze the exact first implementation slice and exact proof matrix around the admitted `selection_status`, `selection_reasons`, `routing_status`, `implementation_route`, `routing_reasons`, `implementation_status`, `owner_repo_implementation`, `implementation_reasons`, `mutation_status`, `owner_repo_mutation`, and `mutation_reasons` surfaces
- root validation is currently clean at `critical=0 error=0 warning=11 info=0`

## Inherited Contract Spine

The future worker must inherit exactly:

- pass 501 exact selected-pilot owner-repo mutation contract
- pass 502 root control-plane owner admission
- pass 503 supporting-lane hold at `none yet`
- pass 504 exact first implementation slice and exact proof matrix

No worker may reinterpret those seams as suggestions.
They are contract inputs.

## Exact Worker Objective

The next worker is allowed to pursue one exact implementation objective only:

- implement one root-local fail-closed selected-pilot owner-repo mutation helper in `ops/atlas/pilot_selected_owner_repo_mutation.py` plus one direct proof file in `tests/test_atlas_pilot_selected_owner_repo_mutation.py` so the helper preserves only `selection_status`, `selection_reasons`, `routing_status`, `implementation_route`, `routing_reasons`, `implementation_status`, `owner_repo_implementation`, `implementation_reasons`, `mutation_status`, `owner_repo_mutation`, and `mutation_reasons`, converts only an explicit `owner_repo_implementation_admissible` implementation result with one explicit `owner_repo_implementation` card and empty implementation reasons into the exact explicit mutation candidate card already present in the bundle, emits only `owner_repo_mutation_admissible` or `no_owner_repo_mutation`, reports only the admitted `mutation_reasons`, fails closed on hidden or widened mutation inputs, and proves behavior against the frozen pass-504 matrix

The worker is not allowed to pursue:

- live repo discovery
- branch or worktree enumeration from live repos
- actual owner-repo mutation authority, owner-repo edits, or owner-readiness inference
- `_stack` execution-home inference or command/runtime design
- Playbook doctrine export
- broader selector, manifest, restart-surface, runtime-inventory, or queue-or-registry redesign

## Exact Preserved Mutation Surface

The worker must preserve exactly these surfaces:

- `selection_status`
- `selection_reasons`
- `routing_status`
- `implementation_route`
- `routing_reasons`
- `implementation_status`
- `owner_repo_implementation`
- `implementation_reasons`
- `mutation_status`
- `owner_repo_mutation`
- `mutation_reasons`

The worker may render these surfaces only.
The worker may not widen them into live repo inventory, branch inventory, worktree inventory, actual owner-repo mutation authority, owner-readiness doctrine, `_stack` execution-home choice, Playbook doctrine export, deploy/publication semantics, or protected-surface exceptions.

## Exact Mandatory Proof Cases

The worker must satisfy exactly these proof cases:

1. explicit `owner_repo_implementation_admissible` result with empty implementation reasons
   - preserve `mutation_status` as `owner_repo_mutation_admissible`
   - preserve `owner_repo_mutation` as the explicit `owner_repo_implementation` card
   - preserve `mutation_reasons` as `[]`

2. `no_owner_repo_implementation` implementation status
   - preserve `mutation_status` as `no_owner_repo_mutation`
   - preserve `owner_repo_mutation` as `null`
   - preserve `implementation_status_not_owner_repo_implementation_admissible`

3. non-empty implementation reasons despite `owner_repo_implementation_admissible`
   - preserve `mutation_status` as `no_owner_repo_mutation`
   - preserve `owner_repo_mutation` as `null`
   - preserve `implementation_reasons_present`

4. missing or non-explicit `owner_repo_implementation`
   - preserve `mutation_status` as `no_owner_repo_mutation`
   - preserve either `owner_repo_implementation_missing` or `owner_repo_implementation_not_explicit`

5. mutation argument invents live repo discovery, branch/worktree enumeration, `_stack` execution-home inference, actual owner-repo mutation authority, or Playbook doctrine export
   - preserve `mutation_status` as `no_owner_repo_mutation`
   - preserve the exact corresponding mutation reason

6. preserved `owner_repo_implementation` violates the admitted protected-surface boundary
   - preserve `mutation_status` as `no_owner_repo_mutation`
   - preserve `protected_surface_violation`

## Exact No-Mutation / No-Discovery / No-Owner-Mutation-By-Adjacency Boundary

The worker must carry this wording forward verbatim:

`No-mutation, no-discovery, and no-owner-mutation-by-adjacency guard: this packet may implement one explicit fail-closed selected-pilot owner-repo mutation helper plus direct proof for the already-admitted selection_status, selection_reasons, routing_status, implementation_route, routing_reasons, implementation_status, owner_repo_implementation, implementation_reasons, mutation_status, owner_repo_mutation, and mutation_reasons surfaces, but it may not discover live repos beyond the explicit preserved owner_repo_implementation card, enumerate worktrees or branches, infer actual owner-repo mutation authority, owner readiness, Playbook doctrine export, or _stack execution-home ownership, widen one mutation-admissible card into owner-side mutation or _stack execution-home choice, mutate queue, registry, runtime, session, merge, manifest, archive, repair, blocker, or owner-repo state, or widen into deploy, publication, .env, secret, or protected-surface work.`

## Exact No Hidden Transcript-State Inference Boundary

The worker must carry this rule forward:

- use only the cited durable surfaces in this receipt chain plus the new owner-repo mutation helper/test files
- do not infer mutation truth, owner readiness, execution-home meaning, or protected-surface exceptions from uncited transcript memory or broad root residue

## Exact Allowed Future Implementation Surfaces

The worker may touch only:

- `ops/atlas/pilot_selected_owner_repo_mutation.py`
- `tests/test_atlas_pilot_selected_owner_repo_mutation.py`

## Exact Forbidden Future Implementation Surfaces

The worker must not touch:

- `ops/atlas/marker_knockout_selector.py`
- `ops/atlas/continuity.py`
- `ops/atlas/pilot_selection_criteria.py`
- `ops/atlas/pilot_candidate_comparison.py`
- `ops/atlas/pilot_winner_conversion.py`
- `ops/atlas/pilot_winner_selection.py`
- `ops/atlas/pilot_selected_implementation_routing.py`
- `ops/atlas/pilot_selected_owner_repo_implementation.py`
- `tests/test_atlas_pilot_selection_criteria.py`
- `tests/test_atlas_pilot_candidate_comparison.py`
- `tests/test_atlas_pilot_winner_conversion.py`
- `tests/test_atlas_pilot_winner_selection.py`
- `tests/test_atlas_pilot_selected_implementation_routing.py`
- `tests/test_atlas_pilot_selected_owner_repo_implementation.py`
- `docs/atlas-book/*`
- `docs/memory/initiatives/*`
- owner repos under `repos/*`
- `_stack` helper-runtime or command-design surfaces
- `archive/`, `.vercel`, `.env*`, `secrets/`, screenshots, captures, `.playwright-mcp/`, or broad untracked root backlog

## Exact Stop Conditions

The worker must stop and return immediately if the implementation requires:

- real repo discovery or mutation routing beyond the explicit preserved `owner_repo_implementation` card
- worktree or branch enumeration from live repos
- actual owner-repo mutation authority, owner-repo edits, Playbook doctrine export, or `_stack` execution-home inference
- deploy/publication, `.env`, secret, archive/delete, or protected-surface touch
- new mutation-status values, new mutation-reason values, or widened implementation/mutation fields beyond the admitted set
- selector, manifest, restart-surface, or runtime-state edits to make the helper work

If one of those triggers appears, the work is no longer valid first-slice implementation work.

## Exact Handoff Prompt-Pack Spine

Any future worker prompt-pack must include:

1. the exact mutation objective
2. the inherited passes 501 through 504 as frozen inputs
3. the exact preserved mutation surface
4. the exact proof matrix
5. the exact no-mutation, no-discovery, and no-owner-mutation-by-adjacency guard verbatim
6. the exact no-hidden-transcript-state boundary
7. the exact allowed-touch and forbidden-touch surfaces
8. the exact stop-and-return conditions

Mirror surfaces may restate this spine.
They may not redefine it.

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot selected-pilot owner-repo mutation implementation-readiness closeout and worker-routing pass 506`

## Marker Decision

- `none`

Why:

- this pass narrows the worker handoff contract only
- no new executed state, broader proof-backed adoption, or blocker clearance landed

## Rule

Freeze the worker handoff contract before authorizing any first-slice implementation work for the root-owned selected-pilot owner-repo mutation family.

## Failure Mode

`Selected-Pilot-Owner-Repo-Mutation Handoff Drift`

This family becomes dishonest when the worker handoff contract stays implicit and the first implementation slice expands through prompt wording into actual owner-repo mutation, live repo discovery, Playbook doctrine export, `_stack` execution-home inference, or protected-surface exceptions that the durable chain has not admitted.
