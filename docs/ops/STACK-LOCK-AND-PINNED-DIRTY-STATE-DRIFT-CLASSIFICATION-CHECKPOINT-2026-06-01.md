# Stack Lock And Pinned Dirty-State Drift Classification Checkpoint - 2026-06-01

- Date: `2026-06-01`
- Owner: ATLAS root
- Mode: `docs-only root classification`
- Scope: `remaining lock-registry-hygiene blocker classification after lane-selection closeout`
- Source surfaces:
  - `docs/ops/ROOT-BOUNDED-LANE-SELECTION-PASS-AFTER-OPERATOR-SECRET-PATH-HYGIENE-FITNESS-QA-AUTH-PROOF-RECEIPT-PATH-DISCIPLINE-NORMALIZATION-RECONCILIATION-PASS-6-CLOSEOUT-2026-06-01.md`
  - `docs/ops/STACK-LOCKFILE.md`
  - `docs/ops/ATLAS-ROOT-SELF-LOCK-POLICY-DECISION-2026-05-25.md`
  - `docs/ops/WAVE-1-ROOT-RECONCILIATION-AFTER-WORKER-A-AND-WORKER-B-2026-05-30.md`
  - `docs/ops/WAVE-2-ROOT-RECONCILIATION-AFTER-WORKER-A-AND-WORKER-B-2026-05-31.md`
  - `runtime/receipts/validation/stack-validation.latest.md`

## Objective

Classify the five remaining validator-visible `lock-registry-hygiene` errors without drifting into owner-side remediation, determine which residuals are true root-owned work versus pinned child-repo drift, refresh restart truth, and freeze the next honest execution path.

## Done

- classified all five live errors
- separated root-owned lock-surface responsibility from child-owned dirty-state cause
- refreshed the ATLAS Book so restart truth no longer implies another immediate root-only packet

## Now

- live validation remains `critical=0 error=5 warning=489 info=0`
- the only blocking validator class remains `lock-registry-hygiene`
- the residual class is now frozen as one mixed surface: root-owned lockfile visibility plus child-owned dirty-state cause

## Next

- no immediate root-only packet is open
- exact next execution path is owner-side dirty-state disposition in `repos/_stack`, `repos/mazer`, and `repos/playbook`
- return to root only after one of two state changes:
  - the three child repos are restored to the pinned clean posture
  - the current dirty posture is intentionally admitted as the new pinned truth and a bounded lock-refresh packet becomes honest

## Repo Health Check

- validation: `critical=0 error=5 warning=489 info=0`
- live errors remain limited to:
  - `stack.lock.yaml` pair
  - pinned dirty-state drift for `_stack`
  - pinned dirty-state drift for `mazer`
  - pinned dirty-state drift for `playbook`

## Evidence Considered

- the current validator report records only five blocking findings, all inside `lock-registry-hygiene`
- `STACK-LOCKFILE.md` freezes that dirty-state mismatches are `stack-lock-worktree-drift`, not proof that refresh is automatically justified
- the same lock policy freezes that refresh is allowed only when the current live working set is the intended pinned truth
- Wave 1 and Wave 2 root reconciliation already froze that any later lock refresh must stay in its own bounded packet and must not be smuggled into reconciliation
- live child-repo status shows `_stack`, `mazer`, and `playbook` are all currently dirty while `stack.lock.yaml` still pins each of them as `dirty: false`
- no evidence shows a generator bug, stale validator artifact, or root self-lock regression

## Residual Error Classification

| Residual | Classification | Owner | Interpretation | Routing consequence |
| --- | --- | --- | --- | --- |
| `stack.lock.yaml`: pinned working set mismatch | `expected temporary hold` | `root surface, child-caused` | the root-owned lockfile no longer matches live stack state because the pinned child repos are dirtier than the committed lock expects | do not refresh by implication; wait for child disposition or an intentional repin decision |
| `stack.lock.yaml`: canonical payload byte mismatch | `expected temporary hold` | `root surface, child-caused` | the canonical generated bytes differ for the same reason as the working-set mismatch; no evidence shows a generator or validator normalization bug | treat as derivative artifact of the same live drift, not as a second independent root cleanup lane |
| `stack.lock.yaml#_stack`: pinned dirty `false` vs live dirty `true` | `pinned owner-repo drift` | `_stack` | observational mismatch against the child repo's current worktree state | owner-side disposition first |
| `stack.lock.yaml#mazer`: pinned dirty `false` vs live dirty `true` | `pinned owner-repo drift` | `mazer` | observational mismatch against the child repo's current worktree state | owner-side disposition first |
| `stack.lock.yaml#playbook`: pinned dirty `false` vs live dirty `true` | `pinned owner-repo drift` | `playbook` | observational mismatch against the child repo's current worktree state | owner-side disposition first |

## Root-Owned Vs Child-Owned Distinction

Root-owned:

- `stack.lock.yaml` remains a root-owned registry surface
- root owns the classification, restart truth, and later lock-refresh admissibility decision

Child-owned:

- the live dirty-state changes inside `repos/_stack`, `repos/mazer`, and `repos/playbook`
- any cleanup, preserve-as-dirty decision, or commit-level state conversion inside those repos

Important boundary:

- the current `stack.lock.yaml` pair is not a free-standing root implementation lane
- it is a root-visible artifact of child-owned drift until the intended child working-set truth becomes explicit

## `stack.lock.yaml` Pair Decision

Decision:

- classify the pair as `expected temporary hold`, not as immediate root-owned execution

Why:

1. both errors derive from the same live child-repo dirty-state mismatch already made explicit by the three component findings
2. current evidence does not show canonical payload instability on unchanged state
3. current evidence does not justify claiming the dirty child worktrees are the intended pinned truth
4. `STACK-LOCKFILE.md` explicitly forbids using refresh to hide unrelated inherited debt or unexpected repo movement

Not classified as:

- `genuine root-only hygiene lane`
- `misclassified or stale validator surface`
- `normalization bug in generator or validator`

## Selected Next Execution Path

Selected next execution path:

- owner-side dirty-state disposition for `_stack`, `mazer`, and `playbook`

Return condition for root:

- rerun root validation after owner-side disposition and only then decide whether a bounded stack-lock refresh packet is honestly justified

Why this won:

- it preserves repo ownership boundaries
- it matches the actual cause of the remaining blocker class
- it avoids fake root progress through a premature repin of dirty child state

## Hold / Defer Posture

- hold direct `stack.lock.yaml` refresh until the child-owned dirty-state truth is either cleared or intentionally admitted
- hold any new Local Data Gateway doctrine packet here; the prior lane-selection result already froze that it lost on leverage
- hold any new docs-only `_stack` first-slice packet; the current `_stack` docs lane is not the blocker
- hold markers flat; this pass is classification and restart-truth tightening only

## Marker Update

- `none`

Why:

- this pass reduces ambiguity around ownership and routing
- it does not clear the residual blocker class
- it does not widen capability, adoption, or execution truth

## Recommended Execution Path

- Codex, owner-side first:
  - classify and dispose the live dirty-state posture in `_stack`, `mazer`, and `playbook`
  - then rerun ATLAS root validation
  - if the residual class collapses to intended pinned truth, open one bounded root stack-lock refresh packet
  - otherwise keep the lock pair held and return with the changed blocker evidence

## Rule

When a root-owned lock surface is red only because child repos are live-dirty against a clean pin, root should classify and route the drift, not repin it by implication.

## Pattern

lane selection -> residual classification -> ownership split freeze -> owner-side state disposition -> validation recheck -> only then consider lock refresh

## Failure Mode

If root refreshes `stack.lock.yaml` before child working-set truth is intentionally settled, the stack can manufacture a false cleanup win that hides live owner-repo drift without actually converting the blocker.
