# AI Long-Run Batch Orchestration Single Supervised Pilot Selected-Pilot Actual Owner-Side Mutation Prompt-Pack And Handoff Contract Pass 512 - 2026-06-21

- Date: `2026-06-21`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded prompt-pack and handoff contract`
- Scope: `freeze the exact worker objective, proof obligations, allowed-touch boundary, and stop conditions for the already-admitted fail-closed selected-pilot actual owner-side mutation slice`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-ACTUAL-OWNER-SIDE-MUTATION-CONTRACT-FREEZE-PASS-508-2026-06-21.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-ACTUAL-OWNER-SIDE-MUTATION-OWNER-SURFACE-ADMISSION-PASS-509-2026-06-21.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-ACTUAL-OWNER-SIDE-MUTATION-SUPPORTING-LANE-ADMISSION-PASS-510-2026-06-21.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-ACTUAL-OWNER-SIDE-MUTATION-FIRST-IMPLEMENTATION-ADMISSION-PASS-511-2026-06-21.md`
- Control-plane checkpoint: `main`

## Objective

Freeze one compact authoritative prompt-pack and handoff contract for the already-admitted first implementation slice of the root-owned `single supervised pilot selected-pilot actual owner-side mutation` family.

This pass does not:

- implement code
- choose one concrete owner-side mutation packet, owner-repo edit, branch, worktree, or execution home by adjacency
- reopen owner-repo mutation redesign, owner-repo implementation redesign, selector redesign, or queue-or-registry doctrine
- infer `_stack` execution-home ownership, actual owner-side mutation authority, or Playbook doctrine export posture
- reopen deploy/publication, archive/delete, `.env`, or secret work
- widen into manifest, restart-surface, runtime-state, or broader orchestration redesign

## Root Health Baseline

- pass 508 already froze the exact selected-pilot actual owner-side mutation contract
- pass 509 already admitted ATLAS root as the owner-facing home for that actual-mutation seam
- pass 510 already proved separate support still honestly holds at `none yet`
- pass 511 already froze the exact first implementation slice and exact proof matrix around the admitted `selection_status`, `selection_reasons`, `routing_status`, `implementation_route`, `routing_reasons`, `implementation_status`, `owner_repo_implementation`, `implementation_reasons`, `mutation_status`, `owner_repo_mutation`, `mutation_reasons`, `actual_mutation_status`, `actual_owner_side_mutation`, and `actual_mutation_reasons` surfaces
- root validation is currently clean at `critical=0 error=0 warning=20 info=0`

## Inherited Contract Spine

The future worker must inherit exactly:

- pass 508 exact selected-pilot actual owner-side mutation contract
- pass 509 root control-plane owner admission
- pass 510 supporting-lane hold at `none yet`
- pass 511 exact first implementation slice and exact proof matrix

No worker may reinterpret those seams as suggestions.
They are contract inputs.

## Exact Worker Objective

The next worker is allowed to pursue one exact implementation objective only:

- implement one root-local fail-closed selected-pilot actual owner-side mutation helper in `ops/atlas/pilot_selected_actual_owner_side_mutation.py` plus one direct proof file in `tests/test_atlas_pilot_selected_actual_owner_side_mutation.py` so the helper preserves only `selection_status`, `selection_reasons`, `routing_status`, `implementation_route`, `routing_reasons`, `implementation_status`, `owner_repo_implementation`, `implementation_reasons`, `mutation_status`, `owner_repo_mutation`, `mutation_reasons`, `actual_mutation_status`, `actual_owner_side_mutation`, and `actual_mutation_reasons`, converts only an explicit `owner_repo_mutation_admissible` mutation result with one explicit `owner_repo_mutation` card and empty mutation reasons into the exact explicit actual-mutation candidate card already present in the bundle, emits only `actual_owner_side_mutation_admissible` or `no_actual_owner_side_mutation`, reports only the admitted `actual_mutation_reasons`, fails closed on hidden or widened actual-mutation inputs, and proves behavior against the frozen pass-511 matrix

The worker is not allowed to pursue:

- live repo discovery
- branch or worktree enumeration from live repos
- actual owner-side mutation authority, owner-repo edits, or owner-readiness inference
- `_stack` execution-home inference or command/runtime design
- Playbook doctrine export
- broader selector, manifest, restart-surface, runtime-inventory, or queue-or-registry redesign

## Exact Preserved Actual-Mutation Surface

The worker must preserve exactly these surfaces:

- `selection_status`
- `selection_reasons`
- `routing_status`
- `implementation_route`
- `routing_reasons`
- `implementation_status`
- `owner_repo_implementation`
- `implementation_reasons`
- `mutation_status`
- `owner_repo_mutation`
- `mutation_reasons`
- `actual_mutation_status`
- `actual_owner_side_mutation`
- `actual_mutation_reasons`

The worker may render these surfaces only.
The worker may not widen them into live repo inventory, branch inventory, worktree inventory, actual owner-side mutation authority, owner-repo edit authority, owner-readiness doctrine, `_stack` execution-home choice, Playbook doctrine export, deploy/publication semantics, or protected-surface exceptions.

## Exact Mandatory Proof Cases

The worker must satisfy exactly these proof cases:

1. explicit `owner_repo_mutation_admissible` result with empty mutation reasons
   - preserve `actual_mutation_status` as `actual_owner_side_mutation_admissible`
   - preserve `actual_owner_side_mutation` as the explicit `owner_repo_mutation` card
   - preserve `actual_mutation_reasons` as `[]`

2. `no_owner_repo_mutation` mutation status
   - preserve `actual_mutation_status` as `no_actual_owner_side_mutation`
   - preserve `actual_owner_side_mutation` as `null`
   - preserve `mutation_status_not_owner_repo_mutation_admissible`

3. non-empty mutation reasons despite `owner_repo_mutation_admissible`
   - preserve `actual_mutation_status` as `no_actual_owner_side_mutation`
   - preserve `actual_owner_side_mutation` as `null`
   - preserve `mutation_reasons_present`

4. missing or non-explicit `owner_repo_mutation`
   - preserve `actual_mutation_status` as `no_actual_owner_side_mutation`
   - preserve either `owner_repo_mutation_missing` or `owner_repo_mutation_not_explicit`

5. actual-mutation argument invents live repo discovery, branch/worktree enumeration, `_stack` execution-home inference, actual owner-side mutation authority, owner-repo edits, or Playbook doctrine export
   - preserve `actual_mutation_status` as `no_actual_owner_side_mutation`
   - preserve the exact corresponding actual-mutation reason

6. preserved `owner_repo_mutation` violates the admitted protected-surface boundary
   - preserve `actual_mutation_status` as `no_actual_owner_side_mutation`
   - preserve `protected_surface_violation`

## Exact No-Mutation / No-Discovery / No-Actual-Mutation-By-Adjacency Boundary

The worker must carry this wording forward verbatim:

`No-mutation, no-discovery, and no-actual-mutation-by-adjacency guard: this packet may implement one explicit fail-closed selected-pilot actual owner-side mutation helper plus direct proof for the already-admitted selection_status, selection_reasons, routing_status, implementation_route, routing_reasons, implementation_status, owner_repo_implementation, implementation_reasons, mutation_status, owner_repo_mutation, mutation_reasons, actual_mutation_status, actual_owner_side_mutation, and actual_mutation_reasons surfaces, but it may not discover live repos beyond the explicit preserved owner_repo_mutation card, enumerate worktrees or branches, infer actual owner-side mutation authority, owner-repo edit authority, Playbook doctrine export, or _stack execution-home ownership, widen one actual-mutation-admissible card into concrete owner-side mutation or _stack execution-home choice, mutate queue, registry, runtime, session, merge, manifest, archive, repair, blocker, or owner-repo state, or widen into deploy, publication, .env, secret, or protected-surface work.`

## Exact No Hidden Transcript-State Inference Boundary

The worker must carry this rule forward:

- use only the cited durable surfaces in this receipt chain plus the new actual owner-side mutation helper/test files
- do not infer actual-mutation truth, owner readiness, execution-home meaning, or protected-surface exceptions from uncited transcript memory or broad root residue

## Exact Allowed Future Implementation Surfaces

The worker may touch only:

- `ops/atlas/pilot_selected_actual_owner_side_mutation.py`
- `tests/test_atlas_pilot_selected_actual_owner_side_mutation.py`

## Exact Forbidden Future Implementation Surfaces

The worker must not touch:

- `ops/atlas/marker_knockout_selector.py`
- `ops/atlas/continuity.py`
- `ops/atlas/pilot_selection_criteria.py`
- `ops/atlas/pilot_candidate_comparison.py`
- `ops/atlas/pilot_winner_conversion.py`
- `ops/atlas/pilot_winner_selection.py`
- `ops/atlas/pilot_selected_implementation_routing.py`
- `ops/atlas/pilot_selected_owner_repo_implementation.py`
- `ops/atlas/pilot_selected_owner_repo_mutation.py`
- `tests/test_atlas_pilot_selection_criteria.py`
- `tests/test_atlas_pilot_candidate_comparison.py`
- `tests/test_atlas_pilot_winner_conversion.py`
- `tests/test_atlas_pilot_winner_selection.py`
- `tests/test_atlas_pilot_selected_implementation_routing.py`
- `tests/test_atlas_pilot_selected_owner_repo_implementation.py`
- `tests/test_atlas_pilot_selected_owner_repo_mutation.py`
- `docs/atlas-book/*`
- `docs/memory/initiatives/*`
- owner repos under `repos/*`
- `_stack` helper-runtime or command-design surfaces
- `archive/`, `.vercel`, `.env*`, `secrets/`, screenshots, captures, `.playwright-mcp/`, or broad untracked root backlog

## Exact Stop Conditions

The worker must stop and return immediately if the implementation requires:

- real repo discovery or mutation routing beyond the explicit preserved `owner_repo_mutation` card
- worktree or branch enumeration from live repos
- actual owner-side mutation authority, owner-repo edits, Playbook doctrine export, or `_stack` execution-home inference
- deploy/publication, `.env`, secret, archive/delete, or protected-surface touch
- new actual-mutation-status values, new actual-mutation-reason values, or widened implementation/mutation fields beyond the admitted set
- selector, manifest, restart-surface, or runtime-state edits to make the helper work

If one of those triggers appears, the work is no longer valid first-slice implementation work.

## Exact Handoff Prompt-Pack Spine

Any future worker prompt-pack must include:

1. the exact actual-mutation objective
2. the inherited passes 508 through 511 as frozen inputs
3. the exact preserved actual-mutation surface
4. the exact proof matrix
5. the exact no-mutation, no-discovery, and no-actual-mutation-by-adjacency guard verbatim
6. the exact no-hidden-transcript-state boundary
7. the exact allowed-touch and forbidden-touch surfaces
8. the exact stop-and-return conditions

Mirror surfaces may restate this spine.
They may not redefine it.

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot selected-pilot actual owner-side mutation implementation-readiness closeout and worker-routing pass 513`

## Marker Decision

- `none`

Why:

- this pass narrows the worker handoff contract only
- no new executed state, broader proof-backed adoption, or blocker clearance landed

## Rule

Freeze the worker handoff contract before authorizing any first-slice implementation work for the root-owned selected-pilot actual owner-side mutation family.

## Failure Mode

`Selected-Pilot-Actual-Owner-Side-Mutation Handoff Drift`

This family becomes dishonest when the worker handoff contract stays implicit and the first implementation slice expands through prompt wording into actual owner-side mutation authority, owner-repo edits, live repo discovery, Playbook doctrine export, `_stack` execution-home inference, or protected-surface exceptions that the durable chain has not admitted.
