# AI Long-Run Batch Orchestration Single Supervised Pilot Candidate Comparison Contract Freeze Pass 468 - 2026-06-20

- Date: `2026-06-20`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded contract freeze`
- Scope: `freeze the exact root-owned comparison contract for choosing the first supervised pilot candidate after the criteria validator is already real`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/memory/initiatives/continuity-manifest-ai-long-run-batch-orchestration.json`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/02-lanes-and-markers.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/11-system-map-graph.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/atlas-book/13-vision-and-endgames.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-CONTRACT-FREEZE-PASS-462-2026-06-18.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-OWNER-SURFACE-ADMISSION-PASS-463-2026-06-19.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-SUPPORTING-LANE-ADMISSION-PASS-464-2026-06-19.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-FIRST-IMPLEMENTATION-ADMISSION-PASS-465-2026-06-19.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-PROMPT-PACK-AND-HANDOFF-CONTRACT-PASS-466-2026-06-19.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-IMPLEMENTATION-READINESS-CLOSEOUT-AND-WORKER-ROUTING-PASS-467-2026-06-19.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTION-CRITERIA-FIRST-IMPLEMENTATION-WORKER-CLUSTER-RECONCILIATION-2026-06-19.md`
  - `ops/atlas/pilot_selection_criteria.py`
  - `tests/test_atlas_pilot_selection_criteria.py`

## Objective

Replace the still-implicit `compare real candidates` question with one exact root-owned contract that says how the first supervised pilot candidates must be compared against the now-implemented criteria surface, while staying below owner-surface admission, support admission, repo discovery, execution-home doctrine, owner-repo mutation, or protected-surface touch.

This pass does not:

- choose one winning pilot repo, worktree, branch, or execution home
- admit `_stack` helper-home, command-design, runtime-state, resume, merge, or dispatch semantics
- admit Playbook doctrine export
- admit owner-repo implementation, deploy/publication, archive/delete, `.env`, or secret work
- claim that comparison-contract freeze alone proves one candidate is already admissible

## Root State

- branch: `main`
- current working tree still contains unrelated preserved tracked and untracked residue outside this pass
- marker posture before this pass:
  - `AI Long-Run Batch Orchestration: 54%`

## Exact Weak Link Before Repair

Before this pass, current durable truth already established:

- one exact pilot-selection criteria contract
- one exact root-owned owner-facing home for those criteria
- one exact support decision that still honestly holds at `none yet`
- one exact fail-closed validator slice
- one exact prompt-pack and worker-routing chain
- one real validator helper plus direct proof on canonical `main`

But the lane still lacked one explicit contract for the next honest question:

- how real candidate repos or worktrees must be compared against that admitted criteria surface before any owner-surface or execution-home widening

That gap was small but real: restart truth could name `candidate comparison contract freeze pass 468` as the next package, but the comparison seam itself was not yet durable.

## Candidate Comparison

### Concrete pilot winner selection now

Why it does not win now:

- the lane still lacks one exact comparison contract that says what evidence classes, rejection rules, and tie discipline are admissible
- choosing a winner now would select by adjacency instead of the already-required contract
- owner repo identity and target specificity are inputs to the comparison, not the comparison contract itself

### `_stack` execution-home or command-home admission now

Why it does not win now:

- `_stack` ownership starts later, after one candidate is selected and the lane actually needs execution semantics
- this family is still pre-selection and root-owned by the original lane charter
- routing into `_stack` now would blur candidate admissibility with later supervised execution doctrine

### Playbook doctrine export now

Why it does not win now:

- reusable doctrine export still depends on proving that this exact candidate-comparison seam is useful in live lane routing first
- current truth still needs the local lane contract before any broader doctrine consumer is honest

### Single supervised pilot candidate comparison

Why it wins now:

- pass 467 and the worker-cluster reconciliation closed the criteria-validator blocker class
- the next honest blocker is no longer `what are the criteria`; it is `how do we compare real candidates against those criteria without widening into execution-home semantics`
- this is narrower than winner selection, `_stack` admission, or owner-repo mutation

## Frozen Contract

### `family_name`

- `single supervised pilot candidate comparison`

### `expected_contract_artifact`

- one exact root-owned comparison surface for candidate cards that already satisfy the admitted pilot-selection-criteria schema
- each candidate comparison input must preserve:
  - exactly one owner repo
  - exactly one explicit target worktree or branch
  - one bounded objective narrow enough for one reviewable closeout
  - one declared allowed write scope
  - one declared checkpoint surface
  - one declared verification gate
  - one declared closeout artifact
  - one declared park-or-escalation rule
  - one preserved protected-surface exclusion set
- the comparison must evaluate only admitted comparison dimensions:
  - criteria admissibility from the existing validator helper
  - blast-radius discipline of the declared write scope
  - proof readiness of the declared checkpoint and verification surfaces
  - reviewability of the declared closeout artifact
  - park-or-escalation clarity when the slice stalls
- the comparison may prefer:
  - candidates with more deterministic local proof
  - candidates with narrower declared write scope
  - candidates with cleaner checkpoint and closeout surfaces
  - candidates that stay root-visible and reviewable without live session recovery
- the comparison must fail closed if:
  - any candidate card is not admissible under the existing criteria helper
  - candidates hide required control fields behind prose-only rationale
  - a winning argument depends on undeclared deploy/publication, archive/delete, `.env`, or secret work
  - repo discovery, branch discovery, or worktree discovery is invented at comparison time instead of carried by explicit candidate inputs
  - execution-home, `_stack`, or owner-repo implementation semantics are used as tie-breakers before they are separately admitted

### `comparison_output_boundary`

- output is still contract-only at this stage
- no executable ranking helper, repo scan, or candidate winner is admitted here
- the comparison contract may define:
  - admissible evidence classes
  - fail-closed rejection classes
  - tie-handling discipline
  - the exact boundary between candidate comparison and later owner-surface admission
- it may not define:
  - `_stack` command runtime
  - owner-repo mutation
  - branch or worktree discovery
  - automatic winner selection by hidden scoring

### `owner_boundary`

- ATLAS root owns this comparison contract, the lane-level tie and rejection discipline, and the restart consequence of whether candidate comparison is explicit yet
- `_stack` remains a later owner only after one comparison winner creates a real execution-home question
- Playbook remains a later consumer only after the comparison contract is proven useful beyond this exact lane state
- owner repos remain the owners of repo-local implementation and verification truth

## Exact Marker Decision

Ratcheted:

- `AI Long-Run Batch Orchestration: 54% -> 55%`

Why the move is honest:

- one real blocker class is now cleared
- the lane no longer relies on an implied comparison seam between the criteria validator and later candidate choice
- restart truth can now point at one exact comparison contract instead of only narrating that such a contract should exist next

Why it still stays low:

- no concrete pilot candidate is selected yet
- no `_stack` execution-home semantics are admitted yet
- no owner-side pilot proof or adoption widening happened here

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot candidate comparison owner-surface admission pass 469`

Why:

- the comparison contract is now exact
- the next honest question is which surface owns the producing and consuming home for that comparison before support, implementation, or winner selection is discussed
- selecting a candidate winner would still be premature before that owner-facing boundary is frozen

## Rule

`Comparison Contract Before Candidate Winner`

When the criteria are already explicit and directly proved, freeze the exact comparison contract for real candidate cards before owner-surface admission, winner selection, execution-home doctrine, or reusable export.

## Failure Mode

`Pilot Winner By Adjacency`

This lane becomes synthetic when one repo, worktree, or branch is treated as the obvious first supervised pilot before the candidate-comparison contract makes the rejection rules, tie discipline, and admitted evidence classes explicit.
