# ATLAS Contracts v2 Cluster 5 MarkerEvidence Adoption

## Accepted Result

MarkerEvidence is independently accepted as the tenth Atlas Contracts v2 family. The accepted chain is the canonical Contracts Mesh registry producer, exact `atlas.marker-evidence.v2` package validation, the independent read-only root admission consumer, stable negative conformance, deterministic correlated consumer receipt, this durable receipt, and reconciled machine/human projections.

The preflight baseline was ATLAS `main@684d14337111c35f7d8c07a51cd9456ad1452993`, DiscordOS `main@876b30e17733b6cb3c3c89a667b5d546be09b4c6`, and `_stack main@40ab40f80ac914dd9bd59dbb64272be3ed1cf70f`. Each was at `0/0` upstream parity. ATLAS had no tracked dirt; its known operator-owned untracked scratch directories were observed only at name level and were not entered, modified, staged, cleaned, moved, renamed, deleted, or normalized.

## Root-Owned Changed Surfaces

- `ops/atlas/marker_evidence_admission.mjs` and its focused test under `tests/`;
- the combined adoption validator and its inherited-family rejection suite;
- this Cluster 5 receipt and the Contracts package README;
- the authoritative Contracts Mesh lane and Atlas Book projections;
- deterministic Atlas/Cortex owner exports regenerated by `ops/atlas/project_board_owner_export.py`.

No schema, package fixture, CardRecord, BoardEvent, owner-repository file, `stack.lock.yaml`, stack inventory, parent audit marker, Marker Integrity unit, or KnowledgeCandidate implementation changed.

## Producer Evidence

`docs/registry/ATLAS-FULL-SYSTEM-REEVALUATION-LANES.json` remains the marker producer truth. Its exact lane identity is `lane-atlas-contracts-mesh`, its scope and denominator basis remain unchanged, and its accepted-family state is now numerator `10`, denominator `11`, rounded percentage `91`, measurement unit `schema-family acceptance`, and audit time `2026-07-15T15:30:00Z`.

The deterministic producer in `ops/atlas/validate_contracts_v2_adoption.mjs` projects that registry lane into an `atlas.marker-evidence.v2` artifact with:

- 2,040 canonical bytes and digest `sha256:430870d839ae341b67c9fdfebdbe85de3f06e2945fc5268feb22ffa05c104a20`;
- exact transition `82 -> 91`, representing one accepted family from `9/11` to `10/11`;
- freshness status `current`, measured at the lane's exact audit time;
- rollup policy `child-evidence-no-rollup`;
- no external, marker, parent-marker, Discord, board, data, deploy, or production authority.

The canonical schema is `atlas.marker-evidence.v2` from `@atlas/contracts@0.1.0`, with digest `sha256:1f71644deaa8a930717b7338b8dade46fb8b31cd104a2e9532f410c094959450`. The consumer invokes `packages/atlas-contracts/scripts/validate-artifact.mjs`; it does not copy or replace package schema semantics.

## Independent Admission Consumer

`ops/atlas/marker_evidence_admission.mjs` is a separate, executable, fail-closed consumer. It admits only exact workspace-local inputs and the canonical registry, invokes the Atlas-owned MarkerEvidence, JobEnvelope, and ExecutionReceipt validators, and then proves the cross-artifact semantics JSON Schema does not own:

- marker, scope, parent, measurement-unit, denominator-kind, and exact eleven-family denominator-basis identity;
- numerator, denominator, registry percentage, and rounded math consistency;
- current freshness covering the correlated execution receipt;
- single-family transition identity from the prior accepted count;
- explicit non-rollup behavior and unchanged parent marker;
- exact evidence-reference identity;
- JobEnvelope, ExecutionReceipt, marker digest, test, blocker, and authority correlation;
- read-only behavior with zero changed paths, commits, authority actions, external mutation, or marker mutation.

Identical replay emits consumer receipt `amer_dd0d6257693e43f536592c43260f4661` and result identity `ameres_2246a5e04bd65aea5c29098e9494bc0e`. The stable input identities are:

| Input | SHA-256 |
| --- | --- |
| MarkerEvidence | `sha256:430870d839ae341b67c9fdfebdbe85de3f06e2945fc5268feb22ffa05c104a20` |
| JobEnvelope | `sha256:ce1a51771d71728e177a00dd3cae4654640677b7dd833fe03f40e7a949d0f126` |
| ExecutionReceipt | `sha256:15e7c467c3cda4396fb7c6ee69bbd41a95d5daf3480318c6ee9d12563b08a346` |
| source registry | `sha256:12a3191cfed22da71f7b81b9a986e7c7571136bf6dc5154ad9fa6fec6152ace6` |

The consumer receipt status is `accepted_read_only`; all seven conformance flags are true; `external_mutation=false`, `marker_mutation=false`, and `parent_marker_movement=false`.

## Stable Rejection Matrix

| Rejected drift | Stable reason |
| --- | --- |
| numerator, denominator, registry percentage, or rounded math drift | `MARKER_PERCENTAGE_MISMATCH` |
| prior/current transition or single-family count drift | `MARKER_TRANSITION_MISMATCH` |
| stale status, audit-time drift, or expired receipt coverage | `MARKER_STALE` |
| marker, scope, registry, parent, unit, or denominator-kind drift | `MARKER_SCOPE_MISMATCH` |
| JobEnvelope, ExecutionReceipt, digest, verification, blocker, or authority drift | `MARKER_RECEIPT_MISMATCH` |
| absent independent consumer receipt | `MARKER_CONSUMER_RECEIPT_MISSING` |
| changed replay receipt identity or result | `MARKER_CONSUMER_RECEIPT_MISMATCH` |
| rollup or parent-marker movement drift | `MARKER_ROLLUP_MISMATCH` |
| incomplete or divergent evidence lineage | `MARKER_EVIDENCE_REF_MISMATCH` |
| any apply/write/ratchet/live/send/Discord/deploy/production flag | `MARKER_MUTATION_NOT_ADMITTED` |

All pre-existing nine-family schema, identity, path, authority, producer validation, WorkerLease, CardRecord, BoardEvent, DiscordOS consumer, Git-integrity, and rejection behaviors remain green.

## Source-of-Truth Precedence

1. Git-tracked registry state, the Atlas-owned schema/validator, and executable producer/consumer evidence are authoritative.
2. The deterministic consumer receipt and this durable correlated receipt prove the accepted state and authority boundary.
3. The Atlas Book and owner exports are projections and must agree with the authoritative inputs.
4. Historical percentages or prose cannot override current executable evidence.
5. A future marker ratchet requires a separately accepted receipt; admission itself never edits a marker.

## Ratchet

The nine previously accepted families remain ComponentManifest, JobEnvelope, ContextPacket, ApprovalRecord, WorkerLease, EvidenceBundle, ExecutionReceipt, CardRecord, and BoardEvent. MarkerEvidence adds exactly one accepted unit.

`9 + 1 = 10`. `10 / 11 = 0.909090...`, which rounds to the integer marker `91%`. Contracts Mesh therefore moves exactly from `9/11` (`82%`) to `10/11` (`91%`). KnowledgeCandidate remains the sole `1/11` independent-consumer gap and is not implemented here.

Atlas Full-System Re-evaluation remains exactly `1/2` and `50%`. Marker Integrity remains exactly `51/51`. Neither parent state receives inferred or cosmetic progress.

## Verification

- `node --test tests/test_atlas_marker_evidence_admission.mjs`: `10/10` passed, including deterministic replay, all six required stable reasons, no-rollup, evidence-lineage, and mutation-flag denial.
- `node ops/atlas/test_validate_contracts_v2_adoption.mjs`: passed the inherited seven-family and Cluster 4 suites, MarkerEvidence acceptance, Cluster 5 rejection matrix, and exact `10/11 = 91%` calculation.
- `node ops/atlas/validate_contracts_v2_adoption.mjs --marker-evidence --json`: returned `ACCEPTED` with the stable receipt and identities above.
- `node ops/atlas/validate_contracts_v2_adoption.mjs --all --run repos/_stack/.codex/logs/20260715T134932158Z-atlas-contracts-v2-cluster-3-workerlease-no-change-canary-2/run.json --json`: returned `ACCEPTED`, ten exact families, ten accepted units, eleven foundations, and `91` percent.
- `npm --prefix packages/atlas-contracts run validate`: package fixture and artifact-validator suites passed.
- `node --test tests/test_atlas_native_board_correlation.mjs`: inherited `10/10` BoardEvent producer coverage passed.
- `python ops/atlas/test_project_board_owner_export.py`: `6/6` passed.
- `python ops/atlas/project_board_owner_export.py --check`: passed with 31 Atlas cards, two Cortex cards, `discord_mutation_authorized=false`, and source revision `sha256:fd2014e5d337b94d4f3187ad542f510742a8178fc63bbc5059ecfa0f4df7810e`.
- `powershell -NoProfile -ExecutionPolicy Bypass -File ops/validation/validate_stack.ps1`: passed at `critical=0 error=0 warning=5 info=0`; four inherited absolute-path warnings and one unrelated root capture-artifact warning are outside this packet.

## Governance Closeout

**RULE - Marker progress is executable state, not prose.**

**PATTERN - MarkerEvidence producer -> independent admission consumer -> negative matrix -> durable correlated receipt -> root adoption ratchet.**

**FAILURE MODE - A percentage can look consistent while its scope, denominator, freshness, transition, rollup, or receipt lineage is false.**

No owner-repository, Discord, board, Supabase, Vercel, external-data, live-data, production, deployment, secret, or destructive mutation occurred. No Vercel production deployment or promotion was attempted. No scratch cleanup, destructive reset, force push, broad staging, permission downgrade, completed-lane reopening, knowledge auto-promotion, parent-marker movement, or unsupported percentage movement occurred.
