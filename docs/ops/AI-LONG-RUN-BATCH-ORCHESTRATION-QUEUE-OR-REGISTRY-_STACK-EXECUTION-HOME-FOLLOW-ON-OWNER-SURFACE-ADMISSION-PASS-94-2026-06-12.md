# AI Long-Run Batch Orchestration Queue-Or-Registry `_stack` Execution-Home Follow-On Owner-Surface Admission Pass 94 - 2026-06-12

- Date: `2026-06-12`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded owner admission`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-QUEUE-OR-REGISTRY-_STACK-EXECUTION-HOME-FOLLOW-ON-CONTRACT-FREEZE-PASS-93-2026-06-12.md`
  - `stack.yaml`
  - `README-STACK.md`
  - `repos/_stack/README.md`
  - `repos/_stack/queue/README.md`
  - `runtime/receipts/validation/stack-validation.latest.md`
  - `runtime/receipts/validation/stack-validation.latest.json`
- Control-plane checkpoint: `main@0c88f867`

## Objective

Admit one owner-facing home for `_stack execution-home follow-on` without implying command implementation, live runtime-state reads, queue-drop emission, queue or registry mutation, worker launch, or owner-repo mutation.

## Owner-Surface Candidates Considered

### `ATLAS root`

Why it does not win:

- ATLAS root owns retained-state truth, restart narration, and receipt consequence
- ATLAS root does not own the shared operator execution surface
- keeping helper-home ownership in root would collapse truth ownership and execution-home ownership into one convenience surface again

### `Playbook`

Why it does not win:

- Playbook owns doctrine and reusable governance language
- this seam is not doctrine export; it is one shared operator follow-on posture helper
- assigning the helper home to Playbook would confuse command-home ownership with documentation ownership

### owner repos

Why they do not win:

- the seam is stack-level and root-facing, not product-runtime-local
- the trigger is retained-state queue-or-registry truth already owned above the repos
- no single owner repo should become the shared execution-home for this family

### `_stack`

Why it wins:

- `stack.yaml` and `README-STACK.md` already route orchestration and worker flow through `_stack`
- `repos/_stack/README.md` already defines `_stack` as the workspace operator layer for shared workflow commands and queue drops
- the first follow-on helper belongs on the shared operator layer once retained-state execution-gate truth is real
- `_stack` can host the helper surface without displacing ATLAS as the truth owner

## Admission Decision

### Truth owner

- ATLAS root remains the truth owner for:
  - retained-state candidate meaning
  - blocked-before-execution semantics
  - receipt consequence and restart projection

### Execution home admitted now

- `_stack`

Why this admission is honest:

- the family contract is already explicit
- the seam is now shared operator follow-on posture rather than root-only retained-state meaning
- `_stack` is already the named operator execution layer for shared workflow commands
- this still stops below command design, implementation admission, live reads, and queue behavior

## Exact Next Package

- `AI Long-Run Batch Orchestration queue-or-registry _stack execution-home follow-on supporting-lane admission pass 95`

## Marker Decision

- `none`

## Rule

Once retained-state execution gating is real, shared execution-home ownership belongs on `_stack` while truth ownership stays in ATLAS.
