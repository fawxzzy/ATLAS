# AI Long-Run Batch Orchestration Queue-Or-Registry Runtime-State Child-Home Selection Owner-Surface Admission Pass 38 - 2026-06-10

- Date: `2026-06-10`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded owner-surface admission`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/02-lanes-and-markers.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/11-system-map-graph.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/architecture/STATE-AND-MEMORY-BOUNDARIES.md`
  - `docs/architecture/AWARENESS-FIRST-WORLD-MODEL.md`
  - `docs/architecture/ORCHESTRATION-BOUNDARIES.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-QUEUE-OR-REGISTRY-CONTRACT-FREEZE-PASS-1-2026-06-09.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-QUEUE-OR-REGISTRY-RUNTIME-STATE-CHILD-HOME-SELECTION-CONTRACT-FREEZE-PASS-37-2026-06-10.md`
  - `stack.yaml`
  - `AGENTS.md`
  - `runtime/receipts/validation/stack-validation.latest.md`
  - `runtime/receipts/validation/stack-validation.latest.json`
- Control-plane checkpoint: `main`

## Objective

Admit the exact owner-facing surface for the contract-frozen `runtime-state child-home selection` seam, keep concrete runtime subtree and execution semantics deferred, and stop below supporting-lane admission, implementation, or `_stack` execution-home widening.

This pass does not:

- implement helper code
- create live queue or registry state
- choose one exact runtime subtree, filename, schema, or persistence layout
- admit `_stack` execution semantics, worker flow, or supervisor behavior
- admit Playbook doctrine ownership
- admit owner-repo mutation, Fitness work, archive/delete work, deploy/publication work, `.env`, or secret work
- claim that owner-surface admission alone earns marker movement

## Root Health Baseline

- pass 37 already froze one exact child-home contract for `runtime-state child-home selection`
- the unresolved question from pass 37 is which surface honestly owns that child-home seam before any support or implementation talk
- the contract already admits `runtime/state/` as the child-home class for mutable pre-execution queue-or-registry truth while explicitly excluding append-only `runtime/receipts/` history from acting as the live queue-state home
- the seam remains explicitly below concrete runtime subtree choice, validator execution, lifecycle advancement, and `_stack` execution-home semantics
- root validation remains clean at `critical=0 error=0 warning=52 info=0`
- local `HEAD` remains in parity with `origin/main`

## Owner-Surface Candidates Considered

### ATLAS root control-plane surfaces

What they already own:

- stack doctrine
- lane definitions
- cross-repo planning
- path and boundary rules
- restart projection and receipt consequence
- the queue-or-registry batch-entry contract plus the child-home meaning and receipt-history exclusion boundaries

Why they win:

- the seam is still child-home classification truth rather than shared execution or runtime-store implementation
- the seam exists to decide where mutable pre-execution queue-or-registry truth may live, not to implement queue writes or run execution
- the contract inherits root-owned boundary semantics around retained mutable state, receipt-history exclusion, deferred concrete subtree/layout, no validator execution, and no owner-repo mutation
- admitting root here keeps the family bounded to child-home and control-plane truth instead of prematurely collapsing it into runtime placement details or execution semantics

### `_stack`

Why it does not win yet:

- `_stack` owns execution-oriented orchestration contracts, command surfaces, worker flow, and supervised dispatch semantics
- this child-home family has not admitted execution transitions, validator execution, queue writes, or shared command-runtime behavior
- moving the seam into `_stack` now would blur root-owned child-home meaning with later execution-home or helper-runtime semantics

### Playbook

Why it does not win:

- Playbook owns reusable workflow doctrine and cross-repo pattern extraction
- this family is still deciding where one root-bounded child-home seam lives before any doctrine export is in scope
- Playbook may later consume the pattern, but it does not own this child-home home at this stage

### owner repos

Why they do not win:

- the child-home contract is cross-repo by design because candidate entries may target different owner repos
- retained-state classification, receipt-history exclusion, and protected-surface rules are stack-level truth
- pushing the seam into one owner repo would collapse root planning into repo-local ownership incorrectly

### `runtime/state/`

Why it does not win:

- `runtime/state/` is now the admitted child-home class for future mutable pre-execution queue-or-registry truth, but it is still a placement class rather than the owner of the contract meaning
- choosing `runtime/state/` as the owner now would blur state placement with control-plane ownership
- exact runtime subtree, filename, schema, snapshot shape, and persistence layout remain deferred until a later support or implementation pass makes them explicit

### `runtime/receipts/`

Why it does not win:

- `runtime/receipts/` is explicitly excluded as the live mutable queue-or-registry state home for this family
- it remains an append-only receipt and observation surface rather than the owner or placement home for mutable queue truth

## Admission Decision

### Child-home meaning remains at root

- `ATLAS root control-plane surfaces`

What that means:

- ATLAS root owns the canonical rule that future mutable pre-execution queue-or-registry truth belongs to the retained-state child-home class under `runtime/state/`
- ATLAS root owns the exclusion of append-only `runtime/receipts/` history from acting as the live mutable queue-or-registry state home
- ATLAS root owns restart-safe projection of the child-home seam and next-packet consequence
- ATLAS root does not thereby become a runtime-store implementation, live queue writer, or execution runner

### State placement remains admitted but not implemented

- `runtime/state/` remains the admitted child-home class for future mutable pre-execution queue-or-registry truth
- `runtime/receipts/` remains explicitly excluded as the live mutable queue-or-registry state home
- exact runtime subtree, filename, schema, snapshot shape, and persistence layout remain deferred

### Execution and helper-runtime ownership remain deferred

- `_stack` remains a later candidate owner for:
  - shared command-runtime behavior
  - execution-oriented orchestration
  - validator execution and status-transition semantics
  - worker flow
  - resume or dispatch behavior

### Doctrine ownership remains deferred

- Playbook remains a later candidate consumer for reusable child-home doctrine extraction

### Repo ownership remains unchanged

- owner repos still own:
  - repo-local mutation truth
  - repo-local verification truth
  - repo-local implementation truth

## Supporting Dependency Decision

- `none yet`

Why:

- owner-surface admission is now exact
- the next honest question is whether any separate supporting lane actually reopens from that decision
- support should be admitted explicitly rather than assumed from eventual future implementation

## Still Not Admitted In This Pass

- exact runtime subtree, filename, schema, snapshot shape, or persistence layout
- live queue or registry writes
- runtime-store implementation
- validator execution
- `_stack` helper-home or command-design work
- supervised pilot implementation
- unattended execution
- marker movement

## Exact Next Package

- `AI Long-Run Batch Orchestration queue-or-registry runtime-state child-home selection supporting-lane admission pass 39`

Why:

- the child-home contract and owner-facing home are now explicit
- the next honest question is whether one exact separate support seam reopens now or whether the family remains root-local at this stage
- implementation would be premature before that support question is frozen

## Marker Decision

- `none`

Why:

- this pass admits the owner-facing surface only
- it does not land code, execution proof, or broader operator adoption

## Rule

`Child-Home Meaning Stays At Root Before Runtime Placement Details`

Until a queue-or-registry child-home seam crosses from root-bounded state-class truth into shared helper-runtime or execution semantics, the owner of the seam stays with ATLAS root control-plane surfaces even though `runtime/state/` is already the admitted child-home class.

## Pattern

child-home contract freeze -> root control-plane owner admission -> explicit support decision -> only then discuss runtime subtree, helper runtime, or implementation

## Failure Mode

`Child-Home Ownership Collapse`

This family becomes dishonest when the admitted `runtime/state/` child-home class is mistaken for ownership of the child-home meaning before support and implementation semantics are explicitly admitted.

## What This Pass Proves

This pass proves:

- the `runtime-state child-home selection` seam now has one exact owner-facing home
- that home is `ATLAS root control-plane surfaces`
- `runtime/state/` remains the admitted child-home class for future mutable pre-execution state, but not the owner of the seam
- `runtime/receipts/`, `_stack`, Playbook, and owner repos are all still outside ownership for this stage

This pass does not prove:

- that any supporting lane is admitted
- that any exact runtime subtree or schema is chosen
- that implementation or execution semantics are admitted
