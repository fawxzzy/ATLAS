# AI Long-Run Batch Orchestration Queue-Or-Registry Runtime-State Concrete-Layout Selection Owner-Surface Admission Pass 45 - 2026-06-10

- Date: `2026-06-10`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded owner-surface admission`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/02-lanes-and-markers.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/11-system-map-graph.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/architecture/STATE-AND-MEMORY-BOUNDARIES.md`
  - `docs/architecture/AWARENESS-FIRST-WORLD-MODEL.md`
  - `docs/architecture/ORCHESTRATION-BOUNDARIES.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-QUEUE-OR-REGISTRY-CONTRACT-FREEZE-PASS-1-2026-06-09.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-QUEUE-OR-REGISTRY-RUNTIME-STATE-CONCRETE-LAYOUT-SELECTION-CONTRACT-FREEZE-PASS-44-2026-06-10.md`
  - `stack.yaml`
  - `AGENTS.md`
  - `runtime/receipts/validation/stack-validation.latest.md`
  - `runtime/receipts/validation/stack-validation.latest.json`
- Control-plane checkpoint: `main`

## Objective

Admit the exact owner-facing surface for the contract-frozen `runtime-state concrete-layout selection` seam, keep descendant retained-state placement distinct from runtime-state discovery and execution semantics, and stop below supporting-lane admission, implementation, or `_stack` execution-home widening.

This pass does not:

- implement helper code
- create live queue or registry state
- choose one final queue-home or registry-home path
- choose one exact runtime filename, schema, snapshot shape, or discovery rule
- admit `_stack` execution semantics, worker flow, runtime-state discovery, or supervisor behavior
- admit Playbook doctrine ownership
- admit owner-repo mutation, Fitness work, archive/delete work, deploy/publication work, `.env`, or secret work
- claim that owner-surface admission alone earns marker movement

## Root Health Baseline

- pass 44 already froze one exact retained-state descendant-layout contract for `runtime-state concrete-layout selection`
- the unresolved question from pass 44 is which surface honestly owns that retained-state layout seam before any support or implementation talk
- the contract already keeps mutable pre-execution queue-or-registry truth under admitted `runtime/state/` while explicitly deferring runtime-state discovery, exact filename/schema/snapshot shape, and final queue-home or registry-home choice
- the seam remains explicitly below validator execution, lifecycle advancement, and `_stack` execution-home semantics
- root validation remains clean at `critical=0 error=0 warning=52 info=0`
- local `HEAD` remains in parity with `origin/main`

## Owner-Surface Candidates Considered

### ATLAS root control-plane surfaces

What they already own:

- stack doctrine
- lane definitions
- cross-repo planning
- path and boundary rules
- restart projection and receipt consequence
- the queue-or-registry batch-entry contract plus the retained-state child-home meaning and descendant-layout non-claim boundaries

Why they win:

- the seam is still retained-state layout classification truth rather than shared execution or runtime-store implementation
- the seam exists to define how retained mutable pre-execution queue-or-registry truth may be shaped beneath admitted `runtime/state/`, not to perform discovery, queue writes, or execution
- the retained-state layout contract inherits root-owned boundary semantics around deferred exact filename/schema/snapshot shape, deferred final queue-home or registry-home choice, no runtime-state discovery, no validator execution, and no owner-repo mutation
- admitting root here keeps the family bounded to descendant-layout and control-plane truth instead of prematurely collapsing it into helper-runtime or execution semantics

### `_stack`

Why it does not win yet:

- `_stack` owns execution-oriented orchestration contracts, command surfaces, worker flow, and supervised dispatch semantics
- this retained-state layout family has not admitted runtime-state discovery, execution transitions, validator execution, queue writes, or shared command-runtime behavior
- moving the seam into `_stack` now would blur root-owned descendant-layout meaning with later discovery or execution-home semantics

### Playbook

Why it does not win:

- Playbook owns reusable workflow doctrine and cross-repo pattern extraction
- this family is still deciding where one root-bounded retained-state layout seam lives before any doctrine export is in scope
- Playbook may later consume the pattern, but it does not own this retained-state layout home at this stage

### owner repos

Why they do not win:

- the retained-state layout contract is cross-repo by design because candidate entries may target different owner repos
- retained-state layout classification, discovery deferral, and protected-surface rules are stack-level truth
- pushing the seam into one owner repo would collapse root planning into repo-local ownership incorrectly

### `runtime/state/`

Why it does not win:

- `runtime/state/` is now the admitted child-home class for future mutable pre-execution queue-or-registry truth, but it is still a placement class rather than the owner of the contract meaning
- choosing `runtime/state/` as the owner now would blur state placement with control-plane ownership
- exact descendant contents, filename, schema, snapshot shape, and final queue-home or registry-home choice remain deferred until a later support or implementation pass makes them explicit

## Admission Decision

### Retained-state layout meaning remains at root

- `ATLAS root control-plane surfaces`

What that means:

- ATLAS root owns the canonical rule that future mutable pre-execution queue-or-registry truth stays under `runtime/state/` and that descendant-layout meaning remains a retained-state control-plane seam rather than runtime-state discovery or execution behavior
- ATLAS root owns the deferral of exact filename, schema, snapshot shape, and final queue-home or registry-home choice
- ATLAS root owns restart-safe projection of the retained-state layout seam and next-packet consequence
- ATLAS root does not thereby become a runtime-store implementation, live queue writer, or execution runner

### State placement remains admitted but not implemented

- `runtime/state/` remains the admitted child-home class for future mutable pre-execution queue-or-registry truth
- exact descendant contents, filename, schema, snapshot shape, and final queue-home or registry-home choice remain deferred

### Execution and helper-runtime ownership remain deferred

- `_stack` remains a later candidate owner for:
  - shared command-runtime behavior
  - runtime-state discovery
  - execution-oriented orchestration
  - validator execution and status-transition semantics
  - worker flow
  - resume or dispatch behavior

### Doctrine ownership remains deferred

- Playbook remains a later candidate consumer for reusable retained-state layout doctrine extraction

### Repo ownership remains unchanged

- owner repos still own:
  - repo-local mutation truth
  - repo-local verification truth
  - repo-local implementation truth

## Supporting Dependency Decision

- `none yet`

Why:

- owner-surface admission is now exact
- the next honest question is whether any separate supporting lane actually reopens from that decision
- support should be admitted explicitly rather than assumed from eventual future implementation

## Still Not Admitted In This Pass

- exact descendant contents, filename, schema, snapshot shape, or final queue-home or registry-home choice
- live queue or registry writes
- runtime-store implementation
- runtime-state discovery
- validator execution
- `_stack` helper-home or command-design work
- supervised pilot implementation
- unattended execution
- marker movement

## Exact Next Package

- `AI Long-Run Batch Orchestration queue-or-registry runtime-state concrete-layout selection supporting-lane admission pass 46`

Why:

- the retained-state layout contract and owner-facing home are now explicit
- the next honest question is whether one exact separate support seam reopens now or whether the family remains root-local at this stage
- implementation would be premature before that support question is frozen

## Marker Decision

- `none`

Why:

- this pass admits the owner-facing surface only
- it does not land code, execution proof, or broader operator adoption

## Rule

`Retained-State Layout Meaning Stays At Root Before Discovery`

Until a runtime-state concrete-layout seam crosses from root-bounded retained-state truth into shared helper-runtime or runtime-state discovery semantics, the owner of the seam stays with ATLAS root control-plane surfaces even though `runtime/state/` is already the admitted child-home class.

## Pattern

retained-state layout contract freeze -> root control-plane owner admission -> explicit support decision -> only then discuss discovery, helper runtime, or implementation

## Failure Mode

`Retained-Layout Ownership Collapse`

This family becomes dishonest when the admitted `runtime/state/` child-home class is mistaken for ownership of retained-state layout meaning before support and implementation semantics are explicitly admitted.

## What This Pass Proves

This pass proves:

- the `runtime-state concrete-layout selection` seam now has one exact owner-facing home
- that home is `ATLAS root control-plane surfaces`
- `runtime/state/` remains the admitted child-home class for future mutable pre-execution state, but not the owner of the seam
- `_stack`, Playbook, and owner repos are all still outside ownership for this stage

This pass does not prove:

- that any supporting lane is admitted
- that any exact descendant contents, filename, schema, or snapshot shape are chosen
- that implementation, runtime-state discovery, or execution semantics are admitted
