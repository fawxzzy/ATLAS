# _Stack Readiness Supervised Execution-Home Runtime-Home Choice First-Implementation Admission Pass 570 - 2026-06-22

- Date: `2026-06-22`
- Lane: `_stack Readiness supervised execution-home runtime-home choice first-implementation admission pass 570`
- Mode: `docs-only root-bounded first-implementation admission`
- Scope: `freeze the smallest fail-closed runtime-home-choice implementation slice and proof matrix without choosing one runtime home, one concrete _stack command implementation surface, or _stack command behavior by adjacency`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-POST-RUNTIME-HOME-SELECTION-NEXT-SLICE-SELECTION-PASS-566-2026-06-22.md`
  - `docs/ops/_STACK-READINESS-SUPERVISED-EXECUTION-HOME-RUNTIME-HOME-CHOICE-CONTRACT-FREEZE-PASS-567-2026-06-22.md`
  - `docs/ops/_STACK-READINESS-SUPERVISED-EXECUTION-HOME-RUNTIME-HOME-CHOICE-OWNER-SURFACE-ADMISSION-PASS-568-2026-06-22.md`
  - `docs/ops/_STACK-READINESS-SUPERVISED-EXECUTION-HOME-RUNTIME-HOME-CHOICE-SUPPORTING-LANE-ADMISSION-PASS-569-2026-06-22.md`
  - `docs/ops/_STACK-READINESS-SUPERVISED-EXECUTION-HOME-RUNTIME-HOME-SELECTION-FIRST-IMPLEMENTATION-WORKER-CLUSTER-RECONCILIATION-2026-06-22.md`
  - `ops/atlas/supervised_execution_home_runtime_home_selection.py`
  - `tests/test_atlas_supervised_execution_home_runtime_home_selection.py`
  - `docs/memory/initiatives/continuity-manifest-ai-long-run-batch-orchestration.json`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/atlas-book/13-vision-and-endgames.md`
- Control-plane checkpoint: `main`

## Objective

Freeze one compact authoritative first implementation slice for the root-owned runtime-home-choice family plus one proof matrix for validating that slice without crossing the no-runtime-home-choice, no-concrete-`_stack`-command-implementation-surface-choice, no-concrete-`_stack`-command-home-choice, no-concrete-command-file-choice, no-`_stack`-command-implementation, no-worker-authority, no-owner-repo-edit, no-actual-owner-side-mutation-authority, no-Playbook-export, no-deploy-or-publication, and no-protected-surface-widening boundary.

## Exact First Admitted Implementation Slice

The first admitted implementation slice is:

1. one root-local runtime-home-choice bundle that preserves only one already-produced runtime-home-selection result through:
   - `command`
   - `normalized_candidate_path`
   - `result_class`
   - `owner_surface`
   - `support_posture`
   - `admitted_evidence_refs`
   - `blocked_questions`
   - `routing_note`
   - `payload`
   - `command_home_selection_status`
   - `command_home_selection_question`
   - `command_home_selection_reasons`
   - `concrete_command_home_status`
   - `concrete_command_home_question`
   - `concrete_command_home_reasons`
   - `concrete_stack_command_home_selection_status`
   - `concrete_stack_command_home_selection_question`
   - `concrete_stack_command_home_selection_reasons`
   - `concrete_command_file_selection_status`
   - `concrete_command_file_selection_question`
   - `concrete_command_file_selection_reasons`
   - `concrete_stack_command_implementation_surface_selection_status`
   - `concrete_stack_command_implementation_surface_selection_question`
   - `concrete_stack_command_implementation_surface_selection_reasons`
   - `runtime_home_selection_status`
   - `runtime_home_selection_question`
   - `runtime_home_selection_reasons`
2. one fail-closed runtime-home-choice selector classifier that evaluates only the already admitted runtime-home-selection dimensions:
   - whether `runtime_home_selection_status` is exactly `runtime_home_selection_admissible`
   - whether `runtime_home_selection_question` is still one explicit contract-local question card with only `question`, `candidate_ref`, and `authoritative_receipt_refs`
   - whether `runtime_home_selection_reasons` is still `[]`
   - whether the preserved upstream `command`, `normalized_candidate_path`, `result_class`, `owner_surface`, `support_posture`, `routing_note`, and payload posture still match the admitted root-owned runtime-home-choice family
   - whether the argument cites only the authoritative pass-567-through-pass-570 contract family rather than forbidden evidence classes
   - whether the slice still stops below one runtime home, one concrete `_stack` command implementation surface, one concrete `_stack` command home, one concrete command file, one `_stack` command implementation path, one worker-authority class, one owner repo, or one doctrine-export path
3. one bounded runtime-home-choice-status layer that may emit only:
   - `runtime_home_choice_admissible`
   - `no_runtime_home_choice`
4. one bounded runtime-home-choice-question layer that may emit only one explicit contract-local `runtime_home_choice_question` card already derived from the preserved runtime-home-selection result or `null`
5. one bounded runtime-home-choice-reason layer that may emit only the pass-567 admitted rejection families
6. one preserved separation layer where the slice may classify whether one explicit reconciled runtime-home-selection posture reopens one bounded runtime-home-choice question but may not choose one runtime home, one concrete `_stack` command implementation surface, one concrete `_stack` command home, one concrete command file, one `_stack` command implementation path, one worker-authority class, one owner repo, or one doctrine-export path

## Exact Preserved Input Surface

The worker must preserve only:

- `command`
- `normalized_candidate_path`
- `result_class`
- `owner_surface`
- `support_posture`
- `admitted_evidence_refs`
- `blocked_questions`
- `routing_note`
- `payload`
- `command_home_selection_status`
- `command_home_selection_question`
- `command_home_selection_reasons`
- `concrete_command_home_status`
- `concrete_command_home_question`
- `concrete_command_home_reasons`
- `concrete_stack_command_home_selection_status`
- `concrete_stack_command_home_selection_question`
- `concrete_stack_command_home_selection_reasons`
- `concrete_command_file_selection_status`
- `concrete_command_file_selection_question`
- `concrete_command_file_selection_reasons`
- `concrete_stack_command_implementation_surface_selection_status`
- `concrete_stack_command_implementation_surface_selection_question`
- `concrete_stack_command_implementation_surface_selection_reasons`
- `runtime_home_selection_status`
- `runtime_home_selection_question`
- `runtime_home_selection_reasons`

## Exact Admitted Output Surface

Top-level runtime-home-choice rules are:

- `runtime_home_choice_status` may include only:
  - `runtime_home_choice_admissible`
  - `no_runtime_home_choice`
- `runtime_home_choice_question` may be either:
  - one explicit contract-local question card
  - `null`
- `runtime_home_choice_question` may be non-null only when:
  - `runtime_home_selection_status` is `runtime_home_selection_admissible`
  - `runtime_home_selection_question` is one explicit contract-local card with only `question`, `candidate_ref`, and `authoritative_receipt_refs`
  - `runtime_home_selection_reasons` is `[]`
  - the preserved upstream command-home-selection, concrete-command-home, concrete-`_stack`-command-home-selection, concrete-command-file-selection, concrete-`_stack`-command-implementation-surface-selection, and runtime-home-selection surfaces remain explicit and fail-closed
  - the argument cites only the authoritative pass-567-through-pass-570 contract family
  - no forbidden evidence class or adjacency leap is required to keep the question contract-local
- `runtime_home_choice_reasons` may include only:
  - `runtime_home_selection_status_not_admissible`
  - `runtime_home_selection_question_not_explicit`
  - `runtime_home_selection_reasons_present`
  - `forbidden_evidence_class_used`
  - `live_repo_discovery_or_hidden_transcript_dependency`
  - `concrete_stack_command_home_choice_attempted`
  - `concrete_command_file_choice_attempted`
  - `stack_command_implementation_surface_choice_attempted`
  - `runtime_home_choice_attempted`
  - `stack_command_implementation_attempted`
  - `worker_authority_attempted`
  - `owner_repo_or_actual_mutation_authority_attempted`
  - `playbook_doctrine_export_attempted`
  - `protected_surface_exception_attempted`
- no live repo discovery, branch discovery, worktree discovery, one concrete `_stack` command-home choice, one concrete command-file choice, one concrete `_stack` command implementation-surface choice, one runtime-home choice, `_stack` command implementation, worker authority, owner-repo edits, actual owner-side mutation authority, Playbook doctrine export, deploy/publication, archive/delete, `.env`, secret work, or protected-surface exceptions may leak into this first slice

## Exact Mandatory Proof Cases

1. explicit `runtime_home_selection_admissible` result with one explicit runtime-home-choice question card and aligned pass-567-through-pass-570 truth
   - emit `runtime_home_choice_status` as `runtime_home_choice_admissible`
   - preserve `runtime_home_choice_question` as one explicit contract-local question card
   - preserve `runtime_home_choice_reasons` as `[]`

2. `runtime_home_selection_status` is `no_runtime_home_selection`
   - emit `runtime_home_choice_status` as `no_runtime_home_choice`
   - preserve `runtime_home_choice_question` as `null`
   - preserve `runtime_home_selection_status_not_admissible`

3. missing or widened runtime-home-selection question card, or upstream `runtime_home_selection_reasons` present
   - emit `runtime_home_choice_status` as `no_runtime_home_choice`
   - preserve `runtime_home_selection_question_not_explicit` or `runtime_home_selection_reasons_present`

4. argument uses forbidden evidence classes or hidden live-repo / hidden-transcript dependency
   - emit `runtime_home_choice_status` as `no_runtime_home_choice`
   - preserve `forbidden_evidence_class_used` or `live_repo_discovery_or_hidden_transcript_dependency`

5. payload or argument attempts one concrete `_stack` command-home choice, one concrete command-file choice, one concrete `_stack` command implementation-surface choice, one runtime-home choice, `_stack` command implementation, worker authority, owner-repo edit authority, actual owner-side mutation authority, Playbook doctrine export, or protected-surface exception
   - emit `runtime_home_choice_status` as `no_runtime_home_choice`
   - preserve the exact corresponding runtime-home-choice reason

## Exact Next Package

- `_stack Readiness supervised execution-home runtime-home choice prompt-pack and handoff contract pass 571`

## Marker Decision

- `none`

Why:

- this pass freezes the smallest exact implementation slice and proof matrix only
- no code landing, proof execution, runtime-home choice, concrete `_stack` command implementation-surface choice, or broader operator adoption occurs here

## Rule

Freeze the smallest fail-closed runtime-home-choice slice before admitting worker touch surfaces, one runtime-home choice, one concrete `_stack` command implementation-surface choice, `_stack` runtime doctrine, or Playbook doctrine export.

## Failure Mode

`Runtime-Home Choice By Adjacency Drift`

This family becomes dishonest when the first implementation slice treats one explicit reconciled runtime-home-selection posture as enough to choose one runtime home, choose one concrete `_stack` command implementation surface, infer `_stack` runtime doctrine, admit `_stack` implementation, export doctrine to Playbook, or launch worker authority before those later boundaries are separately admitted.
