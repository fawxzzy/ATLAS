# AI Long-Run Batch Orchestration Single Supervised Pilot Selected-Pilot Owner-Repo Implementation Contract Freeze Pass 494 - 2026-06-20

- Date: `2026-06-20`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded contract freeze`
- Scope: `freeze the exact root-owned contract for deciding when one preserved selected-pilot implementation_route card may or may not become one bounded downstream owner-repo implementation packet without widening into owner-repo mutation authority, _stack execution-home doctrine, Playbook export, deploy/publication, or protected-surface touch`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/memory/initiatives/continuity-manifest-ai-long-run-batch-orchestration.json`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/02-lanes-and-markers.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/11-system-map-graph.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/atlas-book/13-vision-and-endgames.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-CONTRACT-FREEZE-PASS-487-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-OWNER-SURFACE-ADMISSION-PASS-488-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-SUPPORTING-LANE-ADMISSION-PASS-489-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-FIRST-IMPLEMENTATION-ADMISSION-PASS-490-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-PROMPT-PACK-AND-HANDOFF-CONTRACT-PASS-491-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-IMPLEMENTATION-READINESS-CLOSEOUT-AND-WORKER-ROUTING-PASS-492-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-FIRST-IMPLEMENTATION-WORKER-CLUSTER-RECONCILIATION-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-POST-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-NEXT-SLICE-SELECTION-PASS-493-2026-06-20.md`
  - `ops/atlas/pilot_selected_implementation_routing.py`
  - `tests/test_atlas_pilot_selected_implementation_routing.py`

## Objective

Replace the still-implicit `implementation_route -> owner-repo implementation packet` seam with one exact root-owned contract that says when a directly proved route card may or may not become one bounded owner-repo implementation question, while staying below owner-surface admission, support admission, owner-repo mutation authority, `_stack` execution-home doctrine, Playbook export, deploy/publication, or protected-surface touch.

This pass does not:

- choose one owner-repo implementation packet, branch, worktree, or execution home
- admit owner-repo mutation authority, worker launch, or dispatch semantics
- admit `_stack` helper-home, command-home, runtime-home, or command-design semantics
- admit Playbook doctrine export or reusable consumer framing
- admit deploy/publication, archive/delete, `.env`, or secret work
- admit hidden repo discovery, hidden readiness scoring, or transcript-memory routing
- claim that owner-repo implementation contract freeze alone proves one owner-side implementation packet is already admitted

## Root State

- branch: `main`
- current working tree still contains unrelated preserved tracked and untracked residue outside this pass
- marker posture before this pass:
  - `AI Long-Run Batch Orchestration: 61%`

## Exact Weak Link Before Repair

Before this pass, current durable truth already established:

- one exact pilot-selection criteria contract and landed validator helper plus direct proof
- one exact candidate-comparison contract, owner-facing home, support posture, first implementation slice, prompt-pack, readiness closeout, and landed worker cluster
- one exact winner-conversion contract, owner-facing home, support posture, first implementation slice, prompt-pack, readiness closeout, and landed worker cluster
- one exact winner-selection contract, owner-facing home, support posture, first implementation slice, prompt-pack, readiness closeout, and landed worker cluster
- one exact selected-pilot implementation-routing contract, owner-facing home, support posture, first implementation slice, prompt-pack, readiness closeout, and landed worker cluster
- one exact post-selected-pilot implementation-routing next-slice selector that already named this owner-repo implementation contract as the next honest downstream seam

But the lane still lacked one explicit contract for the next honest question:

- how one explicit `implementation_route` card may or may not become one bounded downstream owner-repo implementation packet without inventing owner-repo mutation authority, `_stack` execution-home doctrine, Playbook export, deploy/publication behavior, or protected-surface exceptions by adjacency

That gap was small but real: restart truth could name `selected-pilot owner-repo implementation contract freeze pass 494` as the next package, but the owner-repo implementation seam itself was not yet durable.

## Selected-Pilot Owner-Repo Implementation

### Owner-surface admission now

Why it does not win now:

- the lane still lacks one exact contract that says what qualifies as admissible owner-repo implementation input and which preserved evidence classes are allowed or forbidden
- selecting the owner-facing home now would turn one explicit route card directly into an owner-home decision by adjacency
- owner-surface home is the consequence of this implementation contract, not part of the contract itself

### `_stack` execution-home or command-home admission now

Why it does not win now:

- the preserved route card still does not create one separately admitted execution-home question
- `_stack` ownership starts later, only after owner-side implementation creates one honest execution-home decision
- routing into `_stack` now would blur owner-side implementation qualification with later execution semantics

### Playbook doctrine export now

Why it does not win now:

- reusable doctrine export still depends on proving this exact owner-side implementation seam in live lane truth first
- current truth still needs the local implementation contract before any broader doctrine consumer is honest

### Owner-repo mutation or worker launch now

Why it does not win now:

- the preserved route card already carries candidate identity, target, and bounded scope fields
- what is still missing is the exact control-plane contract for whether and how that preserved card may route into a later owner packet
- mutating an owner repo now would skip both the implementation contract and the owner-surface admission boundary

### Selected-pilot owner-repo implementation

Why it wins now:

- the routeability helper plus direct proof closed the blocker class around `can one explicit selected-pilot route card exist safely`
- the next honest blocker is no longer routeability itself; it is `how does that preserved route card become or fail closed before owner-home admission, owner-repo mutation, _stack execution-home doctrine, or Playbook widening`
- this seam is narrower than owner-surface admission, owner-repo mutation, `_stack` execution-home admission, or Playbook export

## Frozen Contract

### `family_name`

- `single supervised pilot selected-pilot owner-repo implementation`

### `owner_repo_implementation_input`

- one exact root-owned implementation surface that accepts:
  - one explicit selected-pilot implementation-routing result already produced by the admitted routing family
  - one preserved explicit `implementation_route` candidate card carried by that result
  - one preserved routing-local failure surface explaining when no owner-repo implementation packet is admissible
- owner-repo implementation input is admissible only when:
  - `routing_status` is exactly `implementation_route_admissible`
  - `routing_reasons` is exactly empty
  - `implementation_route` is still one explicit candidate card already preserved by the admitted routing family
  - the preserved implementation-route card still stays inside the already admitted protected-surface and root-visibility boundaries
  - the owner-repo implementation argument depends only on preserved route-card output plus current durable root-owned receipt, Book, and continuity-manifest surfaces
- admitted implementation evidence classes may use only:
  - preserved route-card control fields already carried on the explicit candidate card, including `owner_repo_count`, `target_kind`, `target_ref`, `objective_summary`, `allowed_write_scope`, `checkpoint_surface`, `verification_gate`, `closeout_artifact`, `park_or_escalation_rule`, and `protected_surface_exclusions`
  - preserved routing status fields `selection_status`, `selection_reasons`, `routing_status`, and `routing_reasons`
  - current root-owned lane receipts and restart surfaces that define the existing control-plane boundary
- forbidden evidence classes:
  - live repo, branch, or worktree discovery beyond the preserved implementation-route card
  - owner readiness, release readiness, deploy readiness, or protected-surface exception scoring
  - execution-home, `_stack`, helper-home, command-home, or runtime-home inference
  - Playbook export-worthiness or doctrine-reuse claims
  - deploy/publication, archive/delete, `.env`, secret, or protected-surface exception arguments
  - transcript memory, uploaded transcript residue, or non-durable operator recall
- implementation routing must fail closed if:
  - `routing_status` is not `implementation_route_admissible`
  - `routing_reasons` is not empty
  - `implementation_route` is missing or not explicit
  - the owner-repo implementation argument depends on hidden discovery or any other forbidden evidence class
  - the implementation argument tries to admit owner-repo mutation, worker launch, execution-home selection, or support widening directly from the preserved route card

### `implementation_output_boundary`

- output is still contract-only at this stage
- no owner-facing home is admitted now
- no owner repo is selected now beyond preserving the already carried candidate identity inside `implementation_route`
- no execution home is selected now
- no `_stack` command or runtime surface is admitted now
- no Playbook doctrine export is admitted now
- no deploy/publication behavior is admitted now
- no owner-repo mutation is admitted now
- the implementation contract may define:
  - admissible owner-repo implementation input
  - admitted evidence classes
  - forbidden evidence classes
  - fail-closed implementation rejection classes
  - the exact boundary between preserved `implementation_route` and later owner-surface admission
- it may not define:
  - `_stack` command runtime
  - owner-repo mutation
  - branch or worktree discovery
  - automatic implementation routing by hidden scoring
  - later execution-home choice
  - Playbook export or deploy/publication semantics

### `owner_boundary`

- ATLAS root owns this implementation contract, the lane-level implementation discipline, and the restart consequence of whether owner-repo implementation is explicit yet
- owner repos remain the later owners of repo-local implementation and verification truth only after owner-surface admission and later implementation admission are separately frozen
- `_stack` remains a later owner only after implementation and owner-facing surfaces create one separately admitted execution-home question
- Playbook remains a later consumer only after this exact implementation seam proves reusable beyond the current lane state

## Exact Marker Decision

- `none`

Why:

- pass 494 freezes the control-plane owner-repo implementation contract only
- operator instructions for this batch explicitly keep the marker at `61%` unless executed state, proof-backed adoption, restart breadth, or blocker class materially changes
- current truth gets sharper here, but the pass does not itself widen owner-side execution or clear a new blocker class beyond the already-selected next seam

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot selected-pilot owner-repo implementation owner-surface admission pass 495`

Why:

- the owner-repo implementation contract is now exact
- the next honest question is which surface owns the producing and consuming home for that implementation seam before support, concrete implementation admission, or `_stack` execution-home doctrine is discussed
- selecting owner-repo mutation, worker launch, or execution-home behavior would still be premature before that owner-facing boundary is frozen

## Rule

`Owner-Repo Implementation Contract Before Owner Admission`

When one explicit selected-pilot implementation route card is already directly proved, freeze the exact owner-repo implementation contract before owner-surface admission, owner-repo mutation authority, `_stack` execution-home doctrine, Playbook export, or deploy/publication widening.

## Failure Mode

`Owner-Repo Implementation By Route-Card Adjacency`

This lane becomes synthetic when one explicit `implementation_route` card is treated as an obvious owner-side implementation packet before the implementation contract makes the admissible input, evidence classes, fail-closed rejection rules, and later owner/execution boundaries explicit.
