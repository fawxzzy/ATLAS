# Sandbox Simulation Readiness Local-Only First Validator-Candidate-Output Report-Link Contract Freeze - 2026-06-27

- Date: `2026-06-27`
- Scope: `freeze how the admitted local-only Sandbox validator-report stub may cite or remain coupled to the admitted validator-candidate-output stub without admitting validator execution, runner behavior, or any owner-repo, deploy, secret, or live-data widening`
- Lane: `Sandbox Simulation Readiness`
- Mode: `root-owned docs-only validator-candidate-output-report-link contract freeze`

## Objective

Clear the next exact Sandbox blocker by freezing the smallest honest link between the already admitted validator-report stub and validator-candidate-output stub so the validation pair cannot widen into ad hoc run discovery or implicit execution claims.

## Executed

1. Froze the validator-report and validator-candidate-output pair to one exact shared validation home under one exact run directory.
2. Froze shared `validator_id`, `scenario_id`, and `run_id` identity as the only currently admitted coupling rule between the two committed artifacts.
3. Froze that the current link is deterministic sibling pairing only and does not itself admit comparison, verdict, or execution truth.
4. Refreshed the maintained Sandbox continuity and restart surfaces so the exact next packet becomes one first validator-pair coherence semantics contract freeze under the same local-only root.

## Report-Link Contract

### Current Admitted Artifact Pair

The only currently admitted linked pair is:

1. `runtime/atlas/sandbox/runs/local-only-example-stub/local-only-example-run-001/validation/report.json`
2. `runtime/atlas/sandbox/runs/local-only-example-stub/local-only-example-run-001/validation/candidate-output.json`

### Coupling Rule

The current admitted validator-report / candidate-output link may rely only on:

- one shared validation directory:
  - `runtime/atlas/sandbox/runs/<scenario_id>/<run_id>/validation/`
- one shared identity triplet:
  - `validator_id`
  - `scenario_id`
  - `run_id`
- one deterministic sibling-path pairing:
  - `report.json`
  - `candidate-output.json`

### Current Boundary

- the current report stub does not need a new direct `candidate_output_ref` field to remain honestly linked to the admitted candidate-output stub
- no alternate path search, cross-run lookup, cross-scenario lookup, or cross-validator lookup is admitted
- no report-to-candidate link outside the frozen validation home is admitted
- report-link alone cannot imply `match`, `mismatch`, `blocked`, or executed comparison truth

This packet does not admit:

- validator execution
- comparison verdicts
- candidate-output generation behavior
- report-field widening beyond the already admitted contract
- mutation side effects
- owner-repo writes
- publish behavior
- `_stack` routing

## Ratchet Decision

`Sandbox Simulation Readiness` moves from `48%` to `51%`.

Why:

- the lane already had one admitted validator-report stub and one admitted validator-candidate-output stub under the same frozen validation home
- the next exact ambiguity was how those two artifacts may remain honestly linked without widening into execution claims
- one bounded report-link contract is now frozen without widening into behavior

It stays low because:

- no validator-pair coherence semantics are frozen yet
- no validator execution exists
- no runner exists
- no `_stack` or owner-repo widening exists
- no proof-backed adoption exists

## Non-Claim

This does not prove:

- validator behavior
- comparison correctness
- report correctness
- runner behavior
- `_stack` ownership
- owner-repo execution
- safe unattended simulation

## Exact Next Package

- `Sandbox Simulation Readiness local-only first validator-pair coherence semantics contract freeze`

Why:

- one bounded report-link now exists between the admitted validator-report stub and validator-candidate-output stub
- the next honest move is to freeze how that linked pair may coexist with `result.status: not_run` without admitting comparison or execution
- validator behavior, runner behavior, and wider execution claims remain premature
