# AI Long-Run Batch Orchestration Single Supervised Pilot Winner Selection Contract Freeze Pass 480 - 2026-06-20

- Date: `2026-06-20`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded contract freeze`
- Scope: `freeze the exact root-owned selection contract for turning one contract-local pilot_winner result into one lane-level concrete first supervised pilot selection without widening into owner-repo implementation or execution-home doctrine`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/memory/initiatives/continuity-manifest-ai-long-run-batch-orchestration.json`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/02-lanes-and-markers.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/11-system-map-graph.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/atlas-book/13-vision-and-endgames.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-CONVERSION-CONTRACT-FREEZE-PASS-474-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-CONVERSION-OWNER-SURFACE-ADMISSION-PASS-475-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-CONVERSION-SUPPORTING-LANE-ADMISSION-PASS-476-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-CONVERSION-FIRST-IMPLEMENTATION-ADMISSION-PASS-477-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-CONVERSION-PROMPT-PACK-AND-HANDOFF-CONTRACT-PASS-478-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-CONVERSION-IMPLEMENTATION-READINESS-CLOSEOUT-AND-WORKER-ROUTING-PASS-479-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-CONVERSION-FIRST-IMPLEMENTATION-WORKER-CLUSTER-RECONCILIATION-2026-06-20.md`
  - `ops/atlas/pilot_winner_conversion.py`
  - `tests/test_atlas_pilot_winner_conversion.py`

## Objective

Replace the still-implicit `contract-local pilot_winner -> lane-level concrete first supervised pilot` seam with one exact root-owned contract that says when a directly proved winner-conversion result may or may not become one concrete first supervised pilot selection, while staying below owner-surface admission, support admission, owner-repo mutation, execution-home doctrine, or protected-surface touch.

This pass does not:

- choose one owner-side implementation packet, repo mutation plan, branch, worktree, or execution home
- admit `_stack` helper-home, command-design, runtime-state, resume, merge, or dispatch semantics
- admit owner-repo implementation, deploy/publication, archive/delete, `.env`, or secret work
- admit hidden repo discovery, hidden tie-breaking, or transcript-memory selection
- claim that winner-selection contract freeze alone proves one owner-side pilot implementation is already admitted

## Root State

- branch: `main`
- current working tree still contains unrelated preserved tracked and untracked residue outside this pass
- marker posture before this pass:
  - `AI Long-Run Batch Orchestration: 58%`

## Exact Weak Link Before Repair

Before this pass, current durable truth already established:

- one exact pilot-selection criteria contract and landed validator helper plus direct proof
- one exact candidate-comparison contract, owner-facing home, support posture, first implementation slice, prompt-pack, readiness closeout, and landed worker cluster
- one exact winner-conversion contract, owner-facing home, support posture, first implementation slice, prompt-pack, readiness closeout, and landed worker cluster

But the lane still lacked one explicit contract for the next honest question:

- how one explicit contract-local `pilot_winner` card may or may not become one lane-level concrete first supervised pilot selection without inventing owner-side implementation readiness, repo mutation authority, or `_stack` execution-home doctrine by adjacency

That gap was small but real: restart truth could now name `winner selection contract freeze pass 480` as the next package, but the selection seam itself was not yet durable.

## Winner Selection

### Owner-side pilot implementation admission now

Why it does not win now:

- the lane still lacks one exact selection contract that says what winner-conversion states, preserved winner fields, and fail-closed classes are admissible
- selecting an owner-side implementation packet now would turn one contract-local `pilot_winner` card directly into repo mutation authority by adjacency
- owner-side verification and execution surfaces are later consequences of explicit selection, not part of the selection contract itself

### `_stack` execution-home or command-home admission now

Why it does not win now:

- `_stack` ownership starts later, only after one concrete first supervised pilot is explicit and the lane actually needs supervised execution-home semantics
- this family is still pre-execution and root-owned by the original lane charter
- routing into `_stack` now would blur concrete pilot selection with later execution-home doctrine

### Concrete pilot selection now

Why it does not win now:

- the lane still lacks one exact selection contract that says what conversion states, preserved candidate identity, and fail-closed rejection classes are admissible
- choosing the concrete pilot now would leak from one proved conversion helper directly into one live lane decision without the already-required contract
- contract-local `pilot_winner` is not yet the same thing as one admitted concrete first supervised pilot for later owner routing

### Single supervised pilot winner selection

Why it wins now:

- the winner-conversion helper plus direct proof closed the blocker class around `can we map one preferred label back to one explicit candidate card safely`
- the next honest blocker is no longer conversion itself; it is `when does that contract-local pilot_winner become one lane-level concrete first supervised pilot without widening into owner-side mutation or execution-home doctrine`
- this is narrower than owner-repo implementation, `_stack` admission, or live execution-home choice

## Frozen Contract

### `family_name`

- `single supervised pilot winner selection`

### `expected_contract_artifact`

- one exact root-owned selection surface that accepts:
  - one explicit winner-conversion result already produced by the admitted conversion family
  - one preserved explicit `pilot_winner` candidate card carried by that conversion result
  - one preserved conversion-local failure surface explaining when no concrete selection is admissible
- the selection must evaluate only admitted selection dimensions:
  - whether `conversion_status` is exactly `winner_selected`
  - whether `conversion_reasons` is exactly empty
  - whether `pilot_winner` is still one explicit candidate card already preserved by the admitted conversion family
  - whether the preserved winner card still stays inside the already admitted protected-surface and root-visibility boundaries
  - whether the concrete selection story depends only on preserved conversion output rather than hidden repo discovery, hidden branch or worktree discovery, or transcript-memory reconstruction
- the selection must fail closed if:
  - `conversion_status` is `no_winner`
  - `conversion_reasons` is not empty
  - `pilot_winner` is missing or not explicit
  - the selection argument depends on repo discovery, branch discovery, or worktree discovery beyond the preserved candidate card
  - the selection argument depends on owner-readiness tie-breaking, `_stack` execution-home choice, deploy/publication, archive/delete, `.env`, or secret work as a deciding factor
  - the selection argument tries to widen directly into owner-repo mutation, worker launch, or execution-home routing

### `selection_output_boundary`

- output is still contract-only at this stage
- no executable owner-side implementation packet, execution-home selection, or `_stack` command surface is admitted here
- the selection contract may define:
  - admissible selection evidence classes
  - fail-closed selection rejection classes
  - the exact boundary between contract-local `pilot_winner` and one concrete first supervised pilot selection
  - the exact boundary between concrete selection and later owner-surface admission
- it may not define:
  - `_stack` command runtime
  - owner-repo mutation
  - branch or worktree discovery
  - automatic implementation routing by hidden scoring
  - later execution-home choice

### `owner_boundary`

- ATLAS root owns this selection contract, the lane-level selection discipline, and the restart consequence of whether one concrete first supervised pilot is explicit yet
- `_stack` remains a later owner only after one concrete pilot is explicit and a real execution-home question exists
- owner repos remain the owners of repo-local implementation and verification truth after one concrete pilot is separately admitted
- Playbook remains a later consumer only after this exact selection seam proves reusable beyond the current lane state

## Exact Marker Decision

Ratcheted:

- `AI Long-Run Batch Orchestration: 58% -> 59%`

Why the move is honest:

- one real blocker class is now cleared
- the lane no longer relies on an implied seam between the landed winner-conversion helper and later concrete first supervised pilot selection
- restart truth can now point at one exact winner-selection contract instead of only inferring that such a boundary should exist next

Why it still stays low:

- no owner-side pilot implementation is admitted yet
- no `_stack` execution-home semantics are admitted yet
- no owner-side pilot proof or adoption widening happened here

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot winner selection owner-surface admission pass 481`

Why:

- the winner-selection contract is now exact
- the next honest question is which surface owns the producing and consuming home for that selection before support, implementation, or execution-home doctrine is discussed
- selecting one owner-side implementation path would still be premature before that owner-facing boundary is frozen

## Rule

`Winner Selection Contract Before Owner Routing`

When the winner-conversion helper is already explicit and directly proved, freeze the exact selection contract for turning one contract-local `pilot_winner` into one concrete first supervised pilot before owner-surface admission, owner-repo implementation, or `_stack` execution-home widening.

## Failure Mode

`Concrete Pilot By Conversion Leakage`

This lane becomes synthetic when one proved `pilot_winner` card is treated as an obvious owner-side implementation decision before the winner-selection contract makes the admissible evidence classes, fail-closed rejection rules, and later owner/execution boundaries explicit.
