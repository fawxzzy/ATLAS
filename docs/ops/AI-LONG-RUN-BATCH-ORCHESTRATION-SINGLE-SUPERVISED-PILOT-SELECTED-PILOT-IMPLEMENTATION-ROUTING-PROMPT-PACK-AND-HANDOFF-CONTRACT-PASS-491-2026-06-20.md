# AI Long-Run Batch Orchestration Single Supervised Pilot Selected-Pilot Implementation-Routing Prompt-Pack And Handoff Contract Pass 491 - 2026-06-20

- Date: `2026-06-20`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded prompt-pack and handoff contract`
- Scope: `freeze the exact worker objective, proof obligations, allowed-touch boundary, and stop conditions for the already-admitted fail-closed selected-pilot implementation-routing slice`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-CONTRACT-FREEZE-PASS-487-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-OWNER-SURFACE-ADMISSION-PASS-488-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-SUPPORTING-LANE-ADMISSION-PASS-489-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-FIRST-IMPLEMENTATION-ADMISSION-PASS-490-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-SELECTION-FIRST-IMPLEMENTATION-WORKER-CLUSTER-RECONCILIATION-2026-06-20.md`
  - `ops/atlas/pilot_winner_selection.py`
  - `tests/test_atlas_pilot_winner_selection.py`
- Control-plane checkpoint: `main`

## Objective

Freeze one compact authoritative prompt-pack and handoff contract for the already-admitted first implementation slice of the root-owned `single supervised pilot selected-pilot implementation-routing` family.

This pass does not:

- implement code
- choose one concrete owner-side implementation packet, repo, worktree, branch, or execution home by adjacency
- reopen winner selection, selector redesign, or queue-or-registry doctrine
- infer `_stack` execution-home ownership, owner-repo mutation authority, or Playbook doctrine export posture
- reopen deploy/publication, archive/delete, `.env`, or secret work
- widen into manifest, restart-surface, runtime-state, or broader orchestration redesign

## Root Health Baseline

- pass 487 already froze the exact selected-pilot implementation-routing contract
- pass 488 already admitted ATLAS root as the owner-facing home for that routing seam
- pass 489 already proved separate support still honestly holds at `none yet`
- pass 490 already froze the exact first implementation slice and exact proof matrix around the admitted `selection_status`, `selected_pilot`, `selection_reasons`, `routing_status`, `implementation_route`, and `routing_reasons` surfaces
- root validation is currently clean at `critical=0 error=0 warning=11 info=0`

## Inherited Contract Spine

The future worker must inherit exactly:

- pass 487 exact selected-pilot implementation-routing contract
- pass 488 root control-plane owner admission
- pass 489 supporting-lane hold at `none yet`
- pass 490 exact first implementation slice and exact proof matrix

No worker may reinterpret those seams as suggestions.
They are contract inputs.

## Exact Worker Objective

The next worker is allowed to pursue one exact implementation objective only:

- implement one root-local fail-closed selected-pilot implementation-routing helper in `ops/atlas/pilot_selected_implementation_routing.py` plus one direct proof file in `tests/test_atlas_pilot_selected_implementation_routing.py` so the helper preserves only `selection_status`, `selected_pilot`, `selection_reasons`, `routing_status`, `implementation_route`, and `routing_reasons`, converts only an explicit `pilot_selected` winner-selection result with one explicit `selected_pilot` card and empty selection reasons into the exact explicit implementation-route card already present in the bundle, emits only `implementation_route_admissible` or `no_route`, reports only the admitted `routing_reasons`, fails closed on hidden or widened routing inputs, and proves behavior against the frozen pass-490 matrix

The worker is not allowed to pursue:

- live repo discovery
- branch or worktree enumeration from live repos
- owner-repo mutation authority, owner-repo edits, or owner-readiness inference
- `_stack` execution-home inference or command/runtime design
- Playbook doctrine export
- broader selector, manifest, restart-surface, runtime-inventory, or queue-or-registry redesign

## Exact Preserved Routing Surface

The worker must preserve exactly these surfaces:

- `selection_status`
- `selected_pilot`
- `selection_reasons`
- `routing_status`
- `implementation_route`
- `routing_reasons`

The worker may render these surfaces only.
The worker may not widen them into live repo inventory, branch inventory, worktree inventory, owner-repo mutation authority, owner-readiness doctrine, `_stack` execution-home choice, Playbook doctrine export, deploy/publication semantics, or protected-surface exceptions.

## Exact Mandatory Proof Cases

The worker must satisfy exactly these proof cases:

1. explicit `pilot_selected` result with empty selection reasons
   - preserve `routing_status` as `implementation_route_admissible`
   - preserve `implementation_route` as the explicit `selected_pilot` card
   - preserve `routing_reasons` as `[]`

2. `no_selection` selection status
   - preserve `routing_status` as `no_route`
   - preserve `implementation_route` as `null`
   - preserve `selection_status_not_pilot_selected`

3. non-empty selection reasons despite `pilot_selected`
   - preserve `routing_status` as `no_route`
   - preserve `implementation_route` as `null`
   - preserve `selection_reasons_present`

4. missing or non-explicit `selected_pilot`
   - preserve `routing_status` as `no_route`
   - preserve either `selected_pilot_missing` or `selected_pilot_not_explicit`

5. routing argument invents live repo discovery, branch/worktree enumeration, `_stack` execution-home inference, owner-repo mutation authority, or Playbook doctrine export
   - preserve `routing_status` as `no_route`
   - preserve the exact corresponding routing reason

6. preserved `selected_pilot` violates the admitted protected-surface boundary
   - preserve `routing_status` as `no_route`
   - preserve `protected_surface_violation`

## Exact No-Mutation / No-Discovery / No-Owner-Routing-By-Adjacency Boundary

The worker must carry this wording forward verbatim:

`No-mutation, no-discovery, and no-owner-routing-by-adjacency guard: this packet may implement one explicit fail-closed selected-pilot implementation-routing helper plus direct proof for the already-admitted selection_status, selected_pilot, selection_reasons, routing_status, implementation_route, and routing_reasons surfaces, but it may not discover live repos beyond the explicit preserved selected-pilot card, enumerate worktrees or branches, infer owner-repo mutation authority, owner readiness, Playbook doctrine export, or _stack execution-home ownership, widen one route-admissible card into owner-repo mutation or _stack execution-home choice, mutate queue, registry, runtime, session, merge, manifest, archive, repair, blocker, or owner-repo state, or widen into deploy, publication, .env, secret, or protected-surface work.`

## Exact No Hidden Transcript-State Inference Boundary

The worker must carry this rule forward:

- use only the cited durable surfaces in this receipt chain plus the admitted winner-selection helper/test files and the new routing helper/test files
- do not infer routing truth, owner readiness, execution-home meaning, or protected-surface exceptions from uncited transcript memory or broad root residue

## Exact Allowed Future Implementation Surfaces

The worker may touch only:

- `ops/atlas/pilot_selected_implementation_routing.py`
- `tests/test_atlas_pilot_selected_implementation_routing.py`

## Exact Forbidden Future Implementation Surfaces

The worker must not touch:

- `ops/atlas/marker_knockout_selector.py`
- `ops/atlas/continuity.py`
- `ops/atlas/pilot_selection_criteria.py`
- `ops/atlas/pilot_candidate_comparison.py`
- `ops/atlas/pilot_winner_conversion.py`
- `ops/atlas/pilot_winner_selection.py`
- `tests/test_atlas_pilot_selection_criteria.py`
- `tests/test_atlas_pilot_candidate_comparison.py`
- `tests/test_atlas_pilot_winner_conversion.py`
- `tests/test_atlas_pilot_winner_selection.py`
- `docs/atlas-book/*`
- `docs/memory/initiatives/*`
- owner repos under `repos/*`
- `_stack` helper-runtime or command-design surfaces
- `archive/`, `.vercel`, `.env*`, `secrets/`, screenshots, captures, `.playwright-mcp/`, or broad untracked root backlog

## Exact Stop Conditions

The worker must stop and return immediately if the slice requires:

- real repo discovery or routing beyond the explicit preserved `selected_pilot` card
- worktree or branch enumeration from live repos
- owner-repo mutation authority, owner-repo edits, Playbook doctrine export, or `_stack` execution-home inference
- deploy/publication, `.env`, secret, archive/delete, or protected-surface touch
- new routing-status values, new routing-reason values, or widened selection/routing fields beyond the admitted set
- selector, manifest, restart-surface, or runtime-state edits to make the helper work

If one of those triggers appears, the work is no longer valid first-slice implementation work.

## Exact Handoff Prompt-Pack Spine

Any future worker prompt-pack must include:

1. the exact implementation objective
2. the inherited passes 487 through 490 as frozen inputs
3. the exact preserved routing surface
4. the exact proof matrix
5. the exact no-mutation, no-discovery, and no-owner-routing-by-adjacency guard verbatim
6. the exact no-hidden-transcript-state boundary
7. the exact allowed-touch and forbidden-touch surfaces
8. the exact stop-and-return conditions

Mirror surfaces may restate this spine.
They may not redefine it.

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot selected-pilot implementation-routing implementation-readiness closeout and worker-routing pass 492`

## Marker Decision

- `none`

Why:

- this pass narrows the worker handoff contract only
- no new executed state, broader proof-backed adoption, or blocker clearance landed

## Rule

Freeze the worker handoff contract before authorizing any first-slice implementation work for the root-owned selected-pilot implementation-routing family.

## Failure Mode

`Selected-Pilot-Implementation-Routing Handoff Drift`

This family becomes dishonest when the worker handoff contract stays implicit and the first routing slice expands through prompt wording into owner-repo mutation, live repo discovery, Playbook doctrine export, `_stack` execution-home inference, or protected-surface exceptions that the durable chain has not admitted.
