# Cortex Dual-Mode Replacement Readiness ChatGPT/Codex role inventory first-implementation admission

- Date: `2026-07-09`
- Lane: `Cortex Dual-Mode Replacement Readiness`
- Mode: `ATLAS-root docs-only first-implementation admission`
- Control-plane checkpoint: `main@036147e8`
- Marker movement: none

## Decision

Admit one future root-owned helper/test pair for bounded ChatGPT/Codex role inventory classification against the frozen Cortex dual-mode operating model.

The next exact packet is:

```text
Cortex Dual-Mode Replacement Readiness ChatGPT/Codex role inventory prompt-pack and worker handoff contract
```

This admission does not implement the helper, read hidden transcripts, inspect external product sessions, mutate repos, or move any marker.

## Objective

Freeze the smallest honest implementation slice that can turn the dual-mode operating-model contract into deterministic role-inventory output.

The first implementation is advisory only.

It must:

1. read root-owned dual-mode doctrine inputs
2. classify current external scaffolding duties into frozen role buckets
3. map those duties to future Cortex interfaces
4. preserve authority-denial and split-brain warnings explicitly
5. emit deterministic inventory output without inventing historical usage truth

## Admitted Future Surfaces

Only these future files are admitted:

- `ops/cortex/chatgpt_codex_role_inventory.py`
- `tests/test_cortex_chatgpt_codex_role_inventory.py`

No other file is admitted by this packet.

## Why This Is Cortex Dual-Mode Work

This lane is root-owned Cortex governance work because the future helper would operate only on:

- ATLAS-root receipts
- frozen operating-model doctrine
- durable workflow-profile context
- root-owned pattern and boundary truth

It is not product implementation work because the helper would classify roles and gaps, not perform synthesis or execution directly.

## Why This Is Not Owner-Repo Work

This inventory packet is not Fitness, Mazer, DiscordOS, Trove, Foundation, or Playbook implementation work because the admitted helper may only classify roles already described in root-owned doctrine.

It may not:

- mutate owner repos
- inspect owner-repo uncommitted work as role truth
- open deploy/debug loops
- claim owner-side adoption

## Admitted Scope

The future helper may do only this:

1. load the frozen dual-mode operating model
2. load root-owned supporting doctrine inputs
3. classify current external scaffolding roles into deterministic role records
4. map each record to one future Cortex interface target
5. report unmapped roles, overlap risks, and authority-denial requirements

The future helper may not:

- call ChatGPT or Codex APIs
- inspect hidden transcript history
- parse external browser/session state
- infer historical usage beyond the admitted doctrine inputs
- widen into implementation planning beyond inventory output

## Required Root-Owned Inputs

The future helper may consume only root-owned, reproducible sources such as:

- `docs/ops/CORTEX-DUAL-MODE-AND-SIMULATION-SUBSTRATE-MARKER-ADMISSION-2026-07-09.md`
- `docs/ops/CORTEX-DUAL-MODE-REPLACEMENT-READINESS-OPERATING-MODE-CONTRACT-FREEZE-2026-07-09.md`
- `docs/memory/profiles/zachariah_workflow_profile.md`
- `AGENTS.md`
- `docs/atlas-book/05-receipt-index.md`
- `docs/registry/STACK-REPO-INVENTORY.json`

The first implementation is not admitted to depend on:

- hidden transcripts
- ChatGPT export archives
- Codex session dumps
- owner-repo receipts as primary truth
- live product/API queries
- uncommitted diffs

## Admitted Current Role Classes

The future helper may classify only these current external-scaffolding role classes:

- `synthesis_strategy`
- `research_synthesis`
- `packet_framing`
- `operator_facing_tradeoff_compression`
- `execution_mutation`
- `verification_and_tests`
- `proof_and_receipt_capture`
- `execution_reconciliation`

These classes are admitted because they are already implied by the frozen dual-mode operating model.

## Admitted Future Cortex Target Classes

The future helper may map current roles only to these future target classes:

- `cortex_synthesis_interface`
- `cortex_execution_interface`
- `cortex_bridge`
- `shared_atlas_substrate`
- `shared_playbook_doctrine_substrate`

No simulation mapping is admitted in this packet because that belongs to the separate simulation lane.

## Required Output Shape

The future helper must emit deterministic advisory inventory only.

Minimum top-level output fields:

- `schema_version`
- `status`
- `safe_to_use`
- `current_role_count`
- `mapped_role_count`
- `unmapped_role_count`
- `current_roles`
- `future_interface_targets`
- `shared_substrate_dependencies`
- `authority_denials`
- `split_brain_risks`
- `warnings`
- `blockers`

Each role record must be able to carry:

- `role_id`
- `current_role_class`
- `current_system`
- `role_summary`
- `future_target`
- `shared_substrate_dependency`
- `authority_requirements`
- `migration_notes`

The helper must fail closed rather than inventing missing role truth.

## Required Classification Behavior

The future helper must preserve the frozen contract truths:

- ChatGPT-style systems are current synthesis scaffolding
- Codex-style systems are current execution scaffolding
- Cortex must remain one substrate with multiple interfaces
- Bridge duties remain explicit rather than silently merged into synthesis or execution
- ATLAS receipts/manifests/Book/read-model surfaces remain canonical shared truth
- Playbook doctrine remains shared contract truth

## Required Safety Behavior

The future helper must:

- stay read-only
- avoid hidden transcript scraping
- preserve marker-write denial
- preserve owner-repo mutation denial
- preserve secret/deploy/workflow denial
- keep unmapped roles explicit instead of guessing
- keep overlap and split-brain risks explicit instead of collapsing them into success claims

## Role Classes Still Excluded

The future helper must continue to exclude:

- simulation-interface duties
- connector-specific live action duties
- owner-side deploy/debug responsibilities
- any role that depends on secret-bearing evidence
- any role classification based only on undocumented session memory

These exclusions exist to keep the first inventory slice bounded to the dual-mode lane.

## Required Split-Brain Reporting

The future helper must be able to report at least these risk classes when present:

- `memory_truth_split`
- `doctrine_truth_split`
- `execution_truth_split`
- `marker_authority_split`
- `bridge_scope_ambiguity`

These are warnings and blockers only, not automatic decisions.

## Expected CLI Posture

The future helper is admitted with this intended first interface:

- `python ops/cortex/chatgpt_codex_role_inventory.py`
- `--json`
- `--output <root-relative tmp path>`
- `--strict`

These flags are admitted as the intended first surface and may be tightened by the next prompt-pack packet.

## Required Status Classes

Expected status classes:

- `ok`
- `advisory_gap`
- `blocker`
- `internal_error`

## What The Future Helper Must Not Do

The future helper must not:

- call external APIs
- inspect browser state
- read archived chat logs
- mutate repos
- stage, commit, or push
- emit final receipts
- move markers
- claim Cortex adoption is complete

## Proof Matrix For The Future Worker

The future implementation must prove at least:

1. deterministic classification of ChatGPT-style synthesis duties
2. deterministic classification of Codex-style execution duties
3. deterministic mapping of bridge-only duties
4. explicit shared-substrate dependency output for ATLAS truth
5. explicit shared-substrate dependency output for Playbook doctrine
6. failure-closed behavior when the operating-model receipt is missing
7. failure-closed behavior when required role classes cannot be mapped safely
8. rejection of hidden transcript or external-session source attempts
9. safe `tmp/**` output acceptance
10. protected or absolute output-path rejection
11. deterministic JSON ordering
12. `--strict` exits nonzero on blockers

## Not Yet Admitted

This packet does not yet admit:

- exact fixture filenames
- exact worker command strings
- implementation-readiness verdict
- worker execution
- marker movement to `10%` or `20%`

Those belong to the next prompt-pack packet and later implementation packets.

## Marker Decision

No marker moves.

`Cortex Dual-Mode Replacement Readiness` remains `0%`.

Reason:

- this packet admits only the future helper/test slice
- no inventory helper is implemented yet
- no inventory proof exists yet
- no milestone completion is justified from admission alone

## Next

Open only this next packet:

```text
Cortex Dual-Mode Replacement Readiness ChatGPT/Codex role inventory prompt-pack and worker handoff contract
```

That packet may freeze the worker instructions, fixture posture, proof commands, and stop conditions, but it must still keep hidden transcript scraping, external live-product dependence, owner-repo mutation, marker movement, secret handling, and deploy mutation denied.

## Completion

Completion: `100%` for the ChatGPT/Codex role-inventory first-implementation admission itself.

No owner repo was mutated.
No platform surface was mutated.
No hidden transcripts, secrets, or deploy surfaces were touched.
