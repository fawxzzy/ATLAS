# AI Long-Run Batch Orchestration Queue-Or-Registry Runtime-State Exact Child-Path Selection Owner-Surface Admission Pass 66 - 2026-06-12

- Date: `2026-06-12`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded owner-surface admission`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/02-lanes-and-markers.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/11-system-map-graph.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/architecture/STATE-AND-MEMORY-BOUNDARIES.md`
  - `docs/architecture/AWARENESS-FIRST-WORLD-MODEL.md`
  - `docs/architecture/ORCHESTRATION-BOUNDARIES.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-QUEUE-OR-REGISTRY-CONTRACT-FREEZE-PASS-1-2026-06-09.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-QUEUE-OR-REGISTRY-RUNTIME-STATE-EXACT-CHILD-PATH-SELECTION-CONTRACT-FREEZE-PASS-65-2026-06-12.md`
  - `stack.yaml`
  - `AGENTS.md`
  - `runtime/receipts/validation/stack-validation.latest.md`
  - `runtime/receipts/validation/stack-validation.latest.json`
- Control-plane checkpoint: `main@e6df1c17`

## Objective

Admit the exact owner-facing surface for the contract-frozen `runtime-state exact child-path selection` seam, keep retained-state live-path meaning distinct from artifact-shape choice, runtime-state discovery, and execution semantics, and stop below supporting-lane admission, implementation, or `_stack` execution-home widening.

This pass does not:

- implement helper code
- create live queue or registry state
- choose one final queue-home or registry-home destination class
- choose one exact filename, schema, or snapshot shape
- admit `_stack` execution semantics, worker flow, runtime-state discovery, or supervisor behavior
- admit Playbook doctrine ownership
- admit owner-repo mutation, Fitness work, archive/delete work, deploy/publication work, `.env`, or secret work
- claim that owner-surface admission alone earns marker movement

## Root Health Baseline

- pass 65 already froze one exact retained-state child-path contract for `runtime-state exact child-path selection`
- the unresolved question from pass 65 is which surface honestly owns that exact child-path seam before any support or implementation talk
- the contract already keeps mutable pre-execution queue-or-registry truth under the admitted `queue-home` and `registry-home` destination classes while explicitly deferring exact filename/schema/snapshot shape, artifact-shape choice, runtime-state discovery, and execution semantics
- the seam remains explicitly below validator execution, lifecycle advancement, and `_stack` execution-home semantics
- root validation remains clean at `critical=0 error=0 warning=58 info=0`
- local `HEAD` remains in parity with `origin/main`
- unrelated local edits already exist in `docs/atlas-book/01-current-state.md` and `docs/atlas-book/02-lanes-and-markers.md`, so this owner-admission pass records durable truth in receipts only

## Owner-Surface Candidates Considered

### ATLAS root control-plane surfaces

What they already own:

- stack doctrine
- lane definitions
- cross-repo planning
- path and boundary rules
- restart projection and receipt consequence
- the queue-or-registry batch-entry contract plus the retained-state exact-child-path non-claim boundaries

Why they win:

- the seam is still retained-state live-path classification truth rather than shared execution or runtime-store implementation
- the seam exists to define how mutable pre-execution queue-or-registry truth may narrow from admitted destination-class meaning into one exact retained-state path, not to perform discovery, queue writes, registry writes, or execution
- the exact-child-path contract inherits root-owned boundary semantics around deferred filename/schema/snapshot shape, deferred final artifact-shape choice, no runtime-state discovery, no validator execution, and no owner-repo mutation
- admitting root here keeps the family bounded to retained-state live-path and control-plane truth instead of prematurely collapsing it into helper-runtime or execution semantics

### `_stack`

Why it does not win yet:

- `_stack` owns execution-oriented orchestration contracts, command surfaces, worker flow, and supervised dispatch semantics
- this exact-child-path family has not admitted runtime-state discovery, artifact-shape choice, execution transitions, validator execution, queue writes, registry writes, or shared command-runtime behavior
- moving the seam into `_stack` now would blur root-owned retained-state live-path meaning with later discovery or execution-home semantics

### Playbook

Why it does not win:

- Playbook owns reusable workflow doctrine and cross-repo pattern extraction
- this family is still deciding where one root-bounded retained-state live-path seam lives before any doctrine export is in scope
- Playbook may later consume the pattern, but it does not own this exact-child-path home at this stage

### owner repos

Why they do not win:

- the retained-state exact-child-path contract is cross-repo by design because candidate entries may target different owner repos
- retained-state live-path classification, discovery deferral, and protected-surface rules are stack-level truth
- pushing the seam into one owner repo would collapse root planning into repo-local ownership incorrectly

### admitted destination classes

Why they do not win:

- `queue-home` and `registry-home` are now the admitted placement classes for future mutable pre-execution queue-or-registry truth, but they are still placement classes rather than the owners of the exact-child-path contract meaning
- choosing those classes as the owner now would blur retained-state placement with control-plane ownership
- exact filename, schema, snapshot shape, and final artifact-shape choice remain deferred until a later support or implementation pass makes them explicit

## Admission Decision

### Retained-state live-path meaning remains at root

- `ATLAS root control-plane surfaces`

What that means:

- ATLAS root owns the canonical rule that future mutable pre-execution queue-or-registry truth stays beneath the admitted `queue-home` and `registry-home` destination classes and that exact child-path meaning remains a retained-state control-plane seam rather than runtime-state discovery or execution behavior
- ATLAS root owns the deferral of exact filename, schema, snapshot shape, and final artifact-shape choice
- ATLAS root owns restart-safe projection of the retained-state live-path seam and next-packet consequence
- ATLAS root does not thereby become a runtime-store implementation, live queue writer, registry writer, or execution runner

### State placement remains admitted but not implemented

- `queue-home` and `registry-home` remain the admitted retained-state destination classes for future mutable pre-execution queue-or-registry truth
- exact child-path meaning is now contract-frozen beneath those destination classes
- exact filename, schema, snapshot shape, and final artifact-shape choice remain deferred

### Execution and helper-runtime ownership remain deferred

- `_stack` remains a later candidate owner for:
  - shared command-runtime behavior
  - runtime-state discovery
  - artifact-shape-dependent live state interpretation
  - execution-oriented orchestration
  - validator execution and status-transition semantics
  - worker flow
  - resume or dispatch behavior

### Doctrine ownership remains deferred

- Playbook remains a later candidate consumer for reusable retained-state live-path doctrine extraction

### Repo ownership remains unchanged

- owner repos still own:
  - repo-local mutation truth
  - repo-local verification truth
  - repo-local implementation truth

## Supporting Dependency Decision

- `none yet`

Why:

- owner-surface admission is now exact
- the next honest question is whether any separate supporting lane actually reopens from that decision
- support should be admitted explicitly rather than assumed from eventual future implementation

## Still Not Admitted In This Pass

- exact filename, schema, snapshot shape, or final artifact-shape choice
- live queue or registry writes
- runtime-store implementation
- runtime-state discovery
- validator execution
- `_stack` helper-home or command-design work
- supervised pilot implementation
- unattended execution
- marker movement

## Exact Next Package

- `AI Long-Run Batch Orchestration queue-or-registry runtime-state exact child-path selection supporting-lane admission pass 67`

Why:

- the retained-state exact-child-path contract and owner-facing home are now explicit
- the next honest question is whether one exact separate support seam reopens now or whether the family remains root-local at this stage
- implementation would be premature before that support question is frozen

## Marker Decision

- `none`

Why:

- this pass admits the owner-facing surface only
- it does not land code, execution proof, or broader operator adoption

## Rule

Retained-State Exact Child-Path Meaning Stays At Root Before Discovery

Until a runtime-state exact-child-path seam crosses from root-bounded retained-state truth into shared helper-runtime or runtime-state discovery semantics, the owner of the seam stays with ATLAS root control-plane surfaces even though the admitted destination classes are already the placement classes.

## Pattern

exact child-path contract freeze -> root control-plane owner admission -> explicit support decision -> only then discuss artifact-shape, discovery, helper runtime, or implementation

## Failure Mode

`Retained-Live-Path Ownership Collapse`

This family becomes dishonest when the admitted destination classes are mistaken for ownership of retained-state exact-child-path meaning before support and implementation semantics are explicitly admitted.

## What This Pass Proves

This pass proves:

- the `runtime-state exact child-path selection` seam now has one exact owner-facing home
- that home is `ATLAS root control-plane surfaces`
- the admitted destination classes remain the placement classes for future mutable pre-execution state, but not the owner of the seam
- `_stack`, Playbook, and owner repos are all still outside ownership for this stage

This pass does not prove:

- that any supporting lane is admitted
- that any exact filename, schema, or snapshot shape is chosen
- that implementation, runtime-state discovery, or execution semantics are admitted
