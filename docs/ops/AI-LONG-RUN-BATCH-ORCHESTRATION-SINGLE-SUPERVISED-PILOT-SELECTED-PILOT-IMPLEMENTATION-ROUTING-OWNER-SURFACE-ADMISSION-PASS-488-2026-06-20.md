# AI Long-Run Batch Orchestration Single Supervised Pilot Selected-Pilot Implementation-Routing Owner-Surface Admission Pass 488 - 2026-06-20

- Date: `2026-06-20`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded owner-surface admission`
- Scope: `freeze the exact root-owned producing and consuming home for the selected-pilot implementation-routing family before supporting-lane admission, implementation admission, or execution-home widening`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/memory/initiatives/continuity-manifest-ai-long-run-batch-orchestration.json`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/02-lanes-and-markers.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/11-system-map-graph.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/atlas-book/13-vision-and-endgames.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-IMPLEMENTATION-ROUTING-CONTRACT-FREEZE-PASS-487-2026-06-20.md`
  - `ops/atlas/marker_knockout_selector.py`
  - `tests/test_atlas_marker_knockout_selector.py`
- Control-plane checkpoint: `main`

## Objective

Admit the exact owner-facing home for the contract-frozen `single supervised pilot selected-pilot implementation-routing` family, keep supporting-lane posture, concrete implementation, and execution-home semantics deferred, and stop below supporting-lane admission, first implementation, or owner-repo mutation.

This pass does not:

- choose one owner-side implementation packet, repo mutation plan, branch, or worktree
- admit `_stack` execution semantics, helper-home, command design, worker flow, or resume/merge behavior
- admit Playbook doctrine export
- admit owner-repo implementation or verification work
- admit deploy/publication work, `.env`, or secret work
- claim that owner-surface admission alone earns marker movement

## Root Health Baseline

- pass 487 already froze one exact `single supervised pilot selected-pilot implementation-routing` contract
- the unresolved question from pass 487 is which surface honestly owns that routing family before any support or implementation talk
- the continuity manifest and Book restart surfaces already project `pass 488` as the current immediate packet, but no durable basis receipt existed yet for machine-readable consumers
- the selector helper now exposes the exact current packet and expects a real basis receipt with explicit mode and scope metadata

## Owner-Surface Candidates Considered

### ATLAS root control-plane surfaces

What they already own:

- stack doctrine and path discipline
- cross-repo planning and lane consequence
- the selected-pilot implementation-routing contract truth
- restart projection, continuity-manifest posture, and receipt consequence
- machine-readable selector posture for the current root campaign

Why they win:

- the family is still pre-support, pre-implementation, pre-execution, and cross-repo by design
- the routing family governs admissible input, allowed evidence classes, forbidden evidence classes, and the exact boundary between one explicit `selected_pilot` card and later owner-side implementation admission
- choosing one owner repo or execution home from adjacency would still be dishonest before the routing home is explicit
- the producing and consuming surfaces already live in ATLAS root restart, continuity, and selector helpers rather than in one owner repo or shared execution runtime

### `_stack`

Why it does not win yet:

- `_stack` owns later execution-oriented orchestration semantics, command runtime, and worker flow
- this family has not admitted a separate support seam, implementation packet, or execution-home dependency yet
- moving the selected-pilot routing family into `_stack` now would blur root-owned pre-execution admission truth with later runtime doctrine

### Playbook

Why it does not win:

- Playbook owns reusable doctrine and pattern extraction
- this family is still fixing the current exact owner-facing home before any reusable doctrine export is in scope

### owner repos

Why they do not win:

- the routing family is intentionally upstream of any concrete owner-side implementation packet
- owner repos do not own the lane-level routing rules before one later owner-side implementation surface is explicitly admitted

## Admission Decision

### Selected-pilot implementation-routing home remains at root

- `ATLAS root control-plane surfaces`

What that means:

- ATLAS root owns the canonical `single supervised pilot selected-pilot implementation-routing` family
- ATLAS root owns the meaning of admissible routing input, allowed evidence classes, forbidden evidence classes, and the boundary between one explicit `selected_pilot` card and later owner-side implementation admission
- ATLAS root owns the continuity-manifest and machine-readable selector consequence for this routing family
- ATLAS root does not thereby become the owner of repo-local implementation, concrete execution, or `_stack` runtime semantics

### Execution ownership remains deferred

- `_stack` remains a later candidate owner only after one implementation slice is explicit and execution semantics are separately admitted

### Doctrine ownership remains deferred

- Playbook remains a later consumer for reusable doctrine only after the routing family is proven useful beyond this exact lane state

### Repo ownership remains unchanged

- owner repos still own:
  - repo-local mutation truth
  - repo-local verification truth
  - repo-local implementation truth

## Supporting Dependency Decision

- `not admitted in this pass`

Why:

- owner-surface admission is now exact
- the producing and consuming surfaces remain root-local at this stage
- the next honest question is whether one separate supporting seam actually reopens or whether the family still honestly holds at `none yet`

## Still Not Admitted In This Pass

- supporting-lane posture
- concrete owner-side pilot implementation
- `_stack` helper-home or command-design work
- broader doctrine export
- marker movement

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot selected-pilot implementation-routing supporting-lane admission pass 489`

Why:

- the implementation-routing contract and owner-facing home are now explicit
- the next honest question is whether one exact separate support seam reopens now or whether the family remains root-local at this stage
- owner-repo implementation or execution-home doctrine would still be premature before that support question is frozen

## Marker Decision

- `none`

Why:

- this pass admits the owner-facing surface only
- it does not land execution, proof-backed adoption, or blocker conversion

## Rule

`Selected-Pilot Routing Home Before Support Or Implementation`

Until the selected-pilot implementation-routing family crosses from root-owned admission truth into a separate support seam or later concrete implementation, the owner-facing home belongs to ATLAS root control-plane surfaces rather than `_stack`, Playbook, or any owner repo.

## Pattern

selected-pilot implementation-routing contract freeze -> root control-plane owner admission -> explicit support decision -> only then discuss implementation admission or execution-home semantics

## Failure Mode

`Selected-Pilot Routing Home Collapse`

This family becomes dishonest when cross-repo selected-pilot implementation-routing rules are pushed into `_stack`, Playbook, or one owner repo before supporting-lane posture and later implementation semantics are explicitly admitted.
