# AI Long-Run Batch Orchestration Single Supervised Pilot Selection Criteria Contract Freeze Pass 462 - 2026-06-18

- Date: `2026-06-18`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded contract freeze`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/memory/initiatives/continuity-manifest-ai-long-run-batch-orchestration.json`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/02-lanes-and-markers.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/11-system-map-graph.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/atlas-book/13-vision-and-endgames.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/ROOT-BOUNDED-LANE-SELECTION-AFTER-AI-LONG-RUN-BATCH-ORCHESTRATION-QUEUE-OR-REGISTRY-FAMILY-EXHAUSTION-CLOSEOUT-2026-06-17.md`
  - `docs/ops/AI-REPETITION-TO-AUTOMATION-PIPELINE-SELECTOR-LANE-EXHAUSTION-OR-FALLBACK-ROUTING-2026-06-18.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-CONTINUITY-MANIFEST-SEED-AND-RATCHET-DECISION-PASS-461-2026-06-18.md`

## Objective

Clear the generic `queue-or-registry active follow-on` boundary by admitting one exact non-`queue-or-registry` AI Long-Run packet, and freeze the first supervised-pilot selection criteria inside ATLAS-root doctrine without widening into `_stack` execution semantics, Playbook doctrine export, or owner-repo implementation.

This pass does not:

- reopen the exhausted `queue-or-registry` family
- choose one concrete pilot repo, worktree, or branch
- admit `_stack` worker flow, launch, resume, merge, or runtime-state mutation
- admit Playbook-owned verification or closeout export
- authorize unattended multi-hour execution, deploy, publication, archive/delete, `.env`, or secret work

## Root State

- branch: `main`
- shared root remains dirty from adjacent durable work; this pass stays inside bounded doctrine, continuity, and restart surfaces
- marker posture before this pass:
  - `AI Long-Run Batch Orchestration: 52%`

## Exact Weak Link Before Repair

Before this pass, current restart truth had already established:

- the `queue-or-registry` family is exhausted through the reconciled top-level `world_model` branch
- the AI Repetition selector family is exhausted for now
- `AI Long-Run Batch Orchestration` is the active ATLAS-side lane again
- the lane is manifest-backed

But the lane still rested on one generic active boundary:

- `AI Long-Run Batch Orchestration queue-or-registry active follow-on`

That was enough to keep restart honest, but not enough to tell the next worker which exact non-`queue-or-registry` packet is admissible now.

## Candidate Comparison

### `_stack` execution-semantics handoff

Why it does not win now:

- the original lane charter still says doctrine, lane-contract, and ownership-split work come first
- `_stack` owns execution-oriented orchestration semantics later, not the first post-exhaustion root-only doctrine clarification
- routing directly into `_stack` from the generic fallback would skip the narrower root-owned pilot-selection seam

### Playbook verification or closeout doctrine export

Why it does not win now:

- reusable doctrine export remains later than the exact ATLAS-root criteria for admitting the first supervised pilot
- current truth still lacks one compact root-owned selection surface that says what qualifies as that first pilot
- exporting doctrine before the selection criteria are explicit would widen too early

### Concrete pilot repo or worktree selection

Why it does not win now:

- the lane charter says the first pilot should be intentionally narrow, but it does not yet authorize choosing one actual repo candidate by adjacency
- selecting one actual repo, branch, or worktree now would jump past the criteria that must govern that choice

### Single supervised pilot selection criteria

Why it wins now:

- the original lane charter explicitly gives ATLAS root ownership of pilot selection criteria
- it is narrower than `_stack` execution semantics, Playbook doctrine export, or choosing one concrete pilot candidate
- it clears the exact blocker class from pass 461 by replacing the generic active fallback with one explicit non-`queue-or-registry` next packet

## Frozen Contract

### `family_name`

- `single supervised pilot selection criteria`

### `expected_contract_artifact`

- one exact root-owned criteria surface for the first supervised pilot
- the first pilot must require:
  - exactly one owner repo
  - exactly one explicit target worktree or branch
  - one bounded objective narrow enough for one reviewable closeout
  - one declared allowed write scope
  - one declared checkpoint surface
  - one declared verification gate
  - one declared closeout artifact
  - one declared park-or-escalation rule
  - one preserved protected-surface exclusion set
- the first pilot should prefer:
  - local or root-visible proof over remote or session-dependent proof
  - deterministic verification over manually reconstructed success criteria
  - isolated worktree execution over mixed-root or mixed-repo execution
  - low-blast-radius doctrine or helper slices over product-runtime mutation
- the first pilot must fail closed if it requires:
  - multiple owner repos
  - hidden write surfaces
  - direct deploy, publication, archive/delete, `.env`, or secret mutation
  - external session recovery as the main proof gate
  - reopening exhausted `queue-or-registry` semantics by implication

### `owner_boundary`

- ATLAS root owns this criteria contract, restart projection, and the decision boundary for whether one concrete pilot candidate is admissible next
- `_stack` remains a later owner for execution semantics only after a concrete pilot candidate is selected under these criteria
- Playbook remains a later consumer for reusable verification and closeout doctrine only after those criteria are proven useful
- owner repos remain the owners of repo-local implementation and verification truth

## Exact Marker Decision

Ratcheted:

- `AI Long-Run Batch Orchestration: 52% -> 53%`

Why the move is honest:

- the exact blocker from pass 461 is now cleared
- the lane no longer rests on a generic active fallback packet
- one explicit non-`queue-or-registry` next family is now admitted and contract-frozen

Why it still stays low:

- no concrete pilot candidate is selected yet
- no `_stack` execution semantics are admitted yet
- no owner-side pilot proof or adoption widening happened here

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot selection criteria owner-surface admission pass 463`

Why:

- the criteria are now exact
- the next honest question is whether the producing and consuming home for those criteria stays entirely inside ATLAS-root control-plane surfaces
- choosing one concrete pilot candidate would still be premature before that owner-facing boundary is frozen

## Rule

`Criteria Before Candidate`

When one doctrine-first orchestration lane leaves an exhausted helper subfamily, admit the explicit criteria for the first concrete pilot before choosing the pilot candidate, execution home, or reusable doctrine export.

## Failure Mode

`Pilot By Adjacency`

This lane becomes synthetic when a concrete repo, worktree, or execution home is chosen from adjacency pressure before the pilot-selection criteria are explicitly frozen.
