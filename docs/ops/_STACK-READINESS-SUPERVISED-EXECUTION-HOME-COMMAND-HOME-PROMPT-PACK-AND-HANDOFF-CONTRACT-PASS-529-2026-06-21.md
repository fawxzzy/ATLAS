# _Stack Readiness Supervised Execution-Home Command-Home Prompt-Pack And Handoff Contract Pass 529 - 2026-06-21

- Date: `2026-06-21`
- Lane: `_stack Readiness supervised execution-home command-home prompt-pack and handoff contract pass 529`
- Mode: `docs-only root-bounded prompt-pack and handoff contract`
- Scope: `freeze the exact worker objective, proof obligations, allowed-touch boundary, and stop conditions for the already-admitted fail-closed command-home-selection slice`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/ops/_STACK-READINESS-SUPERVISED-EXECUTION-HOME-COMMAND-HOME-SELECTION-CONTRACT-FREEZE-PASS-525-2026-06-21.md`
  - `docs/ops/_STACK-READINESS-SUPERVISED-EXECUTION-HOME-COMMAND-HOME-OWNER-SURFACE-ADMISSION-PASS-526-2026-06-21.md`
  - `docs/ops/_STACK-READINESS-SUPERVISED-EXECUTION-HOME-COMMAND-HOME-SUPPORTING-LANE-ADMISSION-PASS-527-2026-06-21.md`
  - `docs/ops/_STACK-READINESS-SUPERVISED-EXECUTION-HOME-COMMAND-HOME-FIRST-IMPLEMENTATION-ADMISSION-PASS-528-2026-06-21.md`
- Control-plane checkpoint: `main`

## Objective

Freeze one compact authoritative prompt-pack and handoff contract for the already-admitted first implementation slice of the root-owned command-home-selection family.

This pass does not:

- implement code
- choose one concrete `_stack` command home, command file, runtime home, worker path, owner repo, or mutation packet by adjacency
- reopen command-home-selection contract redesign, support-posture redesign, or selector redesign
- infer runtime-home ownership, worker authority, actual owner-side mutation authority, or Playbook doctrine export posture
- reopen deploy/publication, archive/delete, `.env`, or secret work
- widen into manifest, restart-surface, runtime-state, or broader orchestration redesign

## Root Health Baseline

- pass 525 already froze the exact command-home-selection contract
- pass 526 already admitted `_stack Readiness` control-plane surfaces as the owner-facing home for that command-home-selection seam
- pass 527 already proved separate support still honestly holds at `none yet`
- pass 528 already froze the exact first implementation slice and exact proof matrix around the admitted command-home-selection surface
- root validation is currently clean at `critical=0 error=0 warning=21 info=0`

## Inherited Contract Spine

The future worker must inherit exactly:

- pass 525 exact command-home-selection contract
- pass 526 `_stack Readiness` control-plane owner admission
- pass 527 supporting-lane hold at `none yet`
- pass 528 exact first implementation slice and exact proof matrix

No worker may reinterpret those seams as suggestions.
They are contract inputs.

## Exact Worker Objective

The next worker is allowed to pursue one exact implementation objective only:

- implement one root-local fail-closed command-home-selection helper plus one direct proof file so the helper preserves only `command`, `normalized_candidate_path`, `result_class`, `owner_surface`, `support_posture`, `admitted_evidence_refs`, `blocked_questions`, `routing_note`, `payload`, `command_home_selection_status`, `command_home_selection_question`, and `command_home_selection_reasons`, reads only the already admitted supervised execution-home result plus the exact pass-525-through-pass-528 contract family, emits only the pass-528 admitted command-home-selection status values, question-card boundary, and reason families, fails closed on hidden or widened command-home inputs, and proves behavior against the frozen pass-528 matrix

The worker is not allowed to pursue:

- live repo discovery
- branch or worktree enumeration from live repos
- concrete `_stack` command-home choice or concrete command-file choice
- runtime-home inference
- worker authority, owner-repo edits, or actual owner-side mutation authority
- Playbook doctrine export
- broader selector, manifest, restart-surface, runtime-inventory, or queue-or-registry redesign

## Exact Preserved Command-Home Selection Surface

The worker must preserve exactly these surfaces:

- `command`
- `normalized_candidate_path`
- `result_class`
- `owner_surface`
- `support_posture`
- `admitted_evidence_refs`
- `blocked_questions`
- `routing_note`
- `payload`
- `command_home_selection_status`
- `command_home_selection_question`
- `command_home_selection_reasons`

The worker may render these surfaces only.
The worker may not widen them into live repo inventory, branch inventory, worktree inventory, concrete `_stack` command-home choice, concrete command-file choice, runtime-home choice, worker authority, actual owner-side mutation authority, owner-repo edit authority, Playbook doctrine export, deploy/publication semantics, or protected-surface exceptions.

## Exact Mandatory Proof Cases

The worker must satisfy exactly these proof cases:

1. explicit `contract-visible` supervised execution-home result with aligned pass-525-through-pass-528 truth
   - preserve `command_home_selection_status` as `command_home_selection_admissible`
   - preserve the explicit contract-local question card
   - preserve `command_home_selection_reasons` as `[]`

2. non-`contract-visible` result class
   - preserve `command_home_selection_status` as `no_command_home_selection`
   - preserve `command_home_selection_question` as `null`
   - preserve `result_class_not_contract_visible`

3. missing or non-exact command value, non-success routing note, missing candidate path, or drift in owner-surface/support posture
   - preserve `command_home_selection_status` as `no_command_home_selection`
   - preserve the exact corresponding command-home-selection reason

4. command-home-selection argument invents concrete command-home choice, concrete command-file choice, runtime-home inference, worker authority, owner-repo edits, actual owner-side mutation authority, or Playbook doctrine export
   - preserve `command_home_selection_status` as `no_command_home_selection`
   - preserve the exact corresponding command-home-selection reason

5. payload is not explicit enough to keep the command-home question contract-local
   - preserve `command_home_selection_status` as `no_command_home_selection`
   - preserve `payload_not_explicit`

## Exact No-Mutation / No-Authority / No-Command-Home-By-Adjacency Boundary

The worker must carry this wording forward verbatim:

`No-mutation, no-authority, and no-command-home-by-adjacency guard: this packet may implement one explicit fail-closed command-home-selection helper plus direct proof for the already-admitted command, normalized_candidate_path, result_class, owner_surface, support_posture, admitted_evidence_refs, blocked_questions, routing_note, payload, command_home_selection_status, command_home_selection_question, and command_home_selection_reasons surfaces, but it may not discover live repos beyond the explicit preserved supervised_execution_home result, enumerate worktrees or branches, choose one concrete _stack command home, name one concrete command file, infer runtime-home ownership, worker authority, owner-repo edit authority, actual owner-side mutation authority, Playbook doctrine export, or _stack runtime-home ownership, widen one contract-visible posture into concrete command-home or runtime-home placement, mutate queue, registry, runtime, session, merge, manifest, archive, repair, blocker, or owner-repo state, or widen into deploy, publication, .env, secret, or protected-surface work.`

## Exact No Hidden Transcript-State Inference Boundary

The worker must carry this rule forward:

- use only the cited durable surfaces in this receipt chain plus the new command-home-selection helper/test files
- do not infer command-home truth, runtime-home meaning, worker permission, or protected-surface exceptions from uncited transcript memory or broad root residue

## Exact Allowed Future Implementation Surfaces

The worker may touch only:

- one new root-local command-home-selection helper file
- one direct proof file for that helper

## Exact Forbidden Future Implementation Surfaces

The worker must not touch:

- `ops/atlas/marker_knockout_selector.py`
- `docs/atlas-book/*`
- `docs/memory/initiatives/*`
- owner repos under `repos/*`
- `_stack` runtime or implementation surfaces
- `archive/`, `.vercel`, `.env*`, `secrets/`, screenshots, captures, `.playwright-mcp/`, or broad untracked root backlog

## Exact Stop Conditions

The worker must stop and return immediately if the implementation requires:

- real repo discovery or routing beyond the explicit preserved supervised execution-home result
- worktree or branch enumeration from live repos
- concrete command-home choice, concrete command-file choice, runtime-home choice, worker authority, actual owner-side mutation authority, owner-repo edits, or Playbook doctrine export
- deploy/publication, `.env`, secret, archive/delete, or protected-surface touch
- new status values, new question-card fields, or widened output fields beyond the admitted set
- selector, manifest, restart-surface, or runtime-state edits to make the helper work

If one of those triggers appears, the work is no longer valid first-slice implementation work.

## Exact Handoff Prompt-Pack Spine

Any future worker prompt-pack must include:

1. the exact command-home-selection objective
2. the inherited passes 525 through 528 as frozen inputs
3. the exact preserved command-home-selection surface
4. the exact proof matrix
5. the exact no-mutation, no-authority, and no-command-home-by-adjacency guard verbatim
6. the exact no-hidden-transcript-state boundary
7. the exact allowed-touch and forbidden-touch surfaces
8. the exact stop-and-return conditions

Mirror surfaces may restate this spine.
They may not redefine it.

## Exact Next Package

- `_stack Readiness supervised execution-home command-home implementation-readiness closeout and worker-routing pass 530`

## Marker Decision

- `none`

Why:

- this pass narrows the worker handoff contract only
- no new executed state, broader proof-backed adoption, or blocker clearance landed

## Rule

Freeze the worker handoff contract before authorizing any first-slice implementation work for the root-owned command-home-selection family.

## Failure Mode

`Command-Home Handoff Drift`

This family becomes dishonest when the worker handoff contract stays implicit and the first implementation slice expands through prompt wording into concrete command-home choice, concrete command-file choice, runtime-home inference, worker authority, actual owner-side mutation authority, owner-repo edits, Playbook doctrine export, or protected-surface exceptions that the durable chain has not admitted.
