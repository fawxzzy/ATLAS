# AI Long-Run Batch Orchestration Single Supervised Pilot Selected-Pilot Implementation-Routing Contract Freeze Pass 487 - 2026-06-20

- Date: `2026-06-20`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded contract freeze`
- Scope: `freeze the exact root-owned routing contract for deciding when one already-explicit selected_pilot surface may or may not become one bounded downstream implementation-routing packet without widening into owner-repo mutation, _stack execution-home doctrine, Playbook export, or deploy/publication semantics`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/memory/initiatives/continuity-manifest-ai-long-run-batch-orchestration.json`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/02-lanes-and-markers.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/11-system-map-graph.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/atlas-book/13-vision-and-endgames.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-SELECTION-CONTRACT-FREEZE-PASS-480-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-SELECTION-OWNER-SURFACE-ADMISSION-PASS-481-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-SELECTION-SUPPORTING-LANE-ADMISSION-PASS-482-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-SELECTION-FIRST-IMPLEMENTATION-ADMISSION-PASS-483-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-SELECTION-PROMPT-PACK-AND-HANDOFF-CONTRACT-PASS-484-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-SELECTION-IMPLEMENTATION-READINESS-CLOSEOUT-AND-WORKER-ROUTING-PASS-485-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-WINNER-SELECTION-FIRST-IMPLEMENTATION-WORKER-CLUSTER-RECONCILIATION-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-POST-SINGLE-SUPERVISED-PILOT-WINNER-SELECTION-NEXT-SLICE-SELECTION-PASS-486-2026-06-20.md`
  - `ops/atlas/pilot_winner_selection.py`
  - `tests/test_atlas_pilot_winner_selection.py`

## Objective

Replace the still-implicit `selected_pilot -> implementation-routing packet` seam with one exact root-owned contract that says when a directly proved `selected_pilot` result may or may not route into one bounded downstream implementation packet, while staying below owner-surface admission, support admission, owner-repo mutation, `_stack` execution-home doctrine, Playbook doctrine export, or protected-surface touch.

This pass does not:

- choose one owner-side implementation packet, repo mutation plan, branch, or worktree
- admit `_stack` helper-home, command-design, runtime-state, resume, merge, or dispatch semantics
- admit Playbook doctrine export or reusable consumer framing
- admit owner-repo implementation, deploy/publication, archive/delete, `.env`, or secret work
- admit hidden repo discovery, hidden readiness scoring, or transcript-memory routing
- claim that implementation-routing contract freeze alone proves one owner-side pilot implementation is already admitted

## Root State

- branch: `main`
- current working tree still contains unrelated preserved tracked and untracked residue outside this pass
- marker posture before this pass:
  - `AI Long-Run Batch Orchestration: 60%`

## Exact Weak Link Before Repair

Before this pass, current durable truth already established:

- one exact pilot-selection criteria contract and landed validator helper plus direct proof
- one exact candidate-comparison contract, owner-facing home, support posture, first implementation slice, prompt-pack, readiness closeout, and landed worker cluster
- one exact winner-conversion contract, owner-facing home, support posture, first implementation slice, prompt-pack, readiness closeout, and landed worker cluster
- one exact winner-selection contract, owner-facing home, support posture, first implementation slice, prompt-pack, readiness closeout, and landed worker cluster
- one exact post-winner-selection next-slice selector that already named this routing contract as the next honest downstream seam

But the lane still lacked one explicit contract for the next honest question:

- how one explicit lane-level `selected_pilot` card may or may not route into one bounded downstream implementation packet without inventing owner-surface admission, owner-repo mutation authority, `_stack` execution-home doctrine, Playbook export, or deploy/publication behavior by adjacency

That gap was small but real: restart truth could name `selected-pilot implementation-routing contract freeze pass 487` as the next package, but the routing seam itself was not yet durable.

## Selected-Pilot Implementation Routing

### Owner-surface admission now

Why it does not win now:

- the lane still lacks one exact routing contract that says what qualifies as admissible implementation-routing input and which evidence classes are allowed or forbidden
- selecting the owner-facing home now would turn one explicit `selected_pilot` card directly into an owner-home decision by adjacency
- owner-surface home is the consequence of this routing contract, not part of the contract itself

### `_stack` execution-home or command-home admission now

Why it does not win now:

- `_stack` ownership starts later, only after implementation routing creates one separately admitted execution-home question
- this family is still pre-execution and root-owned by the original lane charter
- routing into `_stack` now would blur control-plane implementation routing with later execution semantics

### Playbook doctrine export now

Why it does not win now:

- reusable doctrine export still depends on proving this exact routing seam in live lane truth first
- current truth still needs the local routing contract before any broader doctrine consumer is honest

### Owner-repo mutation or implementation now

Why it does not win now:

- the preserved `selected_pilot` card already carries candidate identity, target, and bounded scope fields
- what is still missing is the exact control-plane contract for whether and how that preserved card may route further
- mutating an owner repo now would skip the routing contract and owner-surface admission boundaries entirely

### Selected-pilot implementation routing

Why it wins now:

- the winner-selection helper plus direct proof closed the blocker class around `can one explicit selected_pilot card exist safely`
- the next honest blocker is no longer selection itself; it is `how does that explicit selected_pilot card route or fail closed before owner-repo mutation, _stack execution-home, or doctrine widening`
- this seam is narrower than owner-surface admission, owner-repo implementation, `_stack` execution-home admission, or Playbook export

## Frozen Contract

### `family_name`

- `single supervised pilot selected-pilot implementation routing`

### `implementation_routing_input`

- one exact root-owned routing surface that accepts:
  - one explicit winner-selection result already produced by the admitted selection family
  - one preserved explicit `selected_pilot` candidate card carried by that result
  - one preserved selection-local failure surface explaining when no route is admissible
- implementation-routing input is admissible only when:
  - `selection_status` is exactly `pilot_selected`
  - `selection_reasons` is exactly empty
  - `selected_pilot` is still one explicit candidate card already preserved by the admitted winner-selection family
  - the preserved selected-pilot card still stays inside the already admitted protected-surface and root-visibility boundaries
  - the routing argument depends only on preserved selected-pilot output plus current durable root-owned receipt, Book, and continuity-manifest surfaces
- admitted routing evidence classes may use only:
  - preserved selected-pilot control fields already carried on the explicit candidate card, including `owner_repo_count`, `target_kind`, `target_ref`, `objective_summary`, `allowed_write_scope`, `checkpoint_surface`, `verification_gate`, `closeout_artifact`, `park_or_escalation_rule`, and `protected_surface_exclusions`
  - preserved winner-selection status fields `selection_status` and `selection_reasons`
  - current root-owned lane receipts and restart surfaces that define the existing control-plane boundary
- forbidden evidence classes:
  - live repo, branch, or worktree discovery beyond the preserved selected-pilot card
  - owner readiness, release readiness, or deploy readiness scoring
  - execution-home, `_stack`, helper-home, or command-home inference
  - Playbook export-worthiness or doctrine-reuse claims
  - deploy/publication, archive/delete, `.env`, secret, or protected-surface exception arguments
  - transcript memory, uploaded transcript residue, or non-durable operator recall
- routing must fail closed if:
  - `selection_status` is not `pilot_selected`
  - `selection_reasons` is not empty
  - `selected_pilot` is missing or not explicit
  - the routing argument depends on hidden discovery or any other forbidden evidence class
  - the routing argument tries to admit owner-repo mutation, worker launch, execution-home selection, or support widening directly from the selected-pilot card

### `routing_output_boundary`

- output is still contract-only at this stage
- no owner repo is selected now beyond preserving the already carried candidate identity inside `selected_pilot`
- no owner-surface home is admitted now
- no execution home is selected now
- no `_stack` command or runtime surface is admitted now
- no Playbook doctrine export is admitted now
- no deploy/publication behavior is admitted now
- no owner-repo mutation is admitted now
- the routing contract may define:
  - admissible implementation-routing input
  - admitted evidence classes
  - forbidden evidence classes
  - fail-closed routing rejection classes
  - the exact boundary between explicit `selected_pilot` and later owner-surface admission
- it may not define:
  - `_stack` command runtime
  - owner-repo mutation
  - branch or worktree discovery
  - automatic implementation routing by hidden scoring
  - later execution-home choice
  - Playbook export or deploy/publication semantics

### `owner_boundary`

- ATLAS root owns this routing contract, the lane-level routing discipline, and the restart consequence of whether implementation routing is explicit yet
- owner repos remain the later owners of repo-local implementation and verification truth only after owner-surface admission and later implementation admission are separately frozen
- `_stack` remains a later owner only after routing and owner-facing surfaces create one separately admitted execution-home question
- Playbook remains a later consumer only after this exact routing seam proves reusable beyond the current lane state

## Exact Marker Decision

- `none`

Why:

- pass 487 freezes the control-plane routing contract only
- operator instructions for this batch explicitly keep the marker at `60%` unless executed state, proof-backed adoption, restart breadth, or blocker class materially changes
- current truth gets sharper here, but the pass does not itself widen owner-side execution or clear a new blocker class beyond the already-selected next seam

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot selected-pilot implementation-routing owner-surface admission pass 488`

Why:

- the implementation-routing contract is now exact
- the next honest question is which surface owns the producing and consuming home for that routing seam before support, concrete implementation admission, or `_stack` execution-home doctrine is discussed
- selecting owner-repo mutation, worker launch, or execution-home behavior would still be premature before that owner-facing boundary is frozen

## Rule

`Selected-Pilot Routing Contract Before Owner Admission`

When one explicit `selected_pilot` card is already directly proved, freeze the exact implementation-routing contract before owner-surface admission, owner-repo mutation authority, `_stack` execution-home doctrine, Playbook export, or deploy/publication widening.

## Failure Mode

`Implementation Routing By Selected-Pilot Adjacency`

This lane becomes synthetic when one explicit `selected_pilot` card is treated as an obvious owner-side implementation route before the routing contract makes the admissible input, evidence classes, fail-closed rejection rules, and later owner/execution boundaries explicit.
