# AI Long-Run Batch Orchestration Single Supervised Pilot Selected-Pilot Owner-Repo Implementation Prompt-Pack And Handoff Contract Pass 498 - 2026-06-20

- Date: `2026-06-20`
- Lane: `AI Long-Run Batch Orchestration`
- Mode: `docs-only root-bounded prompt-pack and handoff contract`
- Scope: `freeze the exact worker objective, proof obligations, allowed-touch boundary, and stop conditions for the already-admitted fail-closed selected-pilot owner-repo implementation slice`
- Source surfaces:
  - `docs/memory/profiles/zachariah_workflow_profile.md`
  - `docs/atlas-book/01-current-state.md`
  - `docs/atlas-book/05-receipt-index.md`
  - `docs/atlas-book/12-restart-and-handoff-guide.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-2026-05-22.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-OWNER-REPO-IMPLEMENTATION-CONTRACT-FREEZE-PASS-494-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-OWNER-REPO-IMPLEMENTATION-OWNER-SURFACE-ADMISSION-PASS-495-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-OWNER-REPO-IMPLEMENTATION-SUPPORTING-LANE-ADMISSION-PASS-496-2026-06-20.md`
  - `docs/ops/AI-LONG-RUN-BATCH-ORCHESTRATION-SINGLE-SUPERVISED-PILOT-SELECTED-PILOT-OWNER-REPO-IMPLEMENTATION-FIRST-IMPLEMENTATION-ADMISSION-PASS-497-2026-06-20.md`
- Control-plane checkpoint: `main`

## Objective

Freeze one compact authoritative prompt-pack and handoff contract for the already-admitted first implementation slice of the root-owned `single supervised pilot selected-pilot owner-repo implementation` family.

This pass does not:

- implement code
- choose one concrete owner-side mutation packet, repo edit, branch, worktree, or execution home by adjacency
- reopen implementation-routing redesign, selector redesign, or queue-or-registry doctrine
- infer `_stack` execution-home ownership, owner-repo mutation authority, or Playbook doctrine export posture
- reopen deploy/publication, archive/delete, `.env`, or secret work
- widen into manifest, restart-surface, runtime-state, or broader orchestration redesign

## Root Health Baseline

- pass 494 already froze the exact selected-pilot owner-repo implementation contract
- pass 495 already admitted ATLAS root as the owner-facing home for that implementation seam
- pass 496 already proved separate support still honestly holds at `none yet`
- pass 497 already froze the exact first implementation slice and exact proof matrix around the admitted `selection_status`, `selection_reasons`, `routing_status`, `implementation_route`, `routing_reasons`, `implementation_status`, `owner_repo_implementation`, and `implementation_reasons` surfaces
- root validation is currently clean at `critical=0 error=0 warning=11 info=0`

## Inherited Contract Spine

The future worker must inherit exactly:

- pass 494 exact selected-pilot owner-repo implementation contract
- pass 495 root control-plane owner admission
- pass 496 supporting-lane hold at `none yet`
- pass 497 exact first implementation slice and exact proof matrix

No worker may reinterpret those seams as suggestions.
They are contract inputs.

## Exact Worker Objective

The next worker is allowed to pursue one exact implementation objective only:

- implement one root-local fail-closed selected-pilot owner-repo implementation helper in `ops/atlas/pilot_selected_owner_repo_implementation.py` plus one direct proof file in `tests/test_atlas_pilot_selected_owner_repo_implementation.py` so the helper preserves only `selection_status`, `selection_reasons`, `routing_status`, `implementation_route`, `routing_reasons`, `implementation_status`, `owner_repo_implementation`, and `implementation_reasons`, converts only an explicit `implementation_route_admissible` routing result with one explicit `implementation_route` card and empty routing reasons into the exact explicit owner-repo implementation card already present in the bundle, emits only `owner_repo_implementation_admissible` or `no_owner_repo_implementation`, reports only the admitted `implementation_reasons`, fails closed on hidden or widened implementation inputs, and proves behavior against the frozen pass-497 matrix

The worker is not allowed to pursue:

- live repo discovery
- branch or worktree enumeration from live repos
- owner-repo mutation authority, owner-repo edits, or owner-readiness inference
- `_stack` execution-home inference or command/runtime design
- Playbook doctrine export
- broader selector, manifest, restart-surface, runtime-inventory, or queue-or-registry redesign

## Exact Preserved Implementation Surface

The worker must preserve exactly these surfaces:

- `selection_status`
- `selection_reasons`
- `routing_status`
- `implementation_route`
- `routing_reasons`
- `implementation_status`
- `owner_repo_implementation`
- `implementation_reasons`

The worker may render these surfaces only.
The worker may not widen them into live repo inventory, branch inventory, worktree inventory, owner-repo mutation authority, owner-readiness doctrine, `_stack` execution-home choice, Playbook doctrine export, deploy/publication semantics, or protected-surface exceptions.

## Exact Mandatory Proof Cases

The worker must satisfy exactly these proof cases:

1. explicit `implementation_route_admissible` result with empty routing reasons
   - preserve `implementation_status` as `owner_repo_implementation_admissible`
   - preserve `owner_repo_implementation` as the explicit `implementation_route` card
   - preserve `implementation_reasons` as `[]`

2. `no_route` routing status
   - preserve `implementation_status` as `no_owner_repo_implementation`
   - preserve `owner_repo_implementation` as `null`
   - preserve `routing_status_not_implementation_route_admissible`

3. non-empty routing reasons despite `implementation_route_admissible`
   - preserve `implementation_status` as `no_owner_repo_implementation`
   - preserve `owner_repo_implementation` as `null`
   - preserve `routing_reasons_present`

4. missing or non-explicit `implementation_route`
   - preserve `implementation_status` as `no_owner_repo_implementation`
   - preserve either `implementation_route_missing` or `implementation_route_not_explicit`

5. implementation argument invents live repo discovery, branch/worktree enumeration, `_stack` execution-home inference, owner-repo mutation authority, or Playbook doctrine export
   - preserve `implementation_status` as `no_owner_repo_implementation`
   - preserve the exact corresponding implementation reason

6. preserved `implementation_route` violates the admitted protected-surface boundary
   - preserve `implementation_status` as `no_owner_repo_implementation`
   - preserve `protected_surface_violation`

## Exact No-Mutation / No-Discovery / No-Owner-Implementation-By-Adjacency Boundary

The worker must carry this wording forward verbatim:

`No-mutation, no-discovery, and no-owner-implementation-by-adjacency guard: this packet may implement one explicit fail-closed selected-pilot owner-repo implementation helper plus direct proof for the already-admitted selection_status, selection_reasons, routing_status, implementation_route, routing_reasons, implementation_status, owner_repo_implementation, and implementation_reasons surfaces, but it may not discover live repos beyond the explicit preserved implementation_route card, enumerate worktrees or branches, infer owner-repo mutation authority, owner readiness, Playbook doctrine export, or _stack execution-home ownership, widen one implementation-admissible card into owner-repo mutation or _stack execution-home choice, mutate queue, registry, runtime, session, merge, manifest, archive, repair, blocker, or owner-repo state, or widen into deploy, publication, .env, secret, or protected-surface work.`

## Exact No Hidden Transcript-State Inference Boundary

The worker must carry this rule forward:

- use only the cited durable surfaces in this receipt chain plus the new owner-repo implementation helper/test files
- do not infer implementation truth, owner readiness, execution-home meaning, or protected-surface exceptions from uncited transcript memory or broad root residue

## Exact Allowed Future Implementation Surfaces

The worker may touch only:

- `ops/atlas/pilot_selected_owner_repo_implementation.py`
- `tests/test_atlas_pilot_selected_owner_repo_implementation.py`

## Exact Forbidden Future Implementation Surfaces

The worker must not touch:

- `ops/atlas/marker_knockout_selector.py`
- `ops/atlas/continuity.py`
- `ops/atlas/pilot_selection_criteria.py`
- `ops/atlas/pilot_candidate_comparison.py`
- `ops/atlas/pilot_winner_conversion.py`
- `ops/atlas/pilot_winner_selection.py`
- `ops/atlas/pilot_selected_implementation_routing.py`
- `tests/test_atlas_pilot_selection_criteria.py`
- `tests/test_atlas_pilot_candidate_comparison.py`
- `tests/test_atlas_pilot_winner_conversion.py`
- `tests/test_atlas_pilot_winner_selection.py`
- `tests/test_atlas_pilot_selected_implementation_routing.py`
- `docs/atlas-book/*`
- `docs/memory/initiatives/*`
- owner repos under `repos/*`
- `_stack` helper-runtime or command-design surfaces
- `archive/`, `.vercel`, `.env*`, `secrets/`, screenshots, captures, `.playwright-mcp/`, or broad untracked root backlog

## Exact Stop Conditions

The worker must stop and return immediately if the slice requires:

- real repo discovery or implementation routing beyond the explicit preserved `implementation_route` card
- worktree or branch enumeration from live repos
- owner-repo mutation authority, owner-repo edits, Playbook doctrine export, or `_stack` execution-home inference
- deploy/publication, `.env`, secret, archive/delete, or protected-surface touch
- new implementation-status values, new implementation-reason values, or widened routing/implementation fields beyond the admitted set
- selector, manifest, restart-surface, or runtime-state edits to make the helper work

If one of those triggers appears, the work is no longer valid first-slice implementation work.

## Exact Handoff Prompt-Pack Spine

Any future worker prompt-pack must include:

1. the exact implementation objective
2. the inherited passes 494 through 497 as frozen inputs
3. the exact preserved implementation surface
4. the exact proof matrix
5. the exact no-mutation, no-discovery, and no-owner-implementation-by-adjacency guard verbatim
6. the exact no-hidden-transcript-state boundary
7. the exact allowed-touch and forbidden-touch surfaces
8. the exact stop-and-return conditions

Mirror surfaces may restate this spine.
They may not redefine it.

## Exact Next Package

- `AI Long-Run Batch Orchestration single supervised pilot selected-pilot owner-repo implementation implementation-readiness closeout and worker-routing pass 499`

## Marker Decision

- `none`

Why:

- this pass narrows the worker handoff contract only
- no new executed state, broader proof-backed adoption, or blocker clearance landed

## Rule

Freeze the worker handoff contract before authorizing any first-slice implementation work for the root-owned selected-pilot owner-repo implementation family.

## Failure Mode

`Selected-Pilot-Owner-Repo-Implementation Handoff Drift`

This family becomes dishonest when the worker handoff contract stays implicit and the first implementation slice expands through prompt wording into owner-repo mutation, live repo discovery, Playbook doctrine export, `_stack` execution-home inference, or protected-surface exceptions that the durable chain has not admitted.
