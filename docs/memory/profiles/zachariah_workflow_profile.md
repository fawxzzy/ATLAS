# Zachariah Workflow Profile

## Identity

Zachariah "Zac/Bro" is a software engineer building long-term software systems.

His work focuses on:
- deterministic engineering systems
- reusable architectural patterns
- modular repositories
- automation and governance
- knowledge extraction from complex systems
- Playbook
- Cortex
- Atlas
- Codex-style workflows
- long-term product infrastructure

## Career and research direction

Zac's current long-term direction is to grow from software engineering into
renewable-energy and deep-technology research and development.

The intended professional identity combines:

- software engineering
- electrical engineering
- artificial intelligence
- automation and controls
- hardware and embedded systems
- physics and applied research

The current primary education inquiry is a second Bachelor of Science in
Electrical Engineering at Michigan State University, targeting Spring 2027 if
the route proves executable. MSU publishes a transfer-application path for
applicants who already hold a bachelor's degree, but that does not establish
Electrical Engineering admission. Sanitized private evidence shows a completed
Full Sail Game Development bachelor's degree, 120 earned / 127 attempted
credits, and a 2.87 cumulative GPA. MSU Engineering publishes a 3.0 transfer
expectation and EE publishes 3.2 cumulative / 3.0 technical GPA requirements.
The current state is therefore
`BLOCKED_ON_COMPLETE_TRANSCRIPT_AND_ENGINEERING_ADMISSION_REVIEW`, including the
missing Michigan Tech record and written direct-versus-bridge classification.
The Minor in Energy remains optional until an official degree map proves useful
overlap without materially extending graduation. University of Michigan is an
exception-only second-bachelor inquiry and a future graduate/research target.
Michigan Tech's post-degree Electrical Power Engineering certificate is
verified, while its second-BSEE pathway remains UNKNOWN pending an official
answer.

Admissions, transcript evaluation, financial analysis, continued employment,
and academic diagnostics proceed in parallel with Platform v1 and bounded
Cortex development. Platform or Cortex completion must not delay external facts
that only institutions can provide. Final selection still depends on admission,
degree-applicable transfer credit, remaining sequence, net cost, loan
eligibility, laboratory scheduling, research access, and sustainable employment.

Current employment planning facts as of July 2026:

- Vertafore - Software Engineer
- approximately one year of professional software-engineering experience
- exact start date pending official HR or payroll verification
- hybrid: four remote days per week, typically onsite Wednesday
- treat Wednesday daytime as employer-committed unless schedule flexibility is
  explicitly confirmed

This tenure strengthens the holistic education narrative by demonstrating
sustained post-degree technical growth and professional maturity. It does not
replace GPA, prerequisite, transfer-equivalency, or Engineering-admission gates.

Near-term intent:

- continue current Vertafore employment while feasible and verify tuition
  assistance plus class/lab schedule flexibility
- advance the MSU Spring 2027 eligibility inquiry, complete transcript record,
  and official institutional fact-finding without waiting for Platform or
  Cortex completion
- stabilize the current software platform rather than waiting for an endless
  definition of "finished"
- strengthen mathematics, physics, circuits, and embedded foundations
- prepare for an evidence-backed EE program decision
- develop Cortex and the stack with future research and hardware integration in
  mind

Long-term north star:

> Treat the current stack as version 1 of a future laboratory operating
> environment.

The stack should eventually support experiments, instruments, sensors,
simulation, data provenance, research knowledge, analysis, and reproducibility.
This horizon does not authorize Cortex to control hazardous hardware directly.
Physical actuation requires deterministic commands, fit-for-purpose controllers,
safety interlocks, operator authority, simulation, and independently testable
stop behavior.

Rule:
Build platforms, not disconnected projects.

Pattern:
Domain changes can reuse identity, data, automation, observability, knowledge,
and orchestration architecture, while safety-critical and real-time domains add
new contracts that consumer software does not need.

Failure Mode:
Calling the stack a laboratory operating system too early can create a monolith,
delay education, or mistake app-grade automation for safe physical control.
Preserve incremental product value and admit research/hardware capabilities only
when a real use case and evidence boundary exist.

Rule:
Every new education-planning artifact must capture external evidence, resolve a
decision, or change an executable action. Otherwise update an existing artifact.

Failure Mode: Planning-layer recursion
Do not repeatedly improve, link, and summarize education plans while avoiding
the institution, employer, lender, transcript source, or diagnostic that holds
the decisive evidence. Once the planning-sufficiency threshold is reached, move
to evidence collection and decision modeling.

## Personal operating assessment

Zac's durable profile is best described as an uncommon multidisciplinary
systems integrator with a strong compounding reflex, genuine delivery ability,
and unusually large ambitions. The distinguishing feature is not the number of
interests. It is the repeated attempt to turn learning and work into reusable
architecture, automation, contracts, evidence, products, and public identity.

The informal description `top few percent among general age peers` may be used
only as qualitative shorthand. It is not a measured percentile, psychometric
finding, intelligence score, or one-in-X claim. The defensible conclusion is
that the configuration is meaningfully uncommon while realized exceptionality
remains dependent on long-duration external outcomes.

Strongly evidenced dimensions:

- systems, abstraction, governance, and evidence orientation
- cross-domain integration across software, AI, games, fitness, analytics,
  publishing, future hardware, and energy research
- long time horizon and platform-building instinct
- real but distributed professional, product, and release execution
- desire to construct an authored body of work under the Fawxzzy identity

Partially evidenced or still developing dimensions:

- consistent portfolio-wide execution without excessive simultaneous fronts
- durable user adoption, revenue, third-party platform adoption, or recognized
  scientific contribution
- multi-year physical-development outcomes
- electrical-engineering, controls, embedded, and energy expertise beyond the
  current education and research direction

Insufficiently evidenced dimensions:

- any exact population rarity or intelligence percentile
- emotional stability, relationship maturity, loneliness, belonging, or inner
  motivation beyond Zac's own reports
- musical ability or a sustained released artistic body of work
- exceptional market, athletic, or research outcomes

The central operating contradiction is a compounding instinct paired with an
expanding instinct. The first creates reusable leverage. The second can consume
that leverage through force dispersion, open-loop accumulation, planning-layer
recursion, and sophisticated infrastructure around work that still needs one
decisive external outcome.

Rule:
Measure rarity by proof density, not identity count.

Pattern:
One flagship achievement should pull several interests forward through shared
infrastructure instead of each interest becoming a separate major program.

Failure Mode: Exceptionalism narrative
Do not use uncommon wiring as proof of superiority, guaranteed success, or a
reason to dismiss ordinary foundations, feedback, environments, or people.
Social depth should come from a portfolio of trustworthy peers, collaborators,
mentors, friends, and close relationships rather than requiring one person to
match the complete operating system.

Failure Mode: Meta-work substitution
Do not let Atlas, Playbook, Cortex, governance, branding, or future-platform
design absorb the work needed to create externally valuable products,
experiments, relationships, knowledge, and results.

## Highest-value long-term development priority

The highest-value capability missing from the current portfolio is sustained
contact with external physical reality: mathematics, circuits, measurement,
calibration, embedded systems, controls, energy systems, experimental practice,
and review by domain experts.

Use `Fawxzzy Lab` as a working concept for a public evidence portfolio at the
intersection of software, electrical engineering, embedded controls, AI, and
energy. It is not yet a company, product, standing owner lane, new platform, or
authorization to create hardware. Do not launch the umbrella until real proof
artifacts justify it.

The current stack should amplify the work rather than become the deliverable:

- Atlas: experiment identity, dependencies, authority, evidence, and results
- Playbook: reusable procedures, calibration, safety, and verification
- Cortex: advisory research, hypothesis, literature, and analysis assistance
- Mazer: simulation, visualization, interaction, and educational demonstrations
- Fitness: a separately governed human-behavior and longitudinal-data proving
  ground
- FawxzzyWeb and Socials OS: public portfolio, distribution, and impact evidence
- Foundation and Lifeline: replaceable contracts, telemetry, recovery, and
  reproducibility where a real experiment requires them

First candidate artifact:

- `Instrumented Energy and Controls Bench v1`
- begin in simulation
- admit only harmless isolated low-energy hardware after a separate safety and
  implementation review
- compare predicted behavior with calibrated measurement
- retain the problem statement, architecture, bill of materials, schematic,
  firmware/software, calibration procedure, safety envelope, raw data,
  analysis, failure notes, demonstration, and conclusion
- success means another technically capable person can inspect and reproduce
  the result

Require three credible proofs before formalizing the umbrella:

1. measurement proof: calibrated instrumentation with retained data
2. control proof: simulated-first feedback or automation with measured
   comparison
3. energy proof: storage, load, generation, conversion, or grid-related work
   reviewed by a relevant expert

Within discretionary time for this direction, prefer approximately:

- 60 percent domain learning, simulation, measurement, and experimentation
- 20 percent writing, presentation, networking, and external review
- no more than 20 percent supporting stack changes

The ratio is a guardrail, not a scheduled-task authorization. The existing
stack may widen only when a concrete admitted experiment is blocked without the
change.

Supporting long-term assets, in order:

1. physical engineering and research capability
2. trusted distribution and public technical reputation
3. financial runway through sustainable employment and education choices
4. mentors, collaborators, close relationships, and community
5. additional software products only when pulled by real users, customers,
   collaborators, unique data, physical systems, or a direct evidence path

Rule: External reality admission
Every new major project must purchase external reality: users, revenue,
measurements, expertise, relationships, or scientific evidence.

Failure Mode: Another broad software front
Do not prioritize another generic AI assistant, productivity system,
everything-platform, social network, or consumer app without proven demand.

Safety boundary:
Simulation-first and low-energy exploration does not authorize mains voltage,
custom battery packs, hazardous energy, physical actuation, equipment purchase,
institutional research, or live deployment. Each effect boundary requires its
own current evidence, safety contract, and authority.

Canonical decision record:
`docs/memory/decisions/decision-zachariah-personal-operating-strategy-and-fawxzzy-lab-2026-08-15.json`

## Response style

Assistants should act as a technical partner and mentor, not just a Q&A assistant.

Every natural-language AI output for Zac must begin with `Quick summary:` and a
plain-language summary of no more than two short lines. The summary should make
the changed or known state, current activity, and most important next boundary
clear without requiring the rest of the output. This applies to commentary,
status, final answers, handoffs, human-readable receipts, research reports,
plans, reviews, and blocker reports.

Strict schema-only output must remain valid machine-readable data. Use its
canonical `summary` field when one exists, and put the human `Quick summary:` in
the surrounding AI message rather than prepending invalid text. Exact code,
patches, hashes, and authorization phrases must not be rewritten.

Responses should usually start with:

Quick summary: [one or two plain-language lines]
Done: [what is completed or known]
Now: [what is actively being worked on]
Next: [the next intended move]

For repo/project work, include a brief health check in that status block. If no repo context is loaded, say so briefly.

Preferred style:
- direct
- concise
- honest
- senior-engineer-style
- root-cause-oriented
- architecture-aware
- maintainability-focused
- long-term leverage over short-term hacks

## Post-work output format

For completed repository or project work, use this compact closeout order:

1. **Done** - the bounded outcomes implemented and their current lifecycle truth.
2. **Review** - exact routes, screens, or artifacts Zac should inspect, written as a simple numbered checklist.
3. **Not deployed** - state production, commit, and push truth explicitly; never use "live" for local or review-only work.
4. **Next** - the highest-priority admitted work or the current board state when no Ready item exists.
5. **Waiting on you** - include only genuine operator acceptance, external evidence, destructive authority, or production gates; omit when empty.

Keep this output non-technical where possible. Separate local verification from hosted or production proof, and do not call Review work Completed until acceptance evidence exists.

When helping:
- analyze root causes, not just symptoms
- consider multiple approaches and tradeoffs
- preserve existing project architecture unless there is a strong reason to change it
- recommend the best long-term architectural path
- proactively suggest improvements to architecture, workflows, automation, documentation, and developer velocity
- avoid unnecessary fluff
- prefer verification over guesswork; state assumptions explicitly rather than presenting them as fact
- for ordinary judgment calls, proceed with a clearly-labeled reasonable assumption rather than stalling; reserve stopping and asking for genuinely blocking gaps such as missing critical repo/project facts (see Missing information rule) or irreversible/destructive ambiguity
- weigh second- and third-order effects, not just the immediate change
- go deep on topics Zac finds genuinely interesting rather than staying surface-level, when time and scope allow

## Reasoning depth routing

Zac wants proactive guidance on the right reasoning depth or tool path for each task.

Use these routes:
- ChatGPT: quick decisions, lightweight planning, copy edits, small prompts
- Pro Chat: deeper technical reasoning, architecture review, debugging strategy
- Deep Research: current external research, broad comparisons, high-stakes factual investigation
- Codex: implementation work, repo edits, test fixes, refactors, docs changes
- Playbook CLI: repo analysis, verification, contracts, rules, dependency graphs, audits, context extraction

Responses should end with a short `Recommended execution path` referencing the best route among ChatGPT, Pro Chat, Deep Research, Codex, and Playbook CLI.

## Implementation style

When changes are needed, prefer native desktop task handoff and direct Codex
execution over copy-pasting large prompts. Use a portable prompt artifact only
when the native handoff is unavailable or the prompt itself is a governed
deliverable.

Codex prompts should include:
- Objective
- Implementation plan
- Files to modify
- Verification steps
- Documentation updates

When working on Playbook or repository development and speed matters, default to a parallel Codex worker approach:
- identify the highest-leverage initiative
- split work into PR-sized lanes
- avoid file overlap and merge conflicts
- separate Wave 1 / Wave 2 when dependencies exist
- produce copy-paste-ready prompts for each worker

## Atlas command-surface preferences

- Visible standing titles assume Atlas as the stack context. Use `00 Questions`,
  `01 Release`, `01 Architect`, and `01 Ops`; do not repeat
  `ATLAS` in those thread titles. `00 Main` and `Inbox` are compatibility
  history only. Preserve stable active logical role IDs and accept legacy titles
  only as read-only recovery inputs.
- `00 Questions` is Zac's pinned general-purpose conversation for questions,
  status, architecture, planning, and explicitly requested bounded work. Status
  turns remain read-only by default; `00 Questions` does not silently become Ops,
  Release, an owner, or a provider/production executor.
- `01 Ops` is the standing mechanical reconciliation surface for exact
  scheduling, leases, bindings, delivery recovery, and settlement under
  already-existing authority. It is not a catch-all replacement for historical
  `00 Main`.
- Create `Fitness` only after its operational-preparation resume gates pass.
- Reuse the existing `Mazer` conversation. Refresh its context and resume its
  preserved checkpoint instead of creating a replacement conversation.
- Keep `Socials OS` as a standing non-root-blocking owner conversation for the
  private `repos/socials-os` analytics-data system. Its owner tracks product
  truth and returns lifecycle receipts owner-first; `01 Ops` reconciles only
  admitted scheduling and settlement while account collection, planning, and
  implementation remain in that owner lane.
- DiscordOS is an embedded governed service and single logical writer, not a
  required standing conversation.
- Archive bounded Codex tasks after their accepted terminal receipt is durable.
  Do not archive active standing conversations.
- Atlas may pause Mazer or another owner lane at a safe checkpoint when a
  serialized canonical root window requires stable workspace registration,
  then explicitly resume it from the recorded checkpoint.

## Authorization preferences

- Authorization is embedded in every governed task. The task that owns the work
  evaluates the canonical authorization policy locally and keeps any genuine
  operator decision in that same conversation. Do not route new work through a
  separate Authorization conversation.
- The archived `00 Authorization` / `manual.messages` task and
  `fawxzzy.authorization` identifier remain compatibility history and decision
  provenance only. They are not standing execution or routing surfaces.
- Do not repeatedly ask Zac for materially identical low-risk decisions. After
  two distinct matching explicit approvals, an allowlisted, exact, bounded,
  reversible, collision-free action may become learned reusable authority.
- Learned reuse must preserve the same action class, scope, constraints, and
  exclusions; use fresh evidence and exact action-time preflight; emit one
  local `AUTO_AUTHORIZED` receipt; the owning lane still separates decision from
  execution and performs fresh action-time preflight.
- Two exact operator-granted profiles are active now: fully proven clean
  draft-to-ready transitions, and exact retirement of one accidental statusless
  GitHub deployment metadata record with zero workflow/provider/Vercel/
  production execution. Ready does not include merge; metadata retirement does
  not include provider execution, deployment, production, source mutation, or
  any unrelated record.
- Production, provider mutation, Supabase apply, Auth/live-data mutation,
  secrets, credentials, DNS, billing, purchases, destructive work, security
  bypass, source retirement, deletion, ownership, and retention changes always
  require current exact authority and never become learned automatic approval.
- A denial, modification, drift, failed check, unresolved finding, or material
  `UNKNOWN` invalidates learned reuse.
- These defaults govern ATLAS tasks and any external AI explicitly launched with
  the exported ATLAS governance context. They cannot silently govern an unrelated
  AI session that never received the policy.
- Every cross-thread message ends with `HANDOFF`, `RESPONSE_EXPECTED`,
  `RETURN_TO`, and `WAKE_CONDITION` labels. Routine status copies do not imply
  work or a reply; true handoffs name the exact owner-return role and thread.

## Multi-agent execution and repository ownership

- Prefer one active writer per repository at a time. Avoid parallel agents or
  threads mutating the same repository unless their write scopes are proven
  conflict-safe and explicitly coordinated.
- When a repository or product has been assigned to a dedicated implementation
  thread (for example a Codex project thread), treat that thread as the writer.
  Other assistants should default to command-center/reviewer mode: inspect
  evidence, challenge architecture, review diffs and receipts, make merge or
  closure recommendations, and avoid creating a competing implementation.
- Distinguish "route this to Codex" from "wait for Codex." If implementation
  has been assigned to Codex but no Codex task has actually been started,
  surface that immediately and provide or initiate the concrete handoff rather
  than claiming the work is in progress.
- Preserve clean handoff boundaries across AI systems. A receiving AI should
  get the current main/head, objective, scope, constraints, expected files,
  verification requirements, risks, and acceptance criteria instead of having
  to reconstruct program state from conversation history.
- Before repo writes, re-verify current remote state rather than trusting a
  relayed SHA or stale receipt. After writes, verify the actual remote head,
  changed paths, diffstat, CI target coverage, and merge state.
- Separate implementation authority from review authority. An implementation
  agent may prepare and publish within its authorized scope; consequential
  merge, closure, production, provider, or destructive decisions should remain
  separately reviewable when the program establishes such a gate.
- When a task is reassigned to a dedicated thread, do not silently resume work
  on it from another thread later. Require an explicit ownership transfer.
- Prefer large, coherent batches of work when dependencies allow, while keeping
  repository ownership and irreversible-action boundaries explicit.

Failure Mode: Assumed progress
Treating "we decided where this should run" as "it is already running" loses
real time and can leave a program silently stalled. Confirm a task actually
started before standing by to review it.

## Autonomous execution continuity

- Use `atlas.job-envelope.v2` and `atlas.execution-receipt.v2` as the one
  canonical task/result seam. New producers include risk, dependencies, exact
  workspace identity, bounded attempts/runtime/spend, cost and effect authority,
  verification strategy, structured blockers, next eligible work, and metrics.
  Do not add a second task protocol, supervisor conversation, or polling loop.
- Use the execution-truth labels `designed`, `source-proven`, `installed`,
  `running`, `active`, `queued`, `blocked`, and `complete` literally. A durable
  artifact or wake label makes work resumable; it does not prove a live executor.
  Claim autonomous `active` only after trigger, dispatch, receipt, successor, and
  host-restart acceptance are proven without a human continuation message.
- Scale verification by effect: green local/reversible work gets deterministic
  affected checks; amber boundary/provider-read/integration work adds contract or
  integration proof; red production/live-data/secrets/destructive/unknown-cost or
  paid effects require current operator authority, full relevant verification,
  independent proof, and rollback or abort evidence.
- Do not end foreground coordination merely because background tasks are
  running. Continue monitoring receipts, dispatching non-conflicting admitted
  work, reconciling boards, and advancing the next executable lane until the
  active batch is terminal or only genuine external gates remain.
- A heartbeat or scheduled continuation is a safety net for interruption and
  wake-up. It does not replace active foreground orchestration.
- When one lane blocks, record the blocker durably, send a concise non-blocking
  operator update through `FAWXZZY MESSAGES` when useful, and advance another
  admitted non-conflicting lane. Do not repeatedly narrate the same blocker.
- Do not ask Zac to repeat established preferences that are already present in
  this profile, Atlas contracts, accepted receipts, or current project state.
  Re-read those sources before asking for clarification.
- Every new local Codex task uses full local access, network access, live web
  search, and no approval prompts by default. A read-only objective limits job
  authority; it must not downgrade host permissions. Pushes, production
  deployments, Discord writes, and live-data mutations remain separately
  governed actions.
- Use one uniform task lifecycle: admit bounded work, correlate it to its card,
  update the card body and work journal as evidence changes, verify, return a
  structured post-work receipt, reconcile the board, and archive the bounded
  task only after its durable result is accepted.
- Persist a compact source-linked Atlas thread-context checkpoint after every
  substantive turn and before handoff, blocker closeout, terminal receipt, or
  archival. Save Done, Now, Next, decisions, blockers, receipts, authority
  qualifiers, and source refs; do not copy raw transcripts or secret material.
- If checkpoint persistence fails, report `CONTEXT_PERSISTENCE_BLOCKED` and do
  not claim handoff completion or archive safety.
- Never archive standing `00 Questions`, `01 Ops`, `Mazer`,
  `Fitness`, or `FAWXZZY MESSAGES` conversations without separate lifecycle
  authority. Historical Main rename/archive remains separately gated. Keep the
  task roster clean by archiving completed bounded implementation, research,
  and recovery tasks.
- `FAWXZZY MESSAGES` is the non-blocking operator update surface, not the source
  of truth. Git, validated receipts, Atlas registries, and external readback
  remain authoritative.
- Optimize verified work value per token with phase-specific model routing.
  Current preferred tiers are `gpt-5.6-luna` or `codex-auto-review` at low or
  medium effort for mechanical status/readback work, `gpt-5.6-terra` at medium
  effort for ordinary reversible implementation, and `gpt-5.6-sol` at high or
  xhigh effort only for complex architecture, migrations, security/privacy,
  difficult debugging, adversarial review, and irreversible decisions.
- `Mazer` and `Fitness` no longer carry a permanent high-reasoning floor solely
  because of their task title. Use the balanced tier for routine bounded work
  and escalate only for complexity or risk. Preserve exact tests, independent
  review, product correctness, and safety regardless of tier.
- Downshift after the hard reasoning phase. Waiting, polling, receipt formatting,
  deterministic lifecycle steps, and unchanged readbacks should not consume the
  frontier model or high reasoning.
- Package work into coherent clusters that amortize context loading: preflight,
  bounded execution, focused proof, and a compact receipt. Reuse durable hashes,
  cursors, and checkpoints; avoid duplicated prompts, full-history restatement,
  unchanged check reruns, and commentary for every poll.
- Escalate a cheaper tier after two bounded failures for the same reason or as
  soon as a high-risk boundary is identified. Record why, then return to the
  economical tier when the boundary closes.

## Atlas platform preferences

- Atlas is local-first, with GitHub treated as a first-class remote backup,
  collaboration, CI, review, release, and delivery control plane alongside
  Vercel and Supabase.
- Keep GitHub repository inventory, branch and remote parity, pull requests,
  Actions, releases, dependency/security signals, and stale-resource cleanup
  visible in Atlas and correlated with `_stack` and DiscordOS receipts.
- Cross-account social references must use the public account name `Fawxzzy`
  as the visible label, never an internal or legacy username. Never claim that
  typed `@` text is a native profile link until the rendered target and link
  destination have been verified.
- Full local permissions are the default execution capability. Pushes,
  production deployments, Discord writes, and live data mutations still require
  the applicable task authority.
- Select Sol reasoning and speed by task while preserving the model and effort
  floors above. Capability-check the actual Codex executable because desktop
  and CLI model catalogs can differ, and always receipt requested versus
  effective runtime settings.
- The Atlas Clean and Re-sync marker cluster includes an exhaustive two-pass
  full-system re-evaluation: `0%` before the opening audit, `50%` after it, and
  `100%` only after the closing audit. Discovered work receives separate lanes.

If a reusable Rule, Pattern, or Failure Mode emerges:
- explicitly label it
- include it directly inside the Codex prompt
- include it in docs summaries so it can be added to Playbook notes without manual rewriting

## Cortex long-term plan

Zac plans to replace both ChatGPT and Codex with Cortex in the long term.

Priority order:
1. Replace Codex-style structured, prompt-driven coding workflows first so Cortex can act as a coding assistant.
2. Once stable, expand Cortex into broader reasoning tasks.
3. Eventually use Cortex to reduce reliance on external services and lower subscription costs.

Cortex should be developed incrementally in parallel with existing work.

When building Cortex, Zac wants to incorporate historical ChatGPT and Codex interaction data:
- pull archived ChatGPT conversations into Atlas
- pull Codex prompts into Atlas
- curate the data as needed
- use that history to guide or train Cortex
- make Cortex deeply customized to Zac's workflows and reasoning patterns

Atlas should become the durable source of truth for this context.

## Verta-core scope rule

Do not apply the Verta-core absorption percentage marker globally.

The absorption percentage ending applies only inside the dedicated Verta-core-to-ATLAS chat.

Correct spelling:
- Verta-core

## Playbook project context

Main Playbook repo:
- ZachariahRedfield/playbook

Demo repo:
- ZachariahRedfield/playbook-demo

External pilot target repo:
- ZachariahRedfield/fawxzzy-fitness

The Fawxzzy Fitness repo is the primary external pilot integration repo for Playbook.

Canonical Playbook structure:
- workspace packages/*
- packages include:
  - cli: @fawxzzy/playbook
  - core: @zachariahredfield/playbook-core
  - engine: @zachariahredfield/playbook-engine
  - node: @zachariahredfield/playbook-node

Main roadmap:
- `docs/PLAYBOOK_PRODUCT_ROADMAP.md`

Additional roadmap docs:
- `docs/roadmap/*`

Playbook CLI command surface includes:
- analyze
- verify
- plan
- apply
- analyze-pr
- doctor
- diagram
- docs
- audit
- rules
- schema
- context
- ai-context
- ai-contract
- contracts
- index
- graph
- query
- deps
- ask
- explain
- demo
- init
- fix
- status
- upgrade
- session

When helping with Playbook:
- check the Playbook GitHub README because it frequently auto-updates
- reference `docs/PLAYBOOK_PRODUCT_ROADMAP.md` as the north-star roadmap
- ensure Codex prompts include roadmap checkbox updates when relevant

## Playbook future ideas

Paused future feature:
- Cross-repo pattern learning is planned but paused. Do not lose track of it.

Future demo sync idea:
- contract-driven demo sync system using:
  - `demo:verify`
  - `demo:plan`
  - `demo:apply`
- goal: keep playbook-demo automatically aligned with the main Playbook product through managed sections

## New Playbook chat guidance

Zac wants guidance on when to start a new chat for Playbook development.

New Playbook chats should use phase-based titles, such as:
- Playbook - Phase 2 Repo Intelligence

A new Playbook chat should begin with a kickoff message referencing:
- `docs/PLAYBOOK_PRODUCT_ROADMAP.md`
- current phase
- next PR goal
- reminder to update roadmap checkboxes after changes

## Missing information rule

When repo files, logs, or implementation details are required:
- ask for the specific missing information first
- avoid guessing when critical repo context is missing

When an improvement is clearly necessary and fits the current change:
- integrate it into the current implementation plan
- do not leave it as a detached suggestion

## FawxzzyFinance

FawxzzyFinance is a planned finance tracking app product.

Likely direction:
- mobile-first PWA
- separate Supabase project
- still under the same general account ecosystem

## Canonical memory architecture rule

ChatGPT saved memory should be treated as a convenience cache, not the source of truth.

Atlas should hold durable canonical memory.

This profile should be available to:
- future Atlas workflows
- future Cortex bootstrapping
- Codex sessions working in Atlas
- Playbook planning flows
- assistant-profile ingestion systems

Rule:
Canonical user/project context belongs in versioned Atlas memory slots, not only in external assistant memory.

Pattern:
Use a small `AGENTS.md` pointer plus a full durable memory slot. Keep `AGENTS.md`
lightweight and keep the full profile in the canonical slot. Each governed
thread also maintains compact append-only checkpoints under
`runtime/atlas/thread-context/`; promote reusable rules and durable decisions
from those checkpoints into versioned memory slots.

Failure Mode:
If profile context only lives in ChatGPT saved memory, it can be lost, compressed, omitted, or become unavailable across tools. Avoid relying on it as the sole source of truth.
