# Local-Only Example Fixture Note

This note belongs to the active fixture graph for the deterministic local-only validator.

The validator may read this fixture but may not mutate owner repositories, deploy surfaces, secrets, live data, `_stack`, or external systems.
