# Local-Only Example Stub Note

This note-only leaf fixture exists to prove the first committed leaf payload slot inside the admitted Sandbox example pack without implying executable input data, expected-output truth, validator behavior, runner behavior, or any mutation authority.
